# Playbook Notes

- WHAT changed: Added pre-scene authenticated progression/settings hydration, revision-aware cross-device writes, safe advancement rebasing, reset conflict refusal, and a fine-pointer portrait browser adapter that reuses the phone maze/control composition without changing the phone branch.
- WHY it changed: The same login previously produced different stats because the app only wrote remote progression and continued to read device-local caches; a `499x958` narrow browser pane also missed the phone-specific maze/control cadence used at `390x844`.
- Evidence: `npm run verify:fast` (`13` files / `184` tests), focused account/browser parity packet (`4` files / `41` tests), `npm run build`, additive live Supabase migration/readback, and route-aware Preview captures at `390x844` and `499x958`.

Use this file to record meaningful Playbook-governed repo changes in a concise, reviewable format.

## 2026-07-04

- WHAT changed: Activated the repo-local Playbook install surface with `playbook.config.json`, a local verification gate, and Mazer-owned notes.
- WHY it changed: Mazer already had adoption evidence, but Playbook's local verification mode needs an explicit `verify:local` command and config surface.
- Evidence: `npm run verify:local`

## 2026-07-08

- WHAT changed: Added local-first player and AI-runner progression tracks, progression-aware maze scale tuning, hidden diagnostics, and level/rank color palettes for player/trail rendering.
- WHY it changed: Player progression and menu AI progression need separate learning lanes now, while keeping implementation data hidden from normal game UI.
- Evidence: `npx vitest run tests/reset/legacy-progression.test.ts tests/reset/legacy-cycle-telemetry.test.ts tests/reset/legacy-reset.test.ts tests/scenes/menu-render-frame.test.ts --maxWorkers 1`; `npm run verify:local`

- WHAT changed: Added a Mazer-native Supabase browser auth layer, in-game account overlay, Login/Log out menu actions, and account-scoped local learning/progression storage with guest fallback.
- WHY it changed: Account login is needed before long-term player-vs-AI progression can become user-specific, while non-player implementation receipts stay hidden behind diagnostics.
- Evidence: `npx vitest run tests/reset/legacy-auth.test.ts tests/reset/legacy-progression.test.ts tests/scenes/menu-render-frame.test.ts --maxWorkers 1`; `npm run verify:local`

- WHAT changed: Added viewport-aware progression scale capping, stricter full-viewport shell CSS, and small-tile corridor rendering so rounded 4px mobile path tiles keep the clean connected-maze shape.
- WHY it changed: High AI/player progression could generate phone-width mazes with sub-4px tiles at browser zoom 100%, making the menu look zoomed out, dense, blurry, and blocky compared with 90% zoom.
- Evidence: `npx vitest run tests/reset/legacy-progression.test.ts tests/reset/legacy-menu-layout.test.ts tests/scenes/menu-render-frame.test.ts --maxWorkers 1`; `npm run lint`; `npm run build`

- WHAT changed: Made generated main-menu demo navigation follow the clean canonical route by default while keeping the legacy mistake/backtrack route available for the fixed snapshot and explicit AI tests.
- WHY it changed: The player-facing menu AI was taking long humanized detours, which made navigation look broken and delayed the visible goal-to-deconstruct cycle.
- Evidence: `npx vitest run tests/ai/demo-walker.test.ts tests/reset/legacy-menu-demo-lifecycle.test.ts tests/reset/legacy-reset.test.ts --maxWorkers 1`; `npm run build`; browser diagnostics proof at `tmp/clean-menu-ai-cycle-proof.png`.

- WHAT changed: Added border-dock rendering for non-corner edge paths so corridors that reach the board edge connect cleanly into the maze border instead of ending as capped/cropped slabs.
- WHY it changed: Mobile menu/play mazes could show edge-touching pathways that looked like they bled off the grid without a deliberate border connection.
- Evidence: `npx vitest run tests/scenes/menu-render-frame.test.ts tests/reset/legacy-path-visual-style.test.ts --maxWorkers 1`; `npm run build`; browser mobile proof at `tmp/edge-dock-proof-405x958.png`.

- WHAT changed: Tightened progression maze-size capping to use the same snapped board and safe-inset math as the renderer, keeping phone-width menu demos at the selected base density and preserving readable 5px-plus mobile maze tiles instead of allowing 61+ cell menus to collapse into the blurry/blocky 4px lane.
- WHY it changed: The clean mobile maze visual regressed when progression scale grew faster than the available board frame at browser zoom 100%, making the board shrink and paths look dense compared with the prior clean style.
- Evidence: `npx vitest run tests/reset/legacy-progression.test.ts tests/reset/legacy-menu-layout.test.ts tests/scenes/menu-render-frame.test.ts --maxWorkers 1`; browser mobile proof at `tmp/clean-tile-sizing-proof-405x958.png`.

- WHAT changed: Added corner-aware perimeter rails for maze border docks so paths that touch the board edge near folded triangular facets connect through the edge gutter instead of stopping as clipped stubs.
- WHY it changed: The prior edge-dock pass connected border cells to the square board edge, but folded-corner cutouts made near-corner pathways look cut off before reaching the visible border.
- Evidence: `npx vitest run tests/reset/legacy-progression.test.ts tests/reset/legacy-menu-layout.test.ts tests/scenes/menu-render-frame.test.ts --maxWorkers 1`; browser mobile proof at `tmp/border-rail-proof-405x958.png`.

## 2026-07-09

- WHAT changed: Configured Mazer Vercel Supabase browser auth env vars from the live shared auth project, pulled ignored local `.env.local`, added `.env.example`, added a feature-gated remote progression sync module, and created the Supabase migration for `public.mazer_progression_states`.
- WHY it changed: Real account signup/login/logout needs deployed and local Supabase browser config before remote player/AI progression can safely sync across devices; remote progression stays local-first and opt-in until the schema is applied.
- Evidence: real Supabase QA auth smoke `signupUserCreated=true`, `signupSessionCreated=true`, `signinSucceeded=true`, `signoutSucceeded=true`; `npx vitest run tests/reset/legacy-auth.test.ts tests/reset/legacy-progression.test.ts tests/reset/legacy-remote-progression.test.ts tests/scenes/menu-render-frame.test.ts --maxWorkers 1`.

- WHAT changed: Added the Mazer-only Supabase storage contract, migration-backed account/profile tables, separate player and AI-runner progression storage, compact cycle receipt storage, and server-owned Stripe license/payment-wall tables.
- WHY it changed: Mazer needs its own tight data boundary before remote learning, per-account AI progression, account progression sync, and future paid licenses can ship without mixing game data into Fitness-owned storage.
- Evidence: `npx vitest run tests/reset/legacy-remote-progression.test.ts tests/reset/legacy-auth.test.ts tests/reset/legacy-progression.test.ts --maxWorkers 1`; `npm run lint`; `npm run build`.

## 2026-08-04

- WHAT changed: Fixed `tests/reset/legacyUnrealSourceFixture.ts` so an explicitly-set-but-blank `MAZER_LEGACY_UNREAL_RESTORE_ROOT` (empty string, spaces-only, or tabs/newlines-only) throws an actionable error instead of silently falling through to parent-directory discovery. The prior guard (`process.env[VAR]?.trim()` then `if (explicitRoot)`) treated `''` and `undefined` identically, since both are falsy -- now the raw value's presence is checked separately from its trimmed content, so "truly unset" still uses parent-walk (unchanged) while "set but blank" fails loudly. Added 4 new tests (empty string, spaces-only, tabs/newlines-only, and an explicit control test confirming truly-unset behavior is unchanged) to `tests/reset/legacy-unreal-source-fixture.test.ts`.
- WHY it changed: A whitespace-only explicit value is a real misconfiguration signal (e.g. a CI/shell template substituting an empty value into the variable) that should be surfaced, not silently masked by falling back to a fixture that happens to exist via parent-walk for an unrelated reason -- concealing exactly the kind of broken environment setup this variable exists to make visible.
- Rule: When distinguishing "not configured" from "configured but blank" for an environment variable, check the raw value's presence (`!== undefined`) before trimming/testing truthiness -- collapsing both into one falsy check silently treats a real misconfiguration as if the setting were absent.
- Evidence: `npx vitest run tests/reset/legacy-unreal-source-fixture.test.ts` (12/12 pass, 4 new), `npx vitest run tests/reset/legacy-reset.test.ts` with a valid `MAZER_LEGACY_UNREAL_RESTORE_ROOT` set (35/35 pass, unchanged), `npm run test` (canonical, 489/489 pass), `npx tsc --noEmit` (clean), `npm run build` (succeeds, no unintended tracked-file changes).
- Status: Applied

- WHAT changed: Added `tests/reset/menu-intent-runtime-integration.test.ts` (9 tests, canonical via the whole-directory `tests/reset` inclusion in `package.json`'s `test`/`test:verify` scripts -- no `package.json` edit needed) covering the full episode -> spectator plan -> intent runtime host -> session chain: `advanceToStep` is idempotent under repeated/backward calls (never grows `intentDeliveries`); negative/fractional/out-of-bounds/`NaN` cursors passed to `advanceToStep`/`getFeedState`/`getBoardState` clamp safely instead of throwing or returning garbage, and an out-of-bounds cursor resolves to the exact same state as the latest reached step; the input `MazeEpisode` object is never mutated; two sessions built from the same episode produce an identical intent-kind sequence and identical board state (deterministic); a freshly-constructed session (the menu-restart case) always starts with zero deliveries regardless of a prior session's state (no cross-session leakage); the `core-only` content profile never emits any mechanic-telegraph intent kind across a full traversal; and a short path below `demoSpectator.ts`'s 6-segment floor falls back to core-only board state end-to-end, matching the already-proven unit-level boundary.
- WHY it changed: The existing `tests/scenes/menu-intent-runtime.test.ts` (9 tests, not itself canonical -- `tests/scenes` only explicitly lists `menu-render-frame.test.ts`) covers the display-controller dwell/coalesce/debounce contract extensively but never asserts cursor-safety, duplicate-effect freedom, input immutability, determinism, or clean-session-reset at the runtime-integration layer. Every one of those properties was independently verified against the real runtime (via a throwaway probe script, not assumed) before writing any assertion -- all were already correct, so this is coverage of a working contract, not a fix.
- Rule: A module whose public methods accept arbitrary numeric "step" arguments (state getters, cursor-driven APIs) needs its out-of-bounds/malformed-input clamping behavior explicitly regression-tested, even when the implementation already handles it correctly -- an incidental correctness property with no test is one accidental refactor away from becoming a real defect.
- Failure Mode: A future change to `advanceToStep`'s while-loop bound check, or to `getFeedState`/`getBoardState`'s `Math.max(0, Math.min(...))` clamping, could silently reintroduce duplicate narrative-event growth on repeated calls or an out-of-bounds crash/garbage state, with nothing catching it before it shipped.
- Decision: `MISSING_INTEGRATION_COVERAGE_ONLY` -- test-only addition, no production runtime code changed, no `package.json` change.
- Evidence: `npx vitest run tests/reset/menu-intent-runtime-integration.test.ts` (9/9 pass), `npx vitest run tests/scenes/menu-intent-runtime.test.ts` and `tests/scenes/menu-render-frame.test.ts` (unaffected, 9/9 and 50/50), `npm run test` (canonical, 498/498 pass, 63/63 files -- confirms the new file runs as part of the real command), `npx tsc --noEmit` (clean), `npm run build` (succeeds, no unintended tracked-file changes).
- Status: Applied

## 2026-08-03

- WHAT changed: Removed `src/render/hudRenderer.ts` (the `createDemoStatusHud` Phaser HUD overlay and its supporting types/profiles), confirmed dead: zero references to the file path or any of its exported symbols (`HudThemeStyle`, `createDemoStatusHud`) anywhere in source, tests, or scripts. Last touched 2026-06-27, from the original "menu demo AI" build, superseded by the current `legacy-runtime/legacyPlayHud.ts` HUD system.
- WHY it changed: A 409-line Phaser-rendering module with a full theme/chrome/deployment-profile configuration surface was carrying real maintenance weight (it would need updating alongside any future HUD/theme/viewport refactor) while contributing nothing -- confirmed unreachable via a repo-wide grep for both the file path and every exported symbol, plus a clean `tsc --noEmit` and `npm run build` both before and after removal.
- Evidence: `npx tsc --noEmit` (clean both before and after removal), `npm run build` (succeeds, no unresolved imports, no unintended tracked-file changes from the build itself), `npm run test` (485/485 pass, matching the pre-removal baseline exactly), repo-wide grep for `hudRenderer|createDemoStatusHud|HudThemeStyle` returning zero hits after removal.

## 2026-08-04

- WHAT changed: Fixed `src/domain/ai/demoSpectator.ts`'s `createDemoSpectatorPlan`, which raises the "path too short to safely populate every full-profile mechanic" fallback floor from `segmentCount < 6` to `segmentCount < DEMO_SPECTATOR_FULL_PROFILE_MINIMUM_SEGMENT_COUNT` (17). Verified by exhaustive enumeration (segmentCount 6 through 2000) that under the old floor, every segmentCount from 6 through 16 produced at least one cross-mechanic tile collision -- most commonly the pressure-door's tile landing on the patrol lane's leading tile, and in several cases the hazard tile additionally landing inside the patrol lane's range -- because each mechanic's cursor lower bound only guarded against the previous mechanic's *starting* cursor, not its full occupied tile range (the door spans 2 tiles, the patrol lane spans 3). 17 is the smallest segmentCount where every bound clears the prior mechanic's full range; reproduced with real `generateMaze()` output (not just synthetic straight paths) at `scale: 15/20` for several seeds, and with the standard `size: 'small'` preset (seed 185, a real ~0.5%-of-seeds case).
- WHY it changed: `createDemoSpectatorPlan` feeds `menuIntentRuntime.ts`'s trap/item/puzzle/warden-patrol placement (`buildTrapContracts`, `buildItemDefinitions`, `buildPuzzleDefinitions`, and the `WardenGraphAgent` start tile). A short-but-not-tiny solution path (17 tiles or fewer) could place two distinct demo mechanics -- e.g. the door and the patrol lane -- on the exact same maze tile, a duplicate-effect placement defect. The existing "too short, fall back to core-only" guard already existed for exactly this class of problem; its threshold was just miscalibrated too low to prevent the overlap it was meant to prevent.
- Rule: When a "path/route too short" fallback guard exists specifically to avoid overlapping placements, its threshold must be derived from the actual minimum space each placed element needs (including multi-tile elements' full occupied range), not an arbitrary round number -- verify the chosen floor by exhaustively enumerating segment counts up to the floor and confirming zero collisions, not just spot-checking one or two values.
- Failure Mode: Any future addition of a new full-profile mechanic, or narrowing of an existing cursor's bounds, could silently reintroduce collisions at short segment counts with nothing catching it before a menu-intent-runtime consumer went live with visibly overlapping demo mechanics.
- Decision: `DUPLICATE_RUNTIME_EFFECT_DEFECT`
- Evidence: `npx vitest run tests/reset/demo-spectator-mechanic-placement.test.ts` (4/4 pass, new file), `npx vitest run tests/ai/demo-walker.test.ts -t createDemoSpectatorPlan` (10/10 pass, includes 2 updated boundary tests), `npx tsc --noEmit` (clean), `npm run test` (canonical; 499 passed / 4 pre-existing-and-unrelated `MAZER_LEGACY_UNREAL_RESTORE_ROOT` fixture failures reproduced identically on a clean `git stash` of this change, confirming they predate it), `npm run build` (succeeds).
- Status: Applied

- WHAT changed: Implemented Wave 0A ("Decision registry and architecture guardrails") of the Mazer UI rework, per the authoritative planning handoff bundle `mazer-everything-bundle-20260803`. Added `docs/contracts/mazer-ui-rework-decision-registry.v1.json` (a machine-readable registry of the 12 locked planning decisions -- product direction, Phaser/DOM/shared-contract ownership split, corridor-not-tiles shipping default, profiles-vs-themes, single canonical theme with noir/ember/vellum/aurora/monolith archived, proof-surface/Watch Pass/Planet 3D/Future Phaser classifications, no-big-bang-MenuScene-rewrite, PR #83 protected-path hold, and no-redesign-complete-without-acceptance-matrix -- each with a stable ID, reusing the bundle's `spec/surface-disposition.json` entrypoint classifications directly), `docs/architecture/MAZER-UI-REWORK-DECISION-REGISTRY.md` (human-readable companion, matching the existing `docs/architecture/MAZER-*-CONTRACT.md` convention), `docs/architecture/MAZER-UI-REWORK-WAVE-0A-RECONCILIATION.md` (a current-source reconciliation report classifying each decision against current `main` as SATISFIED/PARTIALLY_SATISFIED/NOT_YET_IMPLEMENTED/PR83_PROTECTED_HOLD -- 5 already SATISFIED by pre-existing behavior including corridor-locked rendering in `legacyPathVisualStyle.ts` and satellite-entrypoint isolation, 1 PARTIALLY_SATISFIED with a documented caveat about an unreachable `PresentationTheme` type in `src/boot/presentation.ts` that coincidentally reuses the bundle's archived theme names in dead/unwired code, 5 correctly NOT_YET_IMPLEMENTED pending later waves, 1 PR83_PROTECTED_HOLD, zero CURRENT_SOURCE_CONFLICT or EVIDENCE_MISSING), `scripts/check-decision-registry.mjs` (pure validator functions -- `collectDecisionRegistryViolations`, `collectEntrypointExistenceViolations`, `collectProtectedPathViolations` -- following the existing `scripts/check-architecture.mjs` pattern), and `tests/architecture/decision-registry-contract.test.ts` (22 tests: the real registry passes with zero violations; every registered entrypoint is cross-checked as existing in the live repo; 15 synthetic in-memory mutations each independently trigger their targeted violation -- second canonical/public theme, zero canonical themes, profile mislabeled as theme by kind, profile id reused as a canonical theme, Watch Pass billing claim, Planet 3D included in core-v1 or added to the core release acceptance set, tile-square marked shipping default, proof entrypoint reclassified to product/releaseGate without justification, redesign-complete claimed without evidence, duplicate decision id, unknown decision-id references -- plus a protected-path self-check run against this working tree's real `git status --short` output, not just synthetic input).
- WHY it changed: The bundle's own `blueprint/09_MIGRATION_WAVES_AND_PR_LANES.md` scopes Wave 0A to "new docs/specs/tests only; no runtime visual mutation" specifically so the twelve-decision product/architecture boundary is registered and machine-checkable before any of the later renderer-split, token, or MenuScene-decomposition waves begin -- and so an architecture test exists that will fail loudly if a future change accidentally reintroduces a second public theme, mislabels a profile as a theme, lets Watch Pass claim real billing, pulls Planet 3D into core-v1, defaults to visible tile squares, or lets a lane claim the redesign "complete" without evidence.
- Rule: A "decision registry" wave is only real if its checker function is actually exercised against both a synthetic failing case for every stated rule and the live repository state (entrypoint existence, protected-path git status) -- documentation-only "registries" with no validator, or validators with no negative-case tests, silently rot the moment the registry and the source drift apart.
- Failure Mode: Without this contract, a later wave's PR could quietly ship a second public theme, mislabel a TV/OBS/arcade profile as a theme, let Watch Pass's paywall page claim real production billing, or let Planet 3D sneak into the core-v1 release acceptance set, with nothing in CI catching it before it shipped.
- Independent drift check: re-ran the orchestrating session's verification myself rather than trusting it -- confirmed `462030ac942e4ffecd442fe0ceeb22584dcda931` is an ancestor of `HEAD` (`git merge-base --is-ancestor`), confirmed all 15 commits between that snapshot and `HEAD` (`git log --oneline 462030ac..HEAD`) touch none of `MenuScene.ts`/`menuRuntimeDiagnostics.ts`/`legacyAuth.ts`/`legacyPlayerMessage.ts`/`vite.config.ts`/`package.json` (`git log --name-only -- <paths>` returns empty), and confirmed all 8 HTML entrypoints from `spec/surface-disposition.json` exist in the repo root exactly as named. Classification: `WAVE0A_CURRENT_AND_READY` -- agree with the orchestrating session's own classification.
- Decision: `WAVE0A_CURRENT_AND_READY`
- Evidence: `npx vitest run tests/architecture/decision-registry-contract.test.ts` (22/22 pass), `npm run test:architecture` (40/40 pass across all 6 architecture-contract files, canonical via the existing `test:architecture` script -- no `package.json` change needed), `npx tsc --noEmit` (clean), `npm run build` (succeeds, all 8 entrypoints still build), `npm run test` (canonical; 499 passed / 4 pre-existing-and-unrelated `MAZER_LEGACY_UNREAL_RESTORE_ROOT` fixture failures, matching the prior baseline exactly -- confirms no regression), `git status --short` (5 new untracked files only: `docs/architecture/MAZER-UI-REWORK-DECISION-REGISTRY.md`, `docs/architecture/MAZER-UI-REWORK-WAVE-0A-RECONCILIATION.md`, `docs/contracts/mazer-ui-rework-decision-registry.v1.json`, `scripts/check-decision-registry.mjs`, `tests/architecture/decision-registry-contract.test.ts` -- plus this `docs/PLAYBOOK_NOTES.md` entry; zero tracked-file modifications), `gh pr diff 83 --repo fawxzzy/mazer --name-only` and `gh pr diff 82 --repo fawxzzy/mazer --name-only` (zero overlap with the changed-file list above), `package.json` untouched.
- Status: Left uncommitted for independent review per the orchestrating session's instruction.

- WHAT changed: Corrected `docs/architecture/MAZER-UI-REWORK-WAVE-0A-RECONCILIATION.md`'s `single-canonical-theme` finding. A fresh reachability re-proof of `src/boot/presentation.ts`'s `PresentationTheme` surface (requested explicitly, to check whether it was safe to delete) found the prior wave's "dead code importing dead code, only consumer is `intentFeedRenderer.ts`, itself unreferenced" claim undercounted real consumers: `scripts/analysis/mazer-variety-analysis.ts` genuinely calls `resolveAmbientFamilyTheme(...)` and indexes `AMBIENT_FAMILY_THEME_PAIRING_POLICY[...]` for real maze-variety curation logic (not wired to any npm script/CI, but real, functioning source), and `tests/scenes/demo-build.test.ts` directly unit-tests most of `presentation.ts`'s boot-config resolvers alongside `BootScene` -- but that test file is non-canonical (absent from `package.json`'s `test`/`test:verify` scripts) and **currently fails 40 of its 44 tests** against real current source (several assertions expect `BootScene.prototype.create` to call `this.scene.start` with a second config argument and to invoke `resolveBootPresentationConfig`, neither of which the real, current 11-line `BootScene.ts` does) -- meaning that test file is itself stale/abandoned, not evidence of a live boot-time integration. No deletion was performed: the theme-naming surface is entangled in the same file as the genuinely-used analysis-script dependency, so a clean deletion is not possible without first making a product/tooling decision about `mazer-variety-analysis.ts`'s own future, which is outside this bounded cleanup's scope.
- WHY it changed: An inaccurate "safe to delete, nothing uses it" claim in an architecture-decision reconciliation report is worse than no claim at all -- it would have licensed a future cleanup wave to delete real, functioning source (breaking `mazer-variety-analysis.ts`) on the strength of an incomplete reachability search. The correction preserves the original, still-true conclusion (no PUBLIC/reachable theme switch exists, so `single-canonical-theme` is not currently violated) while retracting the specific "safe to delete" characterization.
- Rule: "No live consumer" claims about a source file must search test files and standalone/tooling scripts as thoroughly as production `src/` call sites before licensing a deletion -- a file with zero *production* callers can still have a real, if non-automated, consumer, and a dedicated test file's mere existence is not itself proof of a live integration if that test file is non-canonical and already failing against current source.
- Failure Mode: A later wave trusting the original "dead code, safe to delete" framing at face value could have deleted `src/boot/presentation.ts` (or the theme-related exports within it), silently breaking `scripts/analysis/mazer-variety-analysis.ts` the next time anyone tried to run it, with nothing in CI catching the break since that script isn't wired to any automated check.
- Decision: `AMBIGUOUS_HOLD` -- not `DEAD_UNREACHABLE` (a real, if tooling-only, consumer exists) and not `LIVE_COMPATIBILITY_CONTRACT` in the production sense (nothing shipped depends on it). No deletion performed; this is a documentation-only correction.
- Evidence: `grep -rln "boot/presentation\|PresentationTheme" src tests scripts *.html` (4 referencing files, not 1 as previously characterized); `grep -rln "resolveBootPresentationConfig\|resolveBootPresentationVariant\|..." src --include="*.ts"` excluding `presentation.ts` itself (zero production callers, confirmed); `cat src/scenes/BootScene.ts` (11 lines, imports only `phaser`, no reference to `presentation.ts`); `grep -c "boot/presentation" src/scenes/MenuScene.ts` (0); `npx vitest run tests/scenes/demo-build.test.ts` (40 failed / 4 passed, out of 44 -- reproduced directly); `grep -n "mazer-variety-analysis" package.json` (no matches -- confirms not CI/script-wired) alongside `grep -n "resolveAmbientFamilyTheme\|AMBIENT_FAMILY_THEME_PAIRING_POLICY" scripts/analysis/mazer-variety-analysis.ts` (multiple real call/index sites, confirms genuine usage); `git log --oneline -- src/boot/presentation.ts` (multiple deliberate feature commits, not single-commit accidental residue).
- Status: Applied (documentation correction only; no production or tooling source touched)

- WHAT changed: Reconciled the remaining `tests/boot/` corpus (6 files: `boot-status`, `boot-timing`, `canvas-resolution`, `install-surface`, `orientation-lock`, `viewport-geometry`) after the prior `demo-build.test.ts` cleanup left it uninvestigated. Unlike `demo-build.test.ts`, all 6 files test real, current production modules under `src/boot/` -- confirmed via git-blob-hash comparison that the source they test is unchanged and current, via grepping for genuine production consumers of each module (`src/boot/main.ts`, `src/scenes/MenuScene.ts`, `src/render/textCrispness.ts`), and via a line-by-line comparison against `tests/scenes/menu-render-frame.test.ts`'s superficial `toContain()` source-text checks confirming no real behavioral duplication. All 6 files already passed standalone; the directory was simply never wired into `package.json`'s canonical `test`/`test:verify` scripts. Moved all 6 files unchanged (pure `git mv`, confirmed identical blob SHA before/after via `git hash-object`) from `tests/boot/` into `tests/reset/`, which is canonical via whole-directory inclusion -- no `package.json` edit needed, since import paths (`../../src/boot/...`) resolve identically at the same directory depth.
- WHY it changed: 6 files of real, passing, currently-accurate coverage were silently excluded from every canonical verification run (`npm run test`, `npm run test:verify`) purely because of their directory location, not their content -- meaning a regression in any of `bootStatus.ts`/`bootTiming.ts`/`canvasResolution.ts`/`installSurface.ts`/`orientationLock.ts`/`viewportGeometry.ts` would have shipped undetected by the canonical command a human or CI would actually run.
- Rule: "non-canonical" and "stale" are independent properties of a test file -- a file being outside the canonical test denominator says nothing about whether its content is accurate; each must be verified separately (compare against the `demo-build.test.ts` cleanup, where staleness was the real finding, versus this one, where canonicalization was the fix).
- Failure Mode: A real regression in boot-sequencing, viewport geometry, orientation lock, or install-surface behavior could ship without any canonical test run ever exercising the 6 files that specifically cover it.
- Decision: `NONCANONICAL_BUT_VALUABLE` for all 6 files (no file classified `STALE_AFTER_ARCHITECTURE_PIVOT`, `DUPLICATED_BY_CANONICAL_TEST`, `LIVE_CONTRACT_REGRESSION`, `HISTORICAL_EVIDENCE_ONLY`, or any `*_COLLISION_HOLD` -- confirmed via `gh pr diff 82/83 --name-only`, no overlap with either PR's owned paths).
- Evidence: `git hash-object` matching pre/post-move blob SHAs for all 6 files (pure rename, zero content drift); `grep -rln` for production consumers of each `src/boot/*` module; `npx vitest run` on all 6 moved files individually (33/33 pass); canonical `npm run test` (536 passed / 4 pre-existing-and-unrelated `legacy-reset.test.ts` fixture failures, matching the pre-move baseline exactly); `npx tsc --noEmit` (clean); `npm run build` (succeeds); `git diff --stat -- package.json` (empty).
- Noted, not acted on (out of scope for a pure canonicalization move): `bootTiming.ts` has zero production consumers anywhere in `src/` (confirmed via repo-wide grep) -- real, compiling, correctly-tested instrumentation code that appears to be orphaned rather than wired into any live boot path. Worth a future dead-code reachability pass.
- Status: Applied

- WHAT changed: Resolved the `bootTiming.ts` reachability question flagged by the prior entry, then canonicalized a different non-canonical test corpus as a fallback. Full reachability re-proof (static/dynamic imports, barrel exports, string-based symbol references, `vite.config.ts` aliases, browser globals, HTML entrypoints, decision-registry docs, git history) found `bootTiming.ts` is not simply dead: `git log -- src/boot/bootTiming.ts` and `git show f98cebb3` confirm it was genuinely wired into `src/scenes/MenuScene.ts`'s `create()` in commit `f98cebb3` (2026-04-16), and `git show e6517848` ("Port Mazer front door toward legacy parity", 2026-06-28) confirms every `markBootTiming`/`buildBootTimingReport`/`logBootTimingReport` call was deleted as collateral of a large, scope-unrelated `MenuScene.ts` rewrite -- not a deliberate decision to drop boot-performance instrumentation. A real (currently broken) consumer still exists: `scripts/analysis/capture-boot-timing.mjs`, wired to the still-present `npm run boot:timing` script, expects `window.__MAZER_BOOT_TIMING__` to be published by `MenuScene.ts`; nothing publishes it today. The only viable fix -- re-adding `markBootTiming(...)` calls to `MenuScene.ts` -- collides with PR #83's owned paths (`gh pr diff 83 --repo fawxzzy/mazer --name-only` lists `src/scenes/MenuScene.ts`). No source change was made to `bootTiming.ts`, `MenuScene.ts`, or any other PR #82/#83-owned path. Per this wave's fallback instruction, ran a fresh 5-candidate correctness/coverage scan and selected the highest-confidence candidate: `tests/input-human/` (4 files, 42 tests covering `src/input-human/{reducer,keyboard,touch,actions,policy}.ts` and `src/legacy-runtime/legacyDirectionalIntent.ts`) was outside Mazer's canonical `tests/reset/` denominator despite covering exactly the code `MenuScene.ts` calls on real keyboard/touch input. Moved all 4 files unchanged (pure `git mv`, identical blob SHA before/after via `git hash-object`) from `tests/input-human/` into `tests/reset/`; no `package.json` change needed since import depth (`../../src/...`) is identical between the two directories. An independent review then found this rename broke a *third* consumer nobody had checked: `scripts/verify/run-fast-verify.mjs` (wired to `npm run verify:fast`) hardcoded `tests/input-human/directional-intent.test.ts` and `tests/input-human/touch.test.ts` in its changed-file -> test-selection rule table, plus a `/^tests\/input-human\//` directory-pattern trigger -- both now pointing at a directory that no longer exists, reproducibly causing `npx vitest run` on those stale paths to exit 1 with "No test files found" the next time that rule fired. Fixed by updating the two listed paths to `tests/reset/directional-intent.test.ts` / `tests/reset/touch.test.ts`, and narrowing the directory-wildcard pattern to an exact-filename pattern (`/^tests\/reset\/(directional-intent|touch)\.test\.ts$/`) scoped to just those two files, since `tests/reset/` is itself a large shared canonical directory a bare wildcard would over-match. Left the `tests` run-list and pattern scope otherwise identical to before the rename (still only the same 2 of the 4 moved files, matching the original rule's pre-existing scope) rather than expanding it.
- WHY it changed: The prior entry's "worth a future dead-code reachability pass" note turned out to matter -- `bootTiming.ts` is not dead code, it's the orphaned half of a wiring break caused by an unrelated rewrite, with a real tooling consumer still expecting its output. That distinction (`MISSING_PRODUCTION_INTEGRATION`-shaped, not `DEAD_UNREACHABLE`) determines the correct disposition is "hold, don't touch, don't delete" rather than either silently leaving it miscategorized as dead or deleting it. Since the only real fix path is blocked by PR #83 ownership, this wave banked value elsewhere instead of leaving the lane empty: `tests/input-human/`'s 42 tests cover the same class of live, player-facing input-handling code (`MenuScene.ts`'s real keyboard/touch/directional-intent path) that the two prior `tests/boot/` and `demo-build.test.ts` cleanups targeted, and had the identical "non-canonical, not stale" defect -- real, passing, currently-accurate coverage silently excluded from `npm run test`/`test:verify` purely by directory location. The `run-fast-verify.mjs` break was missed on the first pass because verifying a pure rename focused on "does content drift" and "does the canonical suite still pass," not on grepping the whole repo for scripts that reference the old path by string literal outside any test runner's own file discovery.
- Rule: A "pure rename, zero content drift, canonical suite still green" self-check is necessary but not sufficient -- a rename can still break any script that references the old path as a hardcoded string rather than discovering files by glob (here, a changed-file-to-test-selection rule table). Grep the whole repo for the old path string, not just the canonical test runner's own file list, before calling a rename complete. And separately: a module with a real (if currently non-functional) tooling/script consumer is not dead code even with zero production `src/` importers -- confirm whether an apparent orphan is the *cause* side or the *effect* side of a wiring break by checking git history for the symbol's introduction and removal, not just its current reachability.
- Failure Mode: Without the reachability proof, a future cleanup pass could have deleted `bootTiming.ts` and its canonical test as "unreachable dead code," permanently discarding working instrumentation and silently breaking `npm run boot:timing` in a way nobody would notice until someone tried to run it. Separately, a regression in keyboard/touch/directional-intent input handling could have kept shipping undetected by any canonical test run. And without the `run-fast-verify.mjs` fix, the next commit touching `src/input-human/` or either renamed test file would have made `npm run verify:fast` fail outright on a path-not-found error unrelated to any real regression, wasting a developer's time chasing a false failure.
- Decision: `PR83_COLLISION_HOLD` for `bootTiming.ts` (no source or test change made to it); `NONCANONICAL_BUT_VALUABLE` for the 4 `tests/input-human/` files moved to `tests/reset/`.
- Evidence: `git log --oneline -- src/boot/bootTiming.ts`, `git show f98cebb3 -- src/scenes/MenuScene.ts` and `git show e6517848 -- src/scenes/MenuScene.ts` (wiring added then removed as collateral of an unrelated rewrite); `cat scripts/analysis/capture-boot-timing.mjs` (real Playwright-based consumer reading `window.__MAZER_BOOT_TIMING__`); `grep -n "boot:timing" package.json` (script present, untouched); `gh pr diff 83 --repo fawxzzy/mazer --name-only` (confirms `src/scenes/MenuScene.ts` collision); `git hash-object` blob-SHA match pre/post-move for all 4 renamed files; `npx vitest run tests/reset/input-human.test.ts tests/reset/keyboard.test.ts tests/reset/touch.test.ts tests/reset/directional-intent.test.ts` (42/42 pass); canonical `npm run test` (578 passed / 4 pre-existing-and-unrelated `legacy-reset.test.ts` `MAZER_LEGACY_UNREAL_RESTORE_ROOT` fixture failures, consistent with every prior wave's baseline); `npx tsc --noEmit` (clean); `npm run build` (succeeds); `git diff origin/main --name-only` (only the 4 renamed files; `src/boot/bootTiming.ts`, `src/scenes/MenuScene.ts`, `package.json`, and every PR #82/#83-owned path confirmed untouched); `git diff --stat -- package.json` (empty).
- Status: Applied
