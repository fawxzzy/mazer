# Mazer Auth, AI, And Visual Completion Marker

Last updated: 2026-07-09

This marker tracks the current multi-pass chunk requested after the production auth/progression setup: player-facing login gate, remote cycle receipts, AI/playbook progression contracts, and visual polish. It is separate from the completed mechanics/mobile marker.

## Current Overall Marker

Overall: 94%

## Work Lanes

| Lane | Marker | Status |
| --- | ---: | --- |
| Player-facing auth gate | 94% | Local guest gate keeps the animated menu visible and exposes only Login to guests. Auth overlay is configured with browser-safe Supabase env, duplicate validation/error copy is no longer rendered as loose inline form text, and form controls are backed by real native browser inputs so mobile keyboard and automation entry work. API-level generated QA signup/login/logout is proven after email confirmation was disabled, and the requested real account was created plus sign-in/logout verified through the browser-safe Supabase client. Visible browser QA proves disposable negative login entry through the native inputs with no loose raw status text, then proves visible login for the requested confirmed account, reload persistence under the user-scoped progression key, and pause-menu logout back to guest progression. This pass ports the useful fitness-app persistent-login pattern into Mazer's Vite/canvas runtime: remembered identity is now a JSON state with `ready` and `reauth-required`, the Supabase browser client bootstraps from `getSession()`, one internal `onAuthStateChange` listener dedupes token-free session signatures, authenticated sessions sync the remembered account without storing tokens, explicit no-session reads downgrade remembered accounts back to `reauth-required`, and sign-out is local-only so one browser logout does not revoke every account session. The build wrapper forces local `.env.local` `VITE_*` values to override stale inherited shell env, fixing the wrong-project bundle issue that caused visible auth fetch failures. Remaining work is longer mobile/form soak plus production env parity verification before forcing the gate as the only production path. |
| Remote progression and cycle receipts | 85% | Compact remote `mazer_cycle_receipts` writes are behind the existing opt-in env flag and authenticated session guard. Live generated QA auth proved cycle receipt insert/read, player progression upsert/read, AI progression upsert/read, logout, and login-again. Direct Supabase row exports now normalize into Atlas-safe cycle-learning reports, and the compact receipt JSON now carries the measured maze-complexity breakdown without adding schema columns. Remote sync outcomes now return typed player-message receipts for guest, missing-client, and failure states while keeping disabled sync silent and success non-noisy. The menu diagnostics payload records the latest remote-sync result for QA/Atlas without exposing developer details to normal players. Remote progression track summaries preserve `recentSignals`, and remote cycle receipt JSON now carries the derived `aiDecisionScore`, route-efficiency pressure, explicit shortest viable path length, route overrun steps/ratio, and render-safety pressure beside `aiDecisionSummary`, keeping account-scoped AI/player pacing context aligned with local progression. The authenticated requested-account AI runner reset was checked and bounded to `mazer_ai_progression_states` only, with player progression and cycle receipts left untouched. |
| AI/playbook progression contracts | 70% | Existing docs define separate player and AI-runner tracks, compact receipts, human-local-memory AI behavior, complexity pressure, and Atlas-safe ingestion. Recent passes added remote-row report normalization proof, menu-demo gate diagnostics, a generated-route regression proving the menu AI can leave the start as soon as staged construction is settled, bounded optional-branch retargeting, and a removal of the fixed menu snapshot's forced canonical solver replay. This pass removes the remaining visual-goal inference fallback that could expose the end tile as the AI memory target before the AI had locally reached it, replaces misleading straight-line scoring with cached wrapped-topology goal-distance scoring for adjacent/remembered choices, preserves frontier recovery through known visited tiles, and adds a generated-route regression that fails if the visible memory target leaks an unseen end tile. Local diagnostics, remote receipts, and Atlas-safe reports now expose a compact `aiDecisionScore` with pressure, reliability, recovery, route-noise, retarget pressure, and `clean/searching/chaotic` signal for every AI receipt. Cycle learning now also exposes shortest-path comparison fields, `routeEfficiencyPressureScore`, and `renderSafetyPenaltyScore`, so Atlas/playbook can distinguish "got lost" from "game rendered poorly" and compare actual routes against the generated maze's shortest viable route. This pass adds an Atlas-side validator and bounded consumer receipt for `mazer.cycle-learning.report.v1`, rejecting raw path leakage and selecting a decision focus before any tuning system can consume the report. The report CLI can now also write `mazer.cycle-learning.consumer.v1` to a direct file or timestamped/latest Atlas receipt root. Still needs floor/rank scaling formulas and a real Atlas-side consumer process wired into the broader stack. |
| Level/rank/complexity contract | 56% | `MAZER_LEVEL_RANK_COMPLEXITY_CONTRACT.md` now formalizes current implemented truth and the intended next contract: level/rank derive from target complexity, player and AI-runner tracks stay separate, player performance drives player maze target complexity, AI-runner performance acts as the tuning test dummy, maze complexity is additive/receipt-backed, and future danger/enemy/trap/item terms remain deferred. Recent passes implement first-pass complexity terms for edge-wrap continuity, branch/split pressure, dead-end pressure, fill quality, edge-wrap relief when folded paths make the shortest route unusually short, weighted split/dead-end pressure based on wrapped goal distance and plausible local choices, route-efficiency pressure from actual path waste, explicit shortest viable path overrun metrics, and render-safety pressure from frame timing. Progression now uses bounded target pacing instead of jumping target complexity to measured complexity, a six-signal smoothing window with consistency bonuses/penalties capped to safe target steps, and Atlas-safe reports preserve the active progression pacing review from runtime diagnostics. This pass replaces the coarse time-only public Score pressure with a first-pass numeric run-quality score built from timer pace, shortest-path route efficiency, wrong turns, backtracks, reset use, and render stability; route waste can now force `ease`, unsafe frame timing holds difficulty, and the stored `paceScore` field remains compatibility-only while the UI label stays `Score`. Play-mode start and goal-reset generation use fresh procedural seed overrides mixed from runtime entropy plus the player track's target complexity, completed cycles, level, and score; play-mode scale and shortcut budget continue to resolve from player skill/target-complexity and viewport safety. The public level badge removes debug-style `C:`/`Sig:` labels and shows a clearer two-line AI skill readout: skill level/rank first, then run count/score/time. Internal target-complexity and signal state stay in diagnostics/playbook receipts instead of the player-facing box. Menu-demo cycle timing resets at the exact static-build gate release, so Score/signal no longer include maze construction/preroll wait time. Still needs longer-sample calibration, floor/rank scaling formulas, and implemented target-complexity tuning from the consumer receipt. |
| Visual defaults and menu controls | 79% | Fresh sessions default to dark mode, animated background, stick controls, trail fade off, green Start/player/trail/level palette, guest Login-only, and a cleaner authenticated Start/Options layout. Local screenshots prove guest Login-only, the cleaned auth overlay, native input typing, and authenticated menu spacing. Options/pause toggles now emit concise in-panel setting confirmations through the shared message system. Recent passes harden the Start button text and level display text to the green action color regardless of progression palette tier and give the authenticated Start/Options stack an explicit 10-14px vertical gap. The menu compass sits in the top-center border notch, and the Options overlay now starts with a compact Player Guide legend for Compass, Start, End, and Player instead of exposing target-complexity diagnostics to normal players. Account/Log out is now restricted to the main-menu Options/auth surfaces; the play pause overlay exposes only Reset and Menu, with the back chevron serving as Resume. The play HUD pause control is now centered under the timer on phone portrait layouts instead of sitting left of the timer. Play mode now places the player skill badge above the maze with padding while the main-menu AI badge stays under the menu maze, and both the played-game player marker and main-menu AI runner now use the high-contrast green beacon language. Reset restarts the current maze and marks the current cycle as reset-used for analytics, while Menu is the explicit opt-out from the perpetual play loop. Needs longer mobile menu soak before calling the menu layout fully settled. |
| Player-facing Options info guide | 52% | New lane for the requested Options info section. Current implementation moves the guide to the top of the main-menu Options overlay and simplifies it into a clean player legend: Compass points to the end goal, Start marks where the run begins, End marks the clear target, and Player marks the active position/trail. The same guide is reused inside the play pause scroll area, and the played-game pause overlay now renders `PLAYER GUIDE` before `Game Toggles` and `Move Speed` instead of burying it lower in the scroll flow. This pass enlarges the guide typography/card height and adds mode-specific badge explanations: menu explains AI Level, Score, Time, and run cycles; played-game pause explains Player Level/Rank and Runs. The public skill badges now avoid debug shorthand such as `Sig:` and the played-game badge no longer falls back to the compact `Lv` label. Player target complexity, measured complexity, player rank, AI rank, and wrap-relief diagnostics remain intentionally removed from this user-facing card. Still needs scroll/fade polish under extreme mobile heights and richer mini-illustrations beyond the current glyphs. |
| DiscordOS Mazer feedback board | 90% | DiscordOS now has a live `mazer` forum board under the `Project Feedback Boards` category with marker-backed cards for AI/progression, Options info, edge-wrap topology, crisp graphics, icon-quality target, fluid controls, auth gate, auth/playbook sync, unified player messages, animation smoothness, play-mode lifecycle, iridescent player/trail material, diagonal path graph contract, visual-proof discipline, and DiscordOS board discipline. The board config remains repo-owned, no-send-safe for dry runs, and verified by the DiscordOS board contract. Live sync/readback proves the forum placement, card starter messages, rich required sections, and not-done reactions. Still needs round-trip discipline where each future Mazer implementation pass references the related DiscordOS card and closes with a board-linked receipt before cards are moved to complete. |
| Animation smoothness | 40% | Title, corner glimmer, and trail pulse frame cadence tightened from coarse intervals to 33ms proof cadence. Needs mobile soak/perf proof, remaining loop jitter review, and smoother lifecycle handoff so deconstruct/build pauses do not freeze orbit/background/title effects. |
| Play-mode completion lifecycle | 60% | First functional lifecycle pass is in place. When the player reaches the goal, play input locks, the cycle receipt is recorded, the reset hold is preserved, the shared staged lifecycle enters deconstructing, the player is removed before trail fade, trail/path visibility follows the staged tile order, the handoff burst also renders in play mode, a fresh `play-goal-reset` generation request is queued after the handoff, the next maze rebuilds through the same staged draw path, the compass spin stays active during build/deconstruct, the player marker stays hidden while building, and the play timer restarts at construction release instead of counting build time. Play-mode generation now keeps maze data creation full-stage but row-slices the visual Draw stage, so every play start and post-goal loop visibly constructs a fresh procedural player-scaled maze instead of returning to menu or freezing in lifecycle idle. Browser scene proof drove the player to the goal, observed `reason: goal`, then verified play mode settled on a new seed. The live play QA script now has a runtime-diagnostics-only `qa` movement bridge that drives the real `tryMovePlayer()` path, waits for settled staged construction before moving, records per-step QA acceptance/lock reasons, and proves the full post-goal deconstruct/build/fresh-seed lifecycle with `postGoalLifecycle.pass = true`. The same script now also supports stick-control proof with a repeat-safe short hold, accepts only valid on-route overshoot, and has mobile-sized stick proof reaching the goal plus proving post-goal lifecycle completion. Still needs title parity decision for play mode, final polish on deconstruct timing, and longer soak for held-stick edge cases during lifecycle boundaries. |
| High-tech crisp graphics/art pass | 46% | Requested scope is now tracked explicitly: reduce blur/fuzz, make title diamonds/scanner/stars/shards/button borders/grid corners/glimmer visually consistent, and push the game toward a crisp cyber-arcade language. Recent passes add render-only player interpolation diagnostics, browser smoke proof, a stronger path connector seam fill, a fuller path-frame trench ratio, a centralized mobile DPR target resolver for visual diagnostics, capped high-DPI text resolution for menu/pause/auth UI text, a board-level render-resolution diagnostics alias, and a renderer-safe high-DPI backing-store contract for Phaser Canvas `RESIZE`. DPR-2 browser proof reports `native` backing resolution instead of `undersampled`. This pass adds top-notch compass composition, measured level-badge fit proof, a ringless triangular menu-compass sigil that better matches the border notch geometry, and a played-game green player beacon VFX pass for better readability in dense paths. This is not complete; it still needs route screenshots on desktop and mobile widths, longer mobile soak, and the broader art consistency batch. |
| Icon-quality 2026 visual target | 5% | New lane for matching the supplied app icon's standard: crisp luminous maze paths, deep glassy black/cyan environment, vibrant green/cyan/red glow language, readable high-tech tile material, and a 2026 cyber-arcade feel instead of a flat retro/pixel-only look. Current pass only installs the new icon as the app/browser/PWA/shortcut asset and stores the source in Atlas data for future Link Me, Stripe, and brand setup reuse. Gameplay visuals still need a dedicated art-system pass to approach the icon reference. |
| Shifting color player/trail/pulse material | 50% | Runtime material helper is implemented and wired into the existing dynamic player/trail layer: the player core is locked to the green readability anchor even on high-rank AI/player palettes, the halo/accent shift through a midnight-rainbow prism, trail tiles vary by trail position/time, and pulse tiles use a stronger iridescent crest. Unit tests prove generated material colors stay separated from the pale maze path, trail colors shift by position/time, and player halo/accent plus pulse colors also shift over time. Runtime/visual diagnostics now expose compact shifted material samples for player core, halo, accent, trail head/tail, pulse head/tail, shift periods, and minimum path contrast. The played-game player and main-menu AI runner now share the green anchor as a louder beacon treatment with a breathing ring, while play mode keeps extra locator ticks for dense path visibility. Still needs desktop/mobile screenshots, final tuning of saturation/alpha, and visual QA against dense mazes before this lane can be called complete. |
| Fluid path/player/control feel | 41% | Requested scope is now tracked explicitly: remove tile separator lines so paths read as one fluid route, smooth player travel instead of hard tile snapping, and rework stick movement around direction intent plus dynamic movement checks instead of rigid hard-coded segment behavior. Recent passes add render-only player interpolation for menu-demo and played movement, keep reset/generation snap-safe, route the board marker and compass HUD through the rendered player point, and expose `visualMotionActive` plus visual coordinates in diagnostics. Held stick movement now derives candidates from the continuous pull vector and current legal exits, so a blocked major direction can yield to the next strongest legal direction instead of blindly replaying fixed segment pairs. This pass strengthens connector seam fill and reduces the path-frame edge inset from `0.18` to `0.14`, making connected corridors read as fuller continuous material without changing tile legality, AI, compass, trail topology, or generation. Follow-up wrap-snap work now classifies opposite-edge transitions as wrapped steps, skips render interpolation for those moves, and exposes `visualMotionSnapReason = "wrapped-step"` in runtime diagnostics so QA can prove players no longer visibly drag across the board when taking edge-bleed paths. Play-mode goal completion now stays in play mode and queues the next generated play maze instead of returning to the main menu; richer play-mode deconstruct/title parity remains a separate lifecycle pass. True diagonal tiles/path topology remain deferred because generation, movement legality, AI distance, compass, and trail still need one shared graph contract. |
| Diagonal paths and true diagonal travel | 8% | Re-added as an explicit marker because the concept has been discussed and should not be lost. Desired direction: diagonal maze path tiles, clean corner-filling visuals where diagonal cells touch, true diagonal player travel, diagonal-aware trail coloring, diagonal-aware compass, diagonal-aware AI distance, and generation support for spirals/angled route motifs. Current implementation remains intentionally deferred; existing diagonal buttons/stick direction intent are control conveniences over the cardinal graph, not a true diagonal topology. Next safe step is a graph contract spike that defines diagonal neighbor legality, render joins, route solving, telemetry, and edge-wrap interaction before any visual-only diagonal paths are shipped. |
| Unified player-facing message system | 78% | Auth, boot, remote progression/cycle sync, and overlay setting confirmations are now on the typed message contract. Mazer has a `legacyPlayerMessage` runtime contract inspired by the fitness app toast pattern: tone, source, id, duration, player-facing copy, optional hidden technical detail, centralized auth/boot/remote/overlay copy, centralized tone colors, and a bounded queue capped by `LEGACY_PLAYER_MESSAGE_MAX_VISIBLE`. Auth validation/feedback now resolve into `latestAuthMessage` for diagnostics while `Ready to create` and `Ready to login` stay hidden from the normal login screen. Raw browser/network auth failures like `Failed to fetch` are sanitized into player-safe copy with the raw detail retained only as hidden technical detail. Visible negative-login QA now proves invalid auth copy appears as the compact in-game message card while loose raw/ready text stays absent. Auth feedback, remote sync warnings, options/pause setting confirmations, active-play notices, and explicit advanced field commits now enter the same compact cyber message-card queue; visible cards expire after their configured duration, auth cards clear immediately when fields change, and play mode renders queued messages under the timer on the HUD layer. Bulk overlay field commits stay silent so closing Options does not spam eight confirmations. Remaining work: migrate diagnostics-only technical details beyond remote sync and tune copy/expiry after longer mobile use. |
| Maze complexity/path topology | 33% | Existing size/complexity split and edge-dock continuity tests remain active. Recent passes add a render-level top-center notch reserve that splits top border dock drawing so generated visual path pieces no longer occupy the triangular divot reserved for compass/title composition. Topology-backed perimeter feeder spokes connect near-edge corridors to real border lanes, require at least two usable feeder contacts per side across proof scales, avoid corner/center cutout reserve lines, and reuse opposite-border pairing so edge-bleed paths stay navigable instead of visual-only. This pass makes paired off-border continuities, split counts, dead-end counts, fill quality, short-wrap relief, weighted split pressure, weighted dead-end pressure, route-efficiency pressure, and render-safety pressure part of the numeric receipt/report review. Rooms, true diagonal graph contracts, and richer weighted fill/branch formulas stay deferred until generation, movement, AI distance, compass, trail, and topology share one graph contract. |
| Auth QA and production readiness | 89% | Unit/build proof is green; local env wiring is fixed; configured overlay proof exists; generated QA signup/login/logout/re-login and remote read/write are proven. Native browser input proof exists for visible form entry, visible negative-login QA proves the browser can replace field content, submit the form, receive player-safe failure copy, and avoid console warnings, and visible requested-account QA proves successful login, reload persistence, and pause-menu logout. This pass adds fitness-style persistent-login unit proof for remembered-account JSON state, session bootstrap syncing, duplicate listener protection, token-free remembered identity storage/signature dedupe, explicit no-session reauth downgrade, and local-only sign-out semantics. Runtime diagnostics now expose a safe auth proof block with configured/status/email presence/user-id presence/form mode/latest auth message and remembered identity state through the existing machine-readable diagnostics attribute, so browser QA can prove persistence without scraping visuals or exposing tokens. Local browser proof on the current `4173` preview now shows the requested account authenticated, remembered identity `ready`, and user-scoped progression active after reload. Redacted bundle proof confirms the local preview bundle contains the Mazer Supabase project URL/key prefix after the build-env fix. No production deploy was run in this pass. |
| Visual proof verification discipline | 90% | New lane for making screenshots non-vague and route-correct. `capture.mjs`, `capture-layout-matrix.mjs`, the normal app path in `edge-live-check.mjs`, and `capture-ui-surfaces.mjs` now build screen contracts from expected route, actual URL, runtime diagnostics mode, overlay state, and viewport before screenshotting. Wrong route, wrong mode, wrong overlay, or mismatched viewport diagnostics fail the capture instead of producing misleading visual proof. Metadata and summaries now include `actualUrl` plus `screenContract` or stage-level `screenContracts`, and `docs/PLAYBOOK_VISUAL_EVIDENCE.md` requires citing `screenContract.pass` beside screenshot paths. Full local matrix proof generated 8 captures with 0 failed screen contracts at `tmp/captures/mazer-layout-matrix/screen-contract-proof-local`; full edge-live proof generated 8 captures with first-load, second-load, and lifecycle contracts all passing at `tmp/captures/mazer-edge-live/edge-screen-contract-proof-local`. Still needs authenticated UI-surface fixture proof and proof-surface workflow adoption if those isolated preview pages become part of normal Mazer screenshot QA. |

## Current Proof

- `npm test -- --run tests/reset/legacy-overlay-toggle-fields.test.ts tests/reset/legacy-remote-progression.test.ts tests/reset/legacy-menu-button-chrome.test.ts tests/reset/legacy-progression.test.ts tests/scenes/menu-render-frame.test.ts`
- `npm run lint`
- `npm run build`
- Menu AI gate proof: `tests/reset/legacy-menu-demo-lifecycle.test.ts` now checks generated routes across multiple scales/seeds leave the start on first settled advance, `tests/reset/legacy-reset.test.ts` guards the static-draw gate release source contract, and runtime/visual diagnostics expose `menuDemo.gate` with `waitingForBuild`, `released`, and `nextMoveAtMs`.
- Menu AI browser proof: local in-app browser loaded `v=menu-ai-gate-diagnostics-proof-local`; diagnostics showed `waitingForBuild: true` while construction was `building`, `released: true` when construction became `settled`, path cursor progressing from `0` through `145`, and zero warning/error logs.
- Optional-branch retarget proof: `tests/ai/demo-walker.test.ts` now includes a deterministic human-local-memory route where the AI records an earlier split, continues forward, decides the remembered option is better, walks back through known floor with `backtrack` segments, resumes with `reacquire`, increments `telemetry.optionalRetargetCount`, and proves every move remains adjacent.
- AI unseen-goal target proof: `src/domain/ai/demoWalker.ts` no longer contains the visual-goal inference fallback that called `findFloorPath(current, end)` and exposed the end tile as the memory target. Human-local-memory scoring now uses a cached wrapped-topology distance field only to score adjacent/remembered choices, not to hand the AI a final route. `tests/ai/demo-walker.test.ts` proves generated menu routes stay adjacent/bounded, preserve optional retargeting, reach goal regeneration, and throw if `aiMemory.targetIndex` equals the end while the AI is not already on or adjacent to the end.
- AI decision scoring proof: `src/legacy-runtime/mazeCycleTelemetry.ts` now scores AI receipts into route noise, recovery pressure, retarget pressure, total pressure, reliability, and `clean/searching/chaotic` bands. `scripts/analysis/maze-cycle-telemetry-report.mjs` mirrors those aggregates into Atlas-safe reports, and tests cover local diagnostics, remote receipt shape, runtime diagnostics, and report normalization.
- AI decision-score receipt proof: local diagnostic receipts now include `aiDecisionScore`, authenticated remote cycle payloads include the same score next to `aiDecisionSummary`, and Atlas report normalization preserves that per-receipt score while still excluding full raw path history. Proof: focused telemetry/remote/report Vitest packet passed `35` files / `262` tests through the maintained reset/AI/report spine.
- Weighted complexity pressure proof: `resolveLegacyMazeComplexity()` now adds `weightedSplitPressureScore` and `weightedDeadEndPressureScore` using wrapped goal distance, local legal exits, plausible split choices, and dead-end proximity instead of flat branch/dead-end counts only. Telemetry diagnostics, remote cycle payloads, and Atlas-safe reports preserve the weighted fields and aggregate weighted averages. Proof: `npx tsc --noEmit` passed, and the maintained reset/AI/report spine passed `35` files / `262` tests.
- Score-label and performance-pressure proof: the main-menu AI badge now renders `Score` instead of `Pace`, preserving the internal `paceScore` storage field for compatibility. Cycle receipts now include `routeEfficiencyPressureScore` and `renderSafetyPenaltyScore`; local diagnostics, remote receipt JSON, and Atlas-safe reports preserve both values, and high route waste or unsafe frame timing can drive a conservative `ease` signal. Proof: `npx tsc --noEmit` passed, and the maintained reset/AI/report spine passed `35` files / `263` tests.
- Atlas consumer proof: `scripts/analysis/maze-cycle-telemetry-report.mjs` now exports `validateMazeCycleTelemetryAtlasReport()` and `createMazeCycleTelemetryAtlasConsumerReceipt()` for `mazer.cycle-learning.consumer.v1`. The validator requires Atlas-safe source/policy fields, rejects raw `playerPath` leakage, requires AI/complexity/performance/progression reviews, and blocks tuning until at least three clean samples exist. The consumer selects bounded focuses such as `render-safety`, `route-efficiency`, `ai-chaos`, `increase-complexity`, `reduce-complexity`, or `hold-and-observe` while keeping `noAutoTuningWithoutValidator = true`. The CLI now supports `--consumer-output` and `--atlas-consumer-root` so reports and consumer receipts can be generated together. Proof: `npx tsc --noEmit` passed, and `npm run test -- tests/analysis/maze-cycle-telemetry-report.test.mjs tests/reset/legacy-marker.test.ts` passed through the maintained reset/AI/report spine.
- Edge-wrap snap proof: `src/legacy-runtime/legacyMaze.ts` now exports `isLegacyWrappedStepTransition`, `tests/reset/legacy-reset.test.ts` proves opposite-edge transitions classify as wrapped while adjacent moves do not, `MenuScene.armLegacyPlayerVisualMotion` syncs wrapped moves directly to the destination instead of tweening across the board, and runtime diagnostics expose `visualMotionSnapReason = "wrapped-step"` for wrapped snaps.
- Menu Options account proof: `MenuScene.buildOptionsOverlay` now calls `createLegacyOptionsAccountActionRow`, the UI surface capture contract expects the Options account action, and local in-app browser proof at `v=wrap-snap-options-account-local` showed a bottom `Log out` button on the authenticated main-menu Options overlay with zero visible overlap. Clicking it switched progression to `mazer.progression.v1:guest`, rendered `Signed out. Guest progress is active.` through the shared message card, and changed the bottom action to `Account`.
- DiscordOS board proof: `repos/DiscordOS/config/discordos-mazer-feedback-board.json` defines the no-send `Mazer Feedback` board, `npm run ops:discordos:mazer-feedback-board:json` reads the current seven card backlog, and `npm run verify:discordos-mazer-feedback-board` guards the board contract.
- Visual screen-contract proof: `scripts/visual/screen-contract.mjs` rejects captures whose actual URL, diagnostics surface mode, overlay, or viewport do not match the requested surface. `scripts/visual/capture.mjs`, `scripts/visual/capture-layout-matrix.mjs`, and the normal app capture path in `scripts/visual/edge-live-check.mjs` assert that contract before screenshotting and persist `actualUrl` plus `screenContract`/`screenContracts` in metadata. `tests/visual/screen-contract.test.ts` covers pass/fail behavior, and `tests/visual/edge-live-check.test.ts` guards all three screenshot scripts against removing the contract.
- Edge-live screen-contract artifact proof: `npm run edge:live -- --run edge-screen-contract-proof-local --route "/?content=core-only&theme=aurora" --skip-build true --timeout 60000` generated 8 captures at `<ATLAS_ROOT>/tmp/captures/mazer-edge-live/edge-screen-contract-proof-local`; every first-load, second-load, and lifecycle `screenContracts.*.pass` value was `true`, with diagnostics mode `menu` and overlay `none`.
- Wrapped-route audit proof: `tests/reset/legacy-reset.test.ts` and `tests/reset/legacy-topology-scale-audit.test.ts` now use the wrapped-topology minimum route-length scale while preserving strict checks for paired border wraps, inward border connectivity, multi-route quality, bypass bands, and detached-floor prevention.
- Local-only browser screenshots under `<ATLAS_ROOT>/tmp/captures/mazer-auth-gate-qa/`
- Local-only browser screenshots under `<ATLAS_ROOT>/tmp/captures/mazer-auth-qa-final/`
- Local-only browser screenshots under `<ATLAS_ROOT>/tmp/captures/mazer-auth-native-input/`
- Native input proof: visible Phaser account fields create `[data-mazer-auth-input]` elements and accept browser automation typing.
- Fitness-style auth persistence proof: `tests/reset/legacy-auth.test.ts` validates JSON remembered-identity state, `ready` and `reauth-required` transitions, authenticated session bootstrap syncing, token-free remembered account storage, duplicate listener protection, and local-only sign-out semantics.
- Runtime auth diagnostics proof: `tests/scenes/menu-runtime-diagnostics.test.ts` now guards the safe auth proof block on `__MAZER_RUNTIME_DIAGNOSTICS__` and `data-mazer-runtime-diagnostics`, including authenticated status, remembered identity session state, latest player-safe auth message, and user-id presence without token exposure.
- Runtime auth browser proof: local in-app browser loaded `v=auth-runtime-ready-proof-1783646001`, confirmed `auth.status = "authenticated"`, `auth.rememberedIdentity.sessionState = "ready"`, `auth.userIdPresent = true`, the user-scoped progression key, one canvas, and current bundle `main-ivQttSU1.js`.
- Unified message proof: `tests/reset/legacy-player-message.test.ts` validates the typed player-facing message contract, auth validation/feedback tone mapping, and centralized message colors.
- Level/rank/complexity contract proof: `docs/research/MAZER_LEVEL_RANK_COMPLEXITY_CONTRACT.md` captures the active contract for player-vs-AI progression, target-complexity-derived level/rank, AI local-memory tuning, and future Atlas/playbook receipts. `resolveLegacyMazeComplexity()` now emits `edgeWrapCount/Score`, `splitCount/Score`, `deadEndCount/deadEndPressureScore`, and `fillQualityScore`; telemetry receipts carry `mazeComplexity`; remote receipt JSON preserves it; Atlas reports now emit `complexityReview` plus `progressionReview` aggregates; `tests/reset/legacy-progression.test.ts` proves the new `recentSignals` smoothing window avoids hard-jumping target complexity while still rewarding repeated clean completions and repeated struggle; `tests/scenes/menu-render-frame.test.ts` guards the public badge copy as readable `AI Skill / Level / Rank / Run / Score / Time` text and blocks `C:`/`Sig:` debug terms from returning to the player-facing badge; and `tests/reset/legacy-reset.test.ts` guards that menu-demo timing starts at static-build release instead of counting construction delay against the AI score.
- Run-quality progression proof: `resolveLegacyProgressionPerformanceScore()` now converts timer pace, shortest-path route efficiency, wrong turns, backtracks, reset use, and render stability into the run-quality score that chooses `challenge`/`hold`/`ease` before the six-signal smoothing window adjusts target complexity. `tests/reset/legacy-progression.test.ts` proves efficient runs challenge, route waste against `shortestViablePathLength` eases, and unsafe frame timing holds difficulty without punishing the player. Proof: `npx vitest run tests/reset/legacy-progression.test.ts tests/reset/legacy-marker.test.ts`, maintained AI/telemetry/report spine, `npx tsc --noEmit`, and `npm run build` passed.
- Fresh player-maze generation proof: `createLegacyGenerationRequest()` now accepts a sanitized `seedOverride`, `createLegacyPlayResetGenerationRequest()` can use that override instead of only `seed + 1`, and `MenuScene.createFreshLegacyPlayGenerationSeed()` mixes runtime entropy with player `targetComplexity`, completed cycles, level, and score. `tests/reset/legacy-generation-lifecycle.test.ts` proves play goal-reset can use a non-sequential fresh seed while preserving play-generated budget, and `tests/scenes/menu-render-frame.test.ts` guards play start plus goal reset wiring to fresh seed generation and player-progression scale.
- Shortest-path comparison proof: `createMazeCycleTelemetryReceipt()` now stores `shortestViablePathLength`, `routeOverrunSteps`, and `routeOverrunRatio` from the generated maze solution path, remote cycle receipt payloads preserve those fields inside the compact JSON body, and `scripts/analysis/maze-cycle-telemetry-report.mjs` carries the same fields into Atlas-safe performance pressure reviews.
- Perpetual play-loop proof: `tests/reset/legacy-play-lifecycle.test.ts` guards that play-mode goal reset action is `regenerate-maze` instead of `return-menu`, `tests/reset/legacy-generation-lifecycle.test.ts` covers the play reset generation request, and `tests/reset/legacy-pause-lifecycle.test.ts` plus `tests/scenes/menu-render-frame.test.ts` guard Reset/Menu actions in the pause overlay.
- Live play lifecycle proof: `scripts/analysis/live-play-qa.mjs` now defaults to a runtime-diagnostics-only `qa` input bridge installed as `window.__MAZER_QA__` only when `runtimeDiagnostics=1`, waits for the staged play draw lifecycle to settle before route execution, drives the real `tryMovePlayer()` path to the goal, then samples post-goal lifecycle state until a fresh seed settles. Latest artifact: `<ATLAS_ROOT>/tmp/captures/mazer-live-play-qa/play-lifecycle-post-goal-qa-proof-settled/play-lifecycle-post-goal-proof.summary.json` with `result.pass = true`, `postGoalLifecycle.pass = true`, `sawDeconstructing = true`, `sawBuilding = true`, `sawCompassSpin = true`, `sawFreshSeedQueued = true`, `sawHandoff = true`, and `settledFreshSeed = true`.
- Stick-control lifecycle proof: `scripts/analysis/live-play-qa.mjs --input-method stick` uses a short below-repeat-threshold stick drag and accepts only route-valid stick overshoot. Latest artifact: `<ATLAS_ROOT>/tmp/captures/mazer-live-play-qa/play-lifecycle-stick-control-proof-short-hold/play-lifecycle-stick-control-proof.summary.json` with `result.pass = true`, `executedMoveCount = 152`, `postGoalLifecycle.pass = true`, `sawCompassSpin = true`, and `settledFreshSeed = true`.
- Played pause cleanup proof: `tests/reset/legacy-pause-lifecycle.test.ts` slices `MenuScene.buildPauseOverlay()` and proves the play pause overlay no longer contains `Log out`, `Account`, `Resume`, or `resumeAction` button text while preserving explicit `Reset` and `Menu` actions. Local in-app browser proof at `v=pause-under-timer-clean-local-1783633001` opened the play pause overlay and visually confirmed only `Reset` and `Menu` render beneath the simplified Player Guide.
- Play pause HUD placement proof: `tests/input-human/touch.test.ts` now proves phone portrait `PAUSE` touch control is centered under the timer, above the movement deck, and still resolves to the `pause` action. `tests/scenes/menu-render-frame.test.ts` guards the played pause overlay panel anchoring below the timer frame.
- Shifting material proof: `tests/reset/legacy-iridescent-material.test.ts` validates path-safe midnight-rainbow material color shifts, locked green player core, shifted trail colors, shifted player halo/accent, and shifted pulse colors. `tests/scenes/menu-render-frame.test.ts` guards the scene wiring plus the compact `markerStyle.iridescentMaterial` diagnostics samples for player/trail/pulse material QA.
- Iridescent browser smoke: local in-app browser loaded `v=iridescent-green-core-proof-*`, rendered one menu canvas, reported `markerStyle.playerCoreColor = 0x36ff7d`, and reported no console warnings/errors.
- Player visual-motion proof: `tests/scenes/menu-render-frame.test.ts` guards render-only player interpolation, rendered-player board marker/HUD usage, and diagnostics `visualMotionActive`/visual coordinates. Local in-app browser loaded `v=player-visual-motion-proof-*`, observed menu-demo `visualMotionActive = true`, observed fractional visual coordinates distinct from logical tile coordinates, and reported no console warnings/errors.
- Stick intent proof: `tests/scenes/menu-render-frame.test.ts` guards live stick candidate resolution from `normalizedX/Y`, magnitude ordering, legal-exit filtering through `resolveLegacyNavigationTarget`, and fallback to legacy segment candidates. `tests/input-human/touch.test.ts` and `tests/input-human/input-human.test.ts` keep the 16-segment stick mapping and shared movement priorities stable. Local in-app browser loaded a phone viewport at `v=stick-intent-mobile-proof-*`, rendered the stick UI, dragged the stick, moved the player, released back to no active controls, and reported no console warnings/errors.
- Path seam proof: `tests/scenes/menu-render-frame.test.ts` guards `fillLegacyPathConnectorSeams`, adjacency-only connector detection, named seam ratio constants, and visual diagnostics `board.pathMaterial`. Local in-app browser loaded `v=path-material-diagnostics-*`, rendered one phone-sized menu canvas screenshot, reported `pathMaterial.connectorSeamsEnabled = true`, `seamCoreAlphaRatio = 0.94`, `seamEdgeAlphaRatio = 0.72`, `seamPadRatio = 0.16`, `pathVisualStyle = "corridor"`, `renderResolution.undersampled = false`, and no console warnings/errors.
- Path-frame fluidity proof: `tests/scenes/menu-render-frame.test.ts` now pins `LEGACY_MENU_TRENCH_EDGE_INSET_RATIO = 0.14` and updated frame/segment geometry so normal 20px corridors render fuller while 4-6px mobile tile separation remains guarded by minimum inset tests.
- Edge trail dock proof: dynamic trail and trail-pulse tiles now call the same border-dock renderer as static path material, so traveled edge-bleed paths color through the maze border frame instead of stopping at the last interior grid tile. `tests/scenes/menu-render-frame.test.ts` guards the dynamic dock helper and the trail/pulse call sites.
- Mobile crispness proof: `src/boot/canvasResolution.ts` centralizes the capped DPR target used by visual diagnostics, and `tests/boot/canvas-resolution.test.ts` guards 1x-2x rounding/clamping. Direct top-level Phaser `resolution` wiring was rejected by the TypeScript `GameConfig` contract, so the real backing-store fix remains a renderer-safe lane instead of a no-op config field.
- Board render-resolution proof: `tests/boot/canvas-resolution.test.ts` guards the shared `summarizeMazerRenderResolution` classifier across native, undersampled, oversampled, and unavailable samples. `tests/scenes/menu-render-frame.test.ts` guards the shared `renderResolutionDiagnostics` object and exposes it through both `renderSurface` and `board.renderResolution`, preventing future browser QA from missing undersampling data while the renderer-safe backing-store lane remains open.
- Render-resolution browser proof: local in-app browser loaded `v=render-resolution-status-proof-local`, parsed the maintained diagnostics DOM fallback, reported `board.renderResolution.status = "native"`, `renderResolutionRatio = 1`, `renderResolutionTargetRatio = 1`, `renderResolutionDeficit = 0`, matching `renderSurface` values, and no console warnings/errors.
- High-DPI mobile proof: local in-app browser CDP emulated a 405x958 DPR-2 mobile viewport at `v=dpr2-render-resolution-proof-local`; diagnostics reported browser `devicePixelRatio = 2`, canvas CSS `405x958`, canvas backing pixels `405x958`, `renderResolutionRatio = 1`, target `2`, deficit `1`, `status = "undersampled"`, matching `renderSurface` values, and no console warnings/errors. The emulation and viewport override were cleared afterward.
- High-DPI backing-store fix proof: `tests/boot/canvas-resolution.test.ts` guards `resolveMazerCanvasBackingResolution` keeping CSS layout `405x958` while scaling backing pixels to `810x1916` at capped DPR-2. `tests/scenes/menu-render-frame.test.ts` guards `MenuScene.refreshLayout()` applying `resolveMazerCanvasBackingResolution` and `applyMazerCanvasBackingResolution`.
- High-DPI browser fix proof: local in-app browser CDP emulated a 405x958 DPR-2 mobile viewport at `v=hidpi-backing-proof-local`; diagnostics reported browser `devicePixelRatio = 2`, canvas CSS `405x958`, canvas backing pixels `810x1916`, `renderResolutionRatio = 2`, target `2`, deficit `0`, `status = "native"`, matching `renderSurface` values, and no console warnings/errors. High-DPI input smoke clicked `Options` from visual diagnostics bounds and opened the options overlay with no console warnings/errors. The emulation and viewport override were cleared afterward.
- UI text crispness proof: `MenuScene` now routes footer, level badge, buttons, auth copy, options rows, pause rows, and shared message cards through the existing capped `applyTextResolution`/`resolveHudTextResolution` helper via `padLegacyUiText`; `tests/scenes/menu-render-frame.test.ts` guards the shared wiring.
- Boot message proof: `tests/boot/boot-status.test.ts` validates player-safe boot/cache failure messages with hidden technical details retained separately.
- Remote message proof: `tests/reset/legacy-remote-progression.test.ts` validates remote success, guest, missing-client, compact cycle receipt, and remote write failure outcomes with player-safe messages plus hidden technical detail.
- Visual diagnostics proof: `tests/scenes/menu-render-frame.test.ts` compiles the diagnostics contract with the new `remoteSync` payload.
- Overlay message proof: `tests/reset/legacy-player-message.test.ts` validates toggle and movement-speed confirmations through the shared player-facing message contract.
- Auth inline-removal proof: `tests/scenes/menu-render-frame.test.ts` asserts auth messages are retained through `latestAuthMessage` diagnostics and that the old inline auth feedback/message render helpers are absent.
- Auth message-card proof: `tests/reset/legacy-player-message.test.ts` validates `Failed to fetch` becomes player-safe account-service copy with hidden technical detail, and `tests/scenes/menu-render-frame.test.ts` guards the shared overlay message-card renderer plus feedback-only auth display path.
- Message expiry proof: `tests/scenes/menu-render-frame.test.ts` guards timestamped auth/overlay message expiry, scene update expiry checks, and helper-routed settings confirmations.
- Message expiry browser smoke: local in-app browser loaded `v=message-expiry-proof-*`, rendered one menu canvas, reported no stale visible auth/overlay message on the guest menu, and reported no console warnings/errors.
- Message queue proof: `tests/reset/legacy-player-message.test.ts` validates bounded queue max-visible, id replacement, and expiry behavior; `tests/scenes/menu-render-frame.test.ts` guards scene queue wiring, stacked card rendering, remote-sync queue insertion, and diagnostics `visibleMessages`.
- Play HUD message proof: `tests/scenes/menu-render-frame.test.ts` guards active-play HUD message stack rendering, HUD dirty-state routing, and shared cyber-card styling on `hudGraphics`.
- Play HUD browser smoke: local in-app browser loaded `v=play-message-hud-proof-*`, rendered one play canvas with HUD timer/controls/marker colors, and reported no console warnings/errors.
- Green menu text proof: `tests/scenes/menu-render-frame.test.ts` asserts front-door Start text and progression badge text use `LEGACY_MENU_ACTION_GREEN`.
- App icon proof: the supplied 2026 Mazer icon was copied into Atlas data at `data/atlas/brand/mazer/mazer-app-icon-2026-07-09-source.png` and `data/atlas/ui-visual-proof/mazer/app-icon-2026-07-09/reference.png`, then regenerated into `public/icons/mazer-app-icon.png`, `mazer-app-icon.ico`, `apple-touch-icon.png`, `icon-192.png`, `icon-512.png`, and both maskable PWA icons. `tests/scenes/menu-render-frame.test.ts` guards browser/PWA icon wiring, Windows shortcut icon wiring, and the Atlas source-path documentation.
- Menu padding proof: `tests/reset/legacy-menu-layout.test.ts` guards board/title/footer/side-gutter spacing across desktop, phone, ultra-narrow, and play surfaces and now validates `resolveLegacyAuthenticatedMenuButtonStack` keeps the signed-in Start/Options pair centered with a 10-14px chrome gap plus at least 40px board-to-stack clearance on landscape desktop. `tests/scenes/menu-render-frame.test.ts` guards that `MenuScene` uses the shared helper.
- Server restore proof: local preview at `http://127.0.0.1:4173` returned HTTP 200 before this pass continued, so the active server was preserved instead of restarted.
- Top-notch path exclusion proof: `tests/scenes/menu-render-frame.test.ts` guards `resolveLegacyMenuBorderDockRenderAreas` splitting top-edge dock areas around `topCenterNotch`; `src/legacy-runtime/legacyMenuRender.ts` now has an explicit `topCenterNotch` option and `pushTopArea` splitter.
- Edge-fill topology proof: `tests/reset/legacy-topology-scale-audit.test.ts` now verifies generated play/menu mazes keep at least two usable border feeder contacts on each side, keep all border bleeds paired across the opposite edge, preserve inward connectivity, and keep generated border floors out of corner/center cutout reserve lines across scale bands `37`, `50`, `75`, `99`, and the bounded extreme `149` smoke. Local in-app browser loaded `v=edge-feeder-proof-local` at phone width, produced a native-resolution canvas, kept the compass inside the notch, kept progression text fitting, reported zero warn/error logs, and showed a generated menu maze with paired reinforcement/border feeder tiles in the build trace.
- Menu compass and level-badge proof: `tests/scenes/menu-render-frame.test.ts` guards `drawLegacyMenuCompass`, `board.topCenterNotch`, `menuCompass`, and measured `progressionBadge.textFits` diagnostics. Local in-app browser loaded mobile `405x958` at `v=notch-compass-reserve-proof-local` and reported compass bounds fully inside notch bounds, `progressionBadge.textFits = true`, native render resolution, and zero warn/error logs. Desktop `1280x720` proof at `v=notch-compass-desktop-proof-local` reported the same clean fit and zero warn/error logs. Follow-up overlap proof at `v=layout-overlap-proof-local` showed the level badge between the board and Start button with visible padding, `progressionBadge.textFits = true`, and zero warn/error logs; mobile `v=layout-overlap-mobile-proof-local` reported the same clean fit. Final notch-crown proof at `v=notch-compass-smaller-desktop-local` reported desktop compass bounds `14x14` inside a `40x30` notch, and `v=notch-compass-smaller-mobile-local` reported mobile compass bounds `17x17` inside a `34x26` notch, both with zero warn/error logs. Ringless sigil proof at `v=notch-compass-sigil-mobile-local` and `v=notch-compass-sigil-desktop-local` removed the circle/crosshair from the menu notch compass while preserving the same notch-bounded diagnostics and zero warn/error logs.
- Level-badge clarity proof: local in-app browser loaded `v=level-badge-clarity-local-1783636201`, reported `visualMode = "menu"`, `progressionBadge.text = "AI Skill  Level 01  Rank E\nRun 135  Score 100  Time 0:20"`, `progressionBadge.textFits = true`, `hasDebugBadgeText = false`, and native render resolution. The screenshot confirmed the badge sits cleanly between the board and Start button without `C:`, `Sig:`, `R:`, or `#` shorthand.
- Play badge placement proof: local in-app browser loaded `v=play-badge-top-local-1783637601`, reported `visualMode = "play"`, `progressionBadge.text = "Player Skill Lv 42 Rank S Runs 4"`, `progressionBadge.textFits = true`, native render resolution, `badgeIsAboveBoard = true`, and `badgeToBoardGapPx = 7`, proving the played-game skill badge sits above the maze instead of under it. Follow-up source/test proof now changes the played badge copy to the clearer two-line `Player Skill  Level ##  Rank X` / `Runs #` structure; visual re-proof is still needed after the next browser pass.
- Play badge gutter proof: local in-app browser loaded current-build `4178` play route `v=play-badge-gap-final-clean-1783641401`, reported `mode = "play"`, `overlay = "none"`, `progressionBadge.textFits = true`, current bundle `main-CEo6PNQS.js`, and `badgeToBoardGapPx = 17`, proving the played-game skill badge no longer touches the maze border.
- Played player beacon proof: local in-app browser loaded isolated current-build preview `v=play-green-beacon-uncapped-proof-1783640001` after clearing stale service-worker/cache state, reported `mode = "play"`, current bundle `main-DplmuDbM.js`, `playerCoreColor = 0x36ff7d`, `playerBeaconColor = 0x36ff7d`, `playerBeaconAccentColor = 0xb6ffd0`, `playerBeaconPeriodMs = 1150`, `playerCoreRadius = 3.24`, `playerHaloRadius = 5.07`, `board.tileSize = 7.04`, native render resolution, and `progressionBadge.textFits = true`.
- Menu AI beacon proof: local in-app browser loaded current-build menu route `v=menu-ai-beacon-proof-1783642201-clean` after clearing stale service-worker/cache state, reported `mode = "menu"`, `overlay = "none"`, current bundle `main-Cw3WUk2v.js`, `playerCoreColor = 0x36ff7d`, `playerBeaconColor = 0x36ff7d`, `playerBeaconAccentColor = 0xb6ffd0`, `playerBeaconPeriodMs = 1150`, native render resolution, `progressionBadge.textFits = true`, zero warn/error logs, and visual artifact `tmp/captures/menu-ai-beacon-proof/menu-ai-beacon-proof-1783642201.png`.
- Visible negative-auth proof: local in-app browser loaded `v=visible-auth-message-qa-*`, opened the Login-only guest auth overlay, replaced the remembered email with a disposable QA address through `[data-mazer-auth-input]`, submitted an invalid password, rendered `Invalid login credentials` through the shared message card, reported no loose `Ready to` or `Failed to fetch` text, and reported no console warnings/errors.
- Visible requested-account login proof: local in-app browser loaded `v=auth-rotated-account-proof-*`, submitted the requested confirmed account through the native auth input plus canvas Login action, reported `authAction.stage = "result"`, `authAction.status = "authenticated"`, `authAction.info = "Signed in."`, and switched progression to `mazer.progression.v1:user:b4f0904d-824a-4b73-b532-2a211986ab68` with no console warnings/errors.
- Account AI reset proof: the authenticated Supabase client reset only `public.mazer_ai_progression_states` for user `b4f0904d-824a-4b73-b532-2a211986ab68` and `runner_key = "menu-runner"` from AI level 1 / rank E / 83 cycles to AI level 1 / rank E / 0 cycles. Local receipt: `runtime/receipts/mazer/account-ai-reset/latest.json`. Player progression and cycle receipts were not touched, and the local AI baseline version was bumped so stale browser AI-runner state resets without wiping player progress.
- Visible reload persistence proof: reloaded the same local preview after successful login; the authenticated menu returned with Start/Options visible and retained the user-scoped progression key.
- Visible pause logout proof: opened the play pause menu, clicked `Log out`, rendered `Signed out. Guest progress is active.`, changed the pause action back to `Account`, switched progression to `mazer.progression.v1:guest`, and reported no console warnings/errors.
- Visual smoke: in-app browser screenshot confirmed the current guest menu remains Login-only with no console warnings/errors after the overlay-message patch.
- Build-env proof: redacted scan confirms `dist/assets/main-*.js` contains the Mazer Supabase project host and publishable key prefix after `npm run build`.
- Requested account proof: browser-safe Supabase client sign-in/logout succeeds for the account created in the Mazer Supabase project.
- Generated QA Supabase proof: signup returned a session, logout succeeded, login-again succeeded, `mazer_cycle_receipts` insert/read succeeded, `mazer_progression_states` upsert/read succeeded, and `mazer_ai_progression_states` upsert/read succeeded.
- Remote-to-Atlas proof: `npm run cycle:report -- --input ../../tmp/mazer-auth-qa/remote-cycle-receipt-export.json --atlas-receipt-root ../../runtime/receipts/mazer/cycle-telemetry --output ../../tmp/mazer-auth-qa/remote-cycle-atlas-report.json --pretty`
- Atlas latest receipt: `<ATLAS_ROOT>/runtime/receipts/mazer/cycle-telemetry/latest.json`
- Live Supabase MCP checks confirmed RLS enabled on active Mazer tables and no direct `anon` table grants.

## Active Blockers

- Visible browser form-entry is fixed with native input overlays. The wrong-project local build env issue has also been fixed. Negative visible login failure, successful visible login, reload persistence, and pause-menu logout are now proven through the app. A transparent native-button/form overlay was tested for the primary auth action but intentionally reverted because the in-app browser did not produce an observable submit transition; the shipped proof path uses native text inputs plus the existing canvas submit control with hidden `authAction` diagnostics.
- High-DPI mobile blur was proven as renderer undersampling and now has a local Canvas backing-store fix with DPR-2 browser proof plus an options-click smoke. Remaining risk is broader mobile soak: play controls, pause scroll, auth native inputs, and longer animation cycles should be tested under DPR-2 before calling the graphics lane complete.
- Top-center notch handling is currently a render-reserve contract. It prevents visible path/border dock pieces from occupying the triangular divot, but it is not the final wrapped-topology/edge-bleed graph contract the maze generator, AI distance model, compass, and trail will eventually need to share.
- The menu board cannot be safely enlarged on normal landscape by a single cap bump right now because integer tile snapping jumps desktop from a safe `735px` board to a `784px` board and collapses authenticated footer clearance. A future "bigger but safe" pass should use a fractional tile/layout contract instead of raising the cap blindly.
- Atlas/playbook remote cycle learning now has a validator-gated consumer receipt, but the next pass should wire that consumer into an actual Atlas-side read/ratchet process instead of leaving it as a repo-local helper and test.
- Unified player-facing messaging has auth, boot, remote sync, and common overlay setting confirmations on the runtime contract, but advanced numeric option commits and broader diagnostics-only messages are not migrated yet. Technical-only diagnostics now have a remote-sync hidden-detail path, but the game still needs one reusable toast/card presentation shared by menu and play before this lane can be called complete.
- Diagonal tiles/pathing remain intentionally deferred to avoid visual-only paths that AI, compass, movement, and telemetry do not understand.
