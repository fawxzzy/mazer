# Mazer Mechanics And Mobile Completion Marker

Date: 2026-07-04
Status: active
Current marker: `100%`

## Intent

This is the active completion marker for the current Mazer direction.

The legacy visual 1:1 screenshot target is retired unless explicitly reopened. Future work should preserve and improve the old project's mechanics, while allowing the browser game to look cleaner, flatter, clearer, and more mobile-friendly than the old prototype.

## What counts as 100%

For this lane, `100%` means:

- menu and active-play mazes are procedurally generated through the intended generation family
- maze topology is playable, connected, and supports meaningful alternate routes without weak shortcut artifacts
- menu AI/demo behavior has a clean loop: explore, recover, backtrack/reacquire, reset, and replay without non-adjacent route splices or runaway routes
- active play movement, collision, reset, pause reset, keyboard input, touch input, HUD semantics, and diagnostics are coherent and proof-backed
- mobile and desktop views are readable, centered, and usable without relying on fake 3D depth or screenshot mimicry
- code ownership is clear enough that future tuning can target the right module without broad rewrites

## What no longer counts

These are no longer required for active completion:

- exact old screenshot silhouette
- exact old button placement
- exact old title overlap
- exact old player/trail material
- fake 3D or pseudo-depth visual treatment

These can still be useful references, but they are not the active target.

## Weighted completion model

| Segment | Weight | Current points | Current truth | Main owners | Remaining gap |
| --- | --- | --- | --- | --- | --- |
| Core gameplay loop and reset semantics | `20` | `20` | closed for current scope with generated-play traversal plus live keyboard/touch play proof | `src/legacy-runtime/legacyPlayStep.ts`, `src/legacy-runtime/legacyPlayLifecycle.ts`, `src/scenes/MenuScene.ts` | continue future feel tuning only if new input regressions are found |
| Maze generation and topology quality | `25` | `25` | closed for current scope with scored shortcut reinforcement, shortcut-enabled menu budget floor, shortcut-scale-band proof, bounded large-scale smoke, and one bounded extreme-scale proof-spine smoke | `src/legacy-runtime/legacyMaze.ts`, `src/domain/maze/*` | continue future stress tuning only if newly discovered weak seed families appear |
| Menu AI/demo loop | `15` | `15` | closed for current scope with generated-menu scale-band recovery, AI-only replay, and later goal-regeneration proof | `src/domain/ai/demoWalker.ts`, `src/legacy-runtime/legacyDemoWalker.ts`, `src/legacy-runtime/legacyMenuDemoLifecycle.ts` | exact Unreal numeric delay/material timing can still tighten future parity if stronger source evidence is recovered |
| Mobile input and active-play usability | `15` | `15` | live touch movement and touch-control proof | `src/scenes/MenuScene.ts`, `src/input-human/touch.ts`, `src/legacy-runtime/legacyPlayStep.ts`, `src/legacy-runtime/legacyMenuLayout.ts` | continue board/player readability checks in the visual-readability lane |
| Top-down visual readability | `15` | `15` | closed for current scope with ultra-narrow play-board/touch-control separation, a cleaner mobile control deck, play-only player locator glyph, and distinct active-play start/goal endpoint markers | `src/scenes/MenuScene.ts`, `src/legacy-runtime/legacyMenuRender.ts`, `src/legacy-runtime/legacyMenuLayout.ts`, `src/input-human/touch.ts` | future visual work should be regression-driven rather than broad polish |
| Documentation, diagnostics, and proof safety | `10` | `10` | closed for current scope with checked active proof-spine documentation in current-truth and system-map | `docs/current-truth.md`, `docs/system-map.md`, `tests/**`, runtime/visual diagnostics | continue future proof-map maintenance only when new proof surfaces are added |

Current total:

- `100 / 100`

## Latest ratchet

- `2026-07-08`: stayed at `100%` after separating real phone portrait menu layout from the tiny Codex/browser side-panel layout. The menu no longer treats every sub-`360px` portrait width as side-panel chrome: normal phone widths (`320`, `360`, `390`) now keep the `Start` / `Options` buttons horizontal with safe board margins, while tiny side-panel widths below `300px` can still use the vertical two-button stack. Active-play ultra-narrow board/control safety below `360px` remains preserved. Proof: focused layout/title/render Vitest passed `3` files / `38` tests, `npm run lint`, `npm run build` producing `assets/main-Bq1yMMCj.js`, and in-app browser proof at `390x844`, `360x740`, `320x568`, and side-panel `172x407` showed `buttonLayout="row"` for the three phone viewports, `buttonLayout="stack"` for the side-panel viewport, `Start` / `Options` sharing one Y coordinate on phone, `recentSpikeCount=0`, zero warn/error logs, and screenshots/diagnostics under `tmp/mobile-menu-layout-proof/`.
- `2026-07-08`: stayed at `100%` after enhancing the front-door `MAZER` title into a stronger original jewel-cut gothic/cyber sigil lockup. The wordmark still rebuilds/deconstructs with the menu maze and still uses the same connected path-piece material, but `MenuScene` now overlays warm/mint gem facets on the title path cells, adds stronger diagonal prism glints, and runs six orbiting diamond sigils around the title on the existing bounded title-only redraw cadence. Runtime proof now publishes `title.animation.facetCellCount`, `facetPulsePeriodMs=2860`, and `sigilOrbitCount=6`. Proof: focused title/render Vitest passed `2` files / `30` tests, `npm run lint`, `npm run build` producing `assets/main-D6j9cDis.js`, full `npm run verify` passed `26` files / `174` tests plus build, and in-app browser proof at `405x958` on `http://127.0.0.1:4173/?content=core-only&theme=aurora&runtimeDiagnostics=1&v=title-gem-sigil-proof-D6j9cDis` showed `title.animation.active=true`, `facetCellCount=87`, `facetPulsePeriodMs=2860`, `sigilOrbitCount=6`, `visiblePieces=87/87`, `progressPercent=100`, `recentSpikeCount=0`, zero warn/error logs, and screenshot/diagnostics under `tmp/title-gem-sigil-proof/`.
- `2026-07-08`: stayed at `100%` after a second backdrop-shape pass on the disliked circle-to-shard replacement. The backdrop primitive budget stays the same (`8` static shards / `5` glass shards / `14` drift runes / `4` sigils), but shard templates are now smaller, thinner, and more edge-weighted; `MenuScene` renders them as faint broken parallel rails with cuts/notches/glints instead of filled hex-like floating pieces, and the large background sigils were reduced into smaller star-map/circuit rail marks. Proof: focused backdrop/render Vitest passed `2` files / `29` tests, `npm run lint`, `npm run build` producing `assets/main-BfOfxIrp.js`, full `npm run verify` passed `26` files / `174` tests plus build, and in-app browser proof at `405x958` on `http://127.0.0.1:4173/?content=core-only&theme=aurora&runtimeDiagnostics=1&v=backdrop-glyph-proof-BfOfxIrp` showed `shards=8`, `glassShards=5`, `driftRunes=14`, `sigils=4`, `starMotion=radial-warp`, `titlePieces=87/87`, `progressPercent=100`, `recentSpikeCount=0`, zero warn/error logs, and screenshot/diagnostics under `tmp/backdrop-glyph-proof/`.
- `2026-07-08`: stayed at `100%` after removing the unnecessary front-door `Exit` button and deleting the browser-safe quit-equivalence runtime path. The active main menu now exposes only a centered `Start` / `Options` pair, with a two-button ultra-narrow stack; tests and capture tooling were updated to reject the old `Exit / Start / Options` contract. Proof: focused front-door/layout Vitest packet passed `4` files / `35` tests, `npm run lint`, `npm run build` producing `assets/main-BVsVavy8.js`, full `npm run verify` passed `26` files / `174` tests plus build, and in-app browser proof at `405x958` on `http://127.0.0.1:4173/?content=core-only&theme=aurora&runtimeDiagnostics=1&v=exit-removed-proof-built-BVsVavy8b` showed rebuilt asset `main-BVsVavy8.js`, labels `["Start","Options"]`, `hasExitLabel=false`, zero warn/error logs, and screenshot/diagnostics under `tmp/exit-removed-proof/`. A pre-build browser check still showed the old `Exit` button, confirming local preview needed the fresh build before visual judgment.
- `2026-07-08`: stayed at `100%` after upgrading the front-door `MAZER` title from a static maze-path-piece wordmark into a more unique animated logo treatment. The title still rebuilds/deconstructs with the menu maze and still uses the same path-piece material, but `MenuScene` now adds top/bottom sigil rails, small crown glyphs, a green/cyan prism sweep, and anchor sparks on a bounded title-only `90ms` redraw cadence with `2200ms` sweep diagnostics. Proof: `npx tsc --noEmit`, focused title/render Vitest passed `2` files / `30` tests, `npm run lint`, `npm run build` producing `assets/main-BseLlygR.js`, full `npm run verify` passed `27` files / `177` tests plus build, and in-app browser proof at `405x958` on `http://127.0.0.1:4173/?content=core-only&theme=aurora&runtimeDiagnostics=1&v=title-animation-strong-1783508523820` showed `title.animation.active=true`, `sweepPeriodMs=2200`, `visiblePieces=87/87`, `progressPercent=100`, `recentSpikeCount=0`, zero browser logs, and screenshot/diagnostics under `tmp/title-animation-proof/`.
- `2026-07-08`: stayed at `100%` after tightening the menu backdrop shard treatment that replaced the old floating circles. Static and animated glass shards are now thinner, more edge-weighted, and rendered as low-fill rune slashes with crisp spine/facet/glint lines instead of broad translucent lozenges near the title and maze. This preserves the existing low-count backdrop budget and keeps the `8` static shards / `5` glass shards / `14` drift runes diagnostic contract. Proof: `npx tsc --noEmit`, focused backdrop/render Vitest passed `2` files / `29` tests, `npm run lint`, full `npm run verify` passed `27` files / `177` tests plus build producing `assets/main-B-AN7p4L.js`, and in-app browser proof at `405x958` on `http://127.0.0.1:4173/?content=core-only&theme=aurora&runtimeDiagnostics=1&v=backdrop-shards-rune-proof-1783509000000` showed `shards=8`, `glassShards=5`, `driftRunes=14`, `starMotion=radial-warp`, `titleVisiblePieces=87/87`, `progressPercent=100`, `recentSpikeCount=0`, zero browser logs, and screenshot/diagnostics under `tmp/backdrop-shard-rune-proof/`.
- `2026-07-08`: stayed at `100%` after turning the folded-corner border gaps from flat black underlayers into subtle translucent jewel facets. `MenuScene` now draws mint/cyan iridescent triangular corner material on the dynamic board layer with a bounded `110ms` shimmer cadence, publishes `board.cornerFacet` diagnostics, and keeps the effect independent of the larger Animated BG toggle so the border still reads alive without repainting the static maze field every frame. Proof: `npx tsc --noEmit`, focused render-frame Vitest passed `1` file / `24` tests, `npm run lint`, `npm run build` producing `assets/main-BV6bXzn9.js`, full `npm run verify` passed `27` files / `177` tests plus build, and in-app browser proof at `405x958` on `http://127.0.0.1:4173/?content=core-only&theme=aurora&runtimeDiagnostics=1&v=border-facet-proof-1783507472512` showed `cornerFacet.visible=true`, `cornerFacet.animated=true`, `shimmerPeriodMs=1800`, alpha movement `0.170 -> 0.214`, zero browser logs, and screenshots/diagnostics under `tmp/border-facet-proof/`.
- `2026-07-08`: stayed at `100%` after improving mobile maze crispness without changing gameplay topology or returning to whole-canvas pixel-art scaling. The active canvas now uses CSS `image-rendering: auto` so text and chrome stay anti-aliased, while maze wall fills, connected corridor path material frames, and cue squares snap to integer canvas-pixel boundaries for sharper phone-size rendering. Visual diagnostics now publish canvas CSS size, backing pixel size, DPR, and render-resolution ratio so future mobile blur reports can separate browser zoom/backing-store issues from maze-layer geometry. Proof: `npx tsc --noEmit`, focused render-frame Vitest passed `1` file / `24` tests, `npm run lint`, `npm run build` producing `assets/main-DoBLMOJX.js`, full `npm run verify` passed `27` files / `177` tests plus build, and in-app browser proof at `405x958` on `assets/main-DoBLMOJX.js` showed `cssImageRendering="auto"`, canvas/backing `405x958`, `renderResolutionRatio=1`, settled `title.visiblePieces=87/87`, settled draw stage `tilesVisible=1026/1026`, no warn/error logs, and screenshots/diagnostics under `tmp/mobile-crispness-proof/`.
- `2026-07-08`: stayed at `100%` after changing the menu backdrop stars from vertical fall to a centered radial-warp space-travel field. `legacyMenuBackdrop` now advances stars outward from the viewport center, recycles wrapped stars near the warp origin, points star streaks back toward center, and publishes `starMotion="radial-warp"` through runtime diagnostics for proof. Proof: `npx tsc --noEmit`, focused backdrop/render/runtime Vitest packet passed `3` files / `36` tests, `npm run lint`, `npm run build` producing `assets/main-vi54_Zab.js`, full `npm run verify` passed `27` files / `177` tests plus build, and in-app browser proof at `405x958` on `assets/main-vi54_Zab.js` showed `starMotion=radial-warp`, `farStars=180`, `shards=8`, `glassShards=5`, `driftRunes=14`, `moving=199`, `recentSpikeCount=0`, no warn/error logs, and screenshots/proof under `tmp/radial-warp-starfield-proof/`.
- `2026-07-08`: stayed at `100%` after adding a matching pre-build border burst before each trace-backed menu build starts. The existing cyan/mint sigil-border handoff still fires after the maze path layer disappears, and new menu builds now hold `rowsVisible=0` / `tilesVisible=0` for a `500ms` preroll while the same border burst renderer runs before the first generated path pieces appear. Runtime and visual diagnostics now publish `buildPrerollActive`, `buildPrerollProgress`, and `buildPrerollDurationMs` for proof. Proof: `npx tsc --noEmit`, focused reset/render/runtime Vitest packet passed `3` files / `56` tests, `npm run lint`, `npm run build` producing `assets/main-Dx6JgbtO.js`, full `npm run verify` passed `27` files / `177` tests plus build, and in-app browser proof at `405x958` on `assets/main-Dx6JgbtO.js` showed preroll `buildPrerollActive=true`, `rowsVisible=0`, `tilesVisible=0`, followed by build start `buildPrerollActive=false`, `rowsVisible=1`, `tilesVisible=11`, no warn/error logs, and screenshots/proof under `tmp/prebuild-border-burst-proof/`.
- `2026-07-08`: stayed at `100%` after normalizing title and trail tiles onto the same connected path material as the maze routes. `MenuScene` now funnels maze paths, menu title cells, and dynamic trail overlays through `drawLegacyPathMaterialTile()`, using `resolveLegacyMenuPathRenderSegments()` / `resolveLegacyMenuPathRenderFrames()` plus dedicated title-grid and trail-grid masks instead of separate square title cells and independent trail stroke ratios. Proof: focused title/render/reset Vitest packet passed `3` files / `55` tests, `npm run lint`, `npm run build` producing `assets/main-CFJYuJl3.js`, full `npm run verify` passed `27` files / `177` tests plus build, and in-app browser proof at `405x958` on `assets/main-CFJYuJl3.js` showed `title.builtFromPathPieces=true`, `visiblePieces=21` during build, `visiblePieces=87` after settle, `trailLength=18`, `board.pathVisualStyle="corridor"`, `recentSpikeCount=0`, with screenshots/proof under `tmp/path-material-sync-proof/`.
- `2026-07-08`: stayed at `100%` after replacing the floating circular backdrop-light treatment with sharper gothic/cyber backdrop primitives. The menu backdrop now resolves `8` static angular shards, `5` animated glass shards, and `14` small drift runes instead of haze orbs, glass circles, and circular motes; `MenuScene` draws those through polygon/line renderers and guards against `backdropGraphics.fillCircle` returning to the backdrop path. Proof: focused backdrop/render/runtime/reset Vitest packet passed `4` files / `61` tests, `npm run lint`, `npm run build` producing `assets/main-PN6DdhL0.js`, full `npm run verify` passed `27` files / `177` tests plus build, and in-app browser proof at `405x958` showed runtime diagnostics with `shards=8`, `glassShards=5`, `driftRunes=14`, `farStars=180`, `moving=199`, `recentSpikeCount=0`, no warn/error logs, and screenshot/proof under `tmp/angular-backdrop-proof/`.
- `2026-07-08`: stayed at `100%` after replacing the front-door Phaser text title with a maze-path-piece title layer. `MAZER` is now drawn from `87` reusable path cells across a `29`-column title grid, rendered through `titleGraphics`, and driven by the same staged visible-tile progress as the menu path layer so it rebuilds and deconstructs in pace with the maze. Proof: focused title/render Vitest packet passed `2` files / `30` tests, `npm run lint`, `npm run build` producing `assets/main-C8gsKQK7.js`, and in-app browser proof on `405x958` showed partial build `drawProgressPercent=10.2` with title `9/87` pieces, settled title `87/87`, partial deconstruct `drawProgressPercent=86.5` with title `76/87`, no warn/error logs, and screenshots/proof under `tmp/menu-path-title-proof/`.
- `2026-07-08`: stayed at `100%` after removing the visible solved-state pause before generated-menu teardown. Live generated menu demo cadence now uses `goalHoldMs=0` and `resetHoldMs=0`, the scene starts deconstruction on the same update that reaches the generated goal, and the old pre-deconstruct static hold is `0ms`; the fixed legacy snapshot cadence remains separate. Deconstruct-start also forces runtime/visual diagnostics to publish immediately for proof. Proof: focused menu lifecycle/reset Vitest packet passed `2` files / `33` tests, `npm run lint`, `npm run build` producing `assets/main-Bo5xz0oF.js`, and in-app browser proof on `405x958` showed the first `reachedGoal=true` sample already had `lifecyclePhase=deconstructing`, player equal to goal `(18,41)`, `nextSeedQueued=true`, `observedGoalToDeconstructMs=0`, `runtimeGoalToDeconstructMs=0`, and no warn/error logs; artifacts under `tmp/menu-goal-immediate-deconstruct-proof/`.
- `2026-07-08`: stayed at `100%` after a frame-rate/lag pass focused on menu trace-build rendering. The board renderer now splits stable wall/frame glass into `boardStaticGraphics` and animated route paths into `boardPathGraphics`, so menu build/deconstruct path animation no longer repaints the full wall field and folded-corner border on every path tick. During staged path animation, the path layer iterates only the visible ordered path tiles instead of scanning all `49x49` cells. Animated backdrop cadence was reduced from `66ms` to `100ms` in full mode and diagnostics publish cadence from `1000ms` to `1500ms`. Proof: focused render/runtime diagnostics tests passed `2` files / `31` tests, `npm run lint`, `npm run build` producing `assets/main-DJSWSuMx.js`, full `npm run verify` passed `27` files / `176` tests plus build, and in-app browser proof at `405x958` on `assets/main-DJSWSuMx.js` showed settled `recentSpikeCount=0`, `worstRecentFrameMs=15.29`, `estimatedFps=90.66`, `activeTimers=0`, `activeTweens=0`, and `boardStaticDirty=false`, `boardPathDirty=false`, `boardDynamicDirty=false`; artifacts under `tmp/perf-layer-split-proof/`.
- `2026-07-08`: stayed at `100%` after tightening the menu/play maze render frame so paths stay inside the gothic folded-corner border. The full board frame remains unchanged, but maze paths, walls, trail, player, start, goal, compass vectors, and diagnostics now use a bounded inner render frame with a `4px-7px` safe inset. Proof: focused render/runtime diagnostics Vitest packet passed `2` files / `31` tests, `npm run lint`, `npm run build` producing `assets/main-Wz_hDgY5.js`, and in-app browser proof at `390x844` showing board `294x294`, render frame `284x284`, safe inset `5px`, `lifecyclePhase=settled`, `recentSpikeCount=0`, screenshot/diagnostics under `tmp/menu-border-safe-inset-proof/`, and pixel safety check showing `0` path-colored pixels in the outer safety ring.
- `2026-07-08`: stayed at `100%` after making the glass-backed menu backdrop visibly move through translucent maze walls without re-expanding the starfield budget. The backdrop now adds `5` broad glass veils and `14` drift motes behind the board; the `Animated BG` toggle animates them, while stagnant mode resolves the same layer statically. Proof: focused backdrop/render/reset Vitest packet passed `3` files / `54` tests, and browser proof at `390x844` with `Animated BG=Animated`, `lifecyclePhase=settled`, `farStars=180`, `veils=5`, `driftMotes=14`, `moving=199`, `recentSpikeCount=0`, screenshots plus boosted diff under `tmp/wall-transparency-investigation/glass-veils/`, and wall-center mean frame diff `0.9184` versus the prior frame-only proof's `0.7536`.
- `2026-07-08`: stayed at `100%` after adding a short visual handoff between generated-menu teardown and the next trace-backed build. Once the final path tile is removed, the menu now holds the stable wall field/title/buttons/border for `500ms`, publishes `handoffActive` / `handoffProgress` / `handoffDurationMs` diagnostics, and draws a brief cyan/mint star-burst on the sigil border before the next queued seed starts building. Proof: focused reset/render Vitest packet passed `2` files / `49` tests, `npm run lint`, `npm run build` producing `assets/main-MIsQnrvn.js`, full `npm run verify` passed `27` files / `175` tests plus build, and in-app browser proof showing `deconstructing` with `tilesVisible=0`, `nextSeedQueued=true`, `handoffActive=true`, `handoffDurationMs=500`, `handoffProgress=0.142`, then next build seed `227730096`; screenshots under `tmp/menu-handoff-burst-proof/`.
- `2026-07-08`: stayed at `100%` after forcing generated menu playback to wait at the start during trace-build. Generated menu bootstrap now sets `prerollSteps: 0`, so the player starts on the generated start tile with a one-tile trail while `lifecyclePhase=building`, then movement begins only after the build settles. The fixed legacy snapshot still keeps its deep preroll behavior. Proof: focused AI/reset/menu lifecycle Vitest packet passed `3` files / `49` tests, `npm run lint`, `npm run build`, and in-app browser proof on `assets/main-C-9b2yGt.js` showing `building` with `pathCursor=0`, `trailLength=1`, player equal to the only trail tile, followed by post-build movement; screenshots under `tmp/menu-player-wait-build-proof/`.
- `2026-07-08`: stayed at `100%` after removing the confusing AI-only replay reset from the live generated menu demo config. Generated menu AI still keeps mistake/backtrack/recovery route character, but `emulateLogicSwitchPotentialCheckBug` is disabled for generated menu playback so the demo reaches `goal-hold`, requests goal regeneration, and then uses the visible player-removal -> trail-fade -> path-deconstruct loop. The fixed legacy snapshot config keeps the old logic-switch replay behavior for source-truth modeling. Proof: focused AI/reset/menu lifecycle Vitest packet passed `3` files / `49` tests, `npm run lint`, `npm run build`, full `npm run verify` passed `27` files / `175` tests, and in-app browser proof on `assets/main-CKczm4b0.js` showed goal-hold at player/goal `(24,1)`, deconstructing with `nextSeedQueued=true`, and next build seed `703652994`; screenshots under `tmp/menu-goal-gated-reset-proof/`.
- `2026-07-08`: stayed at `100%` after correcting menu teardown ordering so deconstruction no longer teleports the demo player back to start. The scene now preserves the last goal-position player/trail state, hides the player first, fades the existing trail, and only then starts static path removal before the next queued build. Proof: focused reset/runtime Vitest packet, `npm run lint`, `npm run build`, and in-app browser proof on `assets/main-BpMq9RnG.js` showing settled player `(46,15)` equals first-deconstruct player `(46,15)`, first-deconstruct player equals goal `(46,15)`, and first path removal starts after the cleanup window; screenshots under `tmp/menu-deconstruct-no-teleport-proof-fresh/`.
- `2026-07-08`: stayed at `100%` after making the trace-backed menu build loop seamless instead of one-way. The front-door menu now keeps the backdrop, title, buttons, wall field, and sigil border stable while the generated walkable path layer deconstructs in reverse, queues the next seed during teardown, publishes a zero-visible diagnostics handoff, and immediately starts the next trace-backed build. Proof: focused reset/runtime Vitest packet, `npm run lint`, `npm run build`, and in-app browser proof on `assets/main-B00pd5I5.js` showing `building -> settled -> deconstructing(tilesVisible=0,nextSeedQueued=true,pendingReason=menu-demo-goal-reset) -> building` with seed `1658520238 -> 1658520239` plus screenshots under `tmp/menu-deconstruct-loop-proof-final/`.
- `2026-07-03`: `84% -> 85%` after adding flat, visible, diagnostics-backed mobile touch controls that share the same resolver as the active touch hit targets. Proof: `npm run lint`, focused Vitest reset/input/render/edge tests, `npm run build`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and a direct 390x844 Playwright diagnostics/screenshot probe showing board bottom `594`, touch frame `19,615 366x216`, visible compact controls, and legacy HUD bounds still isolated at `14,4 354x32`.
- `2026-07-03`: `85% -> 86%` after replacing ultra-narrow mobile overlay shrink rules with readability-first marker/trail sizing and stronger active-play player contrast. Proof: focused Vitest reset/AI/render packet passed `20` files / `138` tests, `npm run build`, `git diff --check`, and in-app-browser play-route proof at effective `166x359` CSS viewport with board bounds inside safe bounds, `tileSize=3.327`, no console errors, and visible centered player/trail/touch controls.
- `2026-07-03`: `86% -> 87%` after widening route-quality reinforcement for generated mazes and adding seed-family guards proving default play and generated-menu mazes stay connected with meaningful alternate routes. Proof: focused reset/AI/render packet passed `20` files / `140` tests, including `16` default play seeds and `16` default menu seeds with no detached floors and `multi-route` route quality.
- `2026-07-03`: `87% -> 88%` after widening generated-menu AI playback proof from a small representative set to the full `16`-seed default menu family plus the existing larger scale case. Proof: `npx vitest run tests/ai/demo-walker.test.ts --reporter=dot` passed `15` tests, including route diagnostics bounds and adjacent playback for `17` generated-menu cases.
- `2026-07-03`: `88% -> 89%` after closing the active-play keyboard/touch mechanics proof gap: desktop keyboard proof now launches directly into the supported `mode=play` route, focuses the game canvas, models the 50ms simultaneous-key input buffer with held movement keys, reads fresh visual-runtime player state, and keyboard controls now cover pause, restart, and trail/thought toggle through the same play command path as touch. Proof: `npm run lint`, `npx vitest run tests/visual/edge-live-check.test.ts --reporter=dot`, `npm run edge:live -- --skip-build true --headless true --run play-mode-interactive`, and `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`.
- `2026-07-03`: `89% -> 90%` after fixing ultra-narrow active-play readability so compact touch controls no longer overlap the maze board. Tight portrait touch controls now scale down below `360px` width, stay inside the viewport, and `172x407` / `320x568` play layouts reserve a clear lane between board and controls while normal `390x844` play remains centered. Proof: focused touch/layout Vitest tests, `npm run build`, and direct Playwright diagnostics showing `172x407` board bottom `226.001`, touch frame top `241`, gap `15`; `320x568` board bottom `361.992`, touch frame top `396`, gap `34`; and `390x844` board bottom `594`, touch frame top `615`, gap `21`.
- `2026-07-03`: `90% -> 91%` after widening the generated-maze topology audit exposed weak seeds and the generator fix closed them. Shortcut reinforcement now scores candidate bridge openings by route-quality improvement instead of blindly accepting random valid bridges, and final post-shortcut route state can rebase a shortened goal to the farthest reachable floor. Proof: `npx vitest run tests/reset/legacy-reset.test.ts --reporter=dot` passed with default play and generated-menu audits covering seeds `1..64` plus `89`, `144`, `233`, `3749`, `777`, `1001`, and `0x5a17f00d`.
- `2026-07-04`: `91% -> 92%` after replacing the crowded phone play controls with a full-width bottom control deck: movement stays on a D-pad, pause becomes the primary right-side action, and restart/trail remain smaller secondary actions. Proof: focused touch/layout Vitest tests, `npm run lint`, `npm run build`, `npx vitest run tests/scenes/menu-render-frame.test.ts tests/visual/edge-live-check.test.ts --reporter=dot`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and direct geometry probes showing clear board/control gaps at `390x844` (`45px`), `360x740` (`34px`), `320x568` (`22px`), and `172x407` (`49px`).
- `2026-07-04`: `92% -> 93%` after making the active-play player readable on tiny mobile tiles without changing gameplay: play mode now draws a top-down locator ring/tick glyph over the existing halo/core marker, while menu demo keeps the older marker treatment. Proof: `npx vitest run tests/scenes/menu-render-frame.test.ts --reporter=dot`, `npm run lint`, `npm run build`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and the maintained phone portrait artifact at `C:\ATLAS\tmp\captures\mazer-edge-live\mobile-touch-smoke\screenshots\phone-portrait-attempt-lifecycle.png`.
- `2026-07-04`: `93% -> 94%` after closing the current visual-readability lane with distinct active-play endpoint glyphs: start now renders as a small target ring/core and goal renders as a red diamond/core marker, while menu endpoints keep their existing treatment. Proof: `npx vitest run tests/scenes/menu-render-frame.test.ts --reporter=dot`, `npm run lint`, `npm run build`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and the maintained phone portrait artifact at `C:\ATLAS\tmp\captures\mazer-edge-live\mobile-touch-smoke\screenshots\phone-portrait-attempt-lifecycle.png`.
- `2026-07-04`: `94% -> 95%` after the shortcut-enabled scale-band topology audit exposed and closed an under-braided generated-menu edge case at scale `37`, seed `55`. Generated-menu mazes now apply a small shortcut-stage budget floor once process `5` is active, preserving shortcut-disabled behavior and existing scale-50 diagnostics while proving play/menu mazes across scales `37`, `50`, and `75` stay connected, non-degenerate, and meaningfully multi-route. Proof: `npx vitest run tests/reset/legacy-topology-scale-audit.test.ts --reporter=dot`, `npx vitest run tests/reset/legacy-reset.test.ts --reporter=dot`, and `npx vitest run tests/reset/legacy-topology-scale-audit.test.ts tests/reset/legacy-generation-lifecycle.test.ts tests/reset/legacy-generation-diagnostics.test.ts --reporter=dot`.
- `2026-07-04`: `95% -> 96%` after closing the current menu AI/demo loop proof gap for mechanics/mobile scope. Generated-menu AI now has scale-band regression proof that it can enter a wrong-branch recovery, perform an AI-only reset without requesting regeneration, replay the same maze from the start, reach goal reset, and then request the later goal regeneration without non-adjacent moves or runaway route timing. Proof: `npx vitest run tests/ai/demo-walker.test.ts --reporter=dot`.
- `2026-07-04`: `96% -> 97%` after widening the generated-maze topology proof beyond the existing shortcut-enabled bands with a bounded large-scale smoke at scale `99` for both play and generated-menu builders. The audit keeps the normal proof spine practical while proving no detached floors, non-degenerate route length, and meaningful multi-route quality at the larger board size. Proof: `npx vitest run tests/reset/legacy-topology-scale-audit.test.ts --reporter=dot`.
- `2026-07-04`: `97% -> 98%` after making the active proof spine explicit and test-guarded. `current-truth.md` now states that `npm run verify` runs `npm run test:verify` plus `npm run build`, and that `test:verify` covers reset, demo walker, menu render-frame, and marker/topology contracts under `--maxWorkers 1`; `legacy-marker.test.ts` guards those doc/script/package contracts against drift. Proof: `npx vitest run tests/reset/legacy-marker.test.ts --reporter=dot`.
- `2026-07-04`: `98% -> 99%` after closing the current proof-map drift in `docs/system-map.md`. The system map now points at the active mechanics/mobile marker and current-truth instead of the retired visual marker, documents the exact `verify` / `test:verify` spine, and records the generated-menu AI recovery/replay/regeneration proof obligation; `legacy-marker.test.ts` now guards system-map proof-spine entries too. Proof: `npx vitest run tests/reset/legacy-marker.test.ts --reporter=dot`.
- `2026-07-04`: `99% -> 100%` after closing the remaining current-scope topology proof gap with a bounded scale-`149`, seed-`55` smoke for both play and generated-menu builders. The normal proof spine now includes connected, non-degenerate, meaningful multi-route evidence at scale bands `37`, `50`, `75`, large scale `99`, and the bounded extreme `149` case without adding a broad slow stress sweep. Proof: `npx vitest run tests/reset/legacy-topology-scale-audit.test.ts --reporter=dot`.
- `2026-07-06`: stayed at `100%` after a post-completion top-down readability maintenance pass for active play. The played board now keeps the same connected-corridor renderer but uses a higher-contrast mint path core, stronger dark-teal corridor edge, darker flat walls, and a slimmer active trail overlay so the board reads closer to a crisp dungeon-map view on mobile. Proof: focused render/runtime/reset/edge Vitest packet, `npm run lint`, `npm run build`, and `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke` with trail reset and green/red marker pixel gates passing.
- `2026-07-06`: stayed at `100%` after a mobile control/HUD formatting pass. Phone portrait play now separates the top action bar (`PAUSE`, `RESET`, `TRAIL`) from a bottom retro arrow-key D-pad, keeps the trail toggle explicit instead of the old unlabeled connected-dot icon, and upgrades the goal compass into a framed red exit needle. Proof: focused touch/HUD/render/reset Vitest packet, `npm run lint`, `npm run build`, and `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`.
- `2026-07-06`: stayed at `100%` after hardening the mobile hosted-proof gate so stale or regressed previews are easier to catch. The mobile touch smoke now fails unless runtime diagnostics prove top action buttons, a bottom D-pad, and a right-side compass frame in addition to the existing trail reset, marker style, and marker pixel checks. Proof: `npx vitest run tests/visual/edge-live-check.test.ts --reporter=dot`, focused affected Vitest packet, `npm run lint`, `npm run build`, local `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, hosted mobile touch smoke against preview commit `58ea1e43`, and in-app browser diagnostics at `390x844` showing visible touch controls with top actions and bottom D-pad.
- `2026-07-06`: stayed at `100%` after simplifying the mobile play controls and tightening touch behavior. The visible mobile deck now removes the confusing `TRAIL` button, keeps only `PAUSE` and `RESET` as top actions, moves the compass into the center of the bottom D-pad, makes movement require tapping an actual arrow key instead of swiping or tapping the board, and broadens the active-play trail overlay so it fills the corridor more cleanly. Proof: focused touch/HUD/render/reset/edge Vitest packet, `npm run lint`, `npm run build`, and rebuilt `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke` with board overflow, HUD overlap, HUD clip, trail reset, marker style, touch chrome, marker pixels, and arrow-key movement all passing.
- `2026-07-06`: stayed at `100%` after adding touch-only diagonal arrow keys to the mobile play D-pad. The four corner buttons now publish exact hit rectangles, render as diagonal retro arrows around the centered compass, and route through a pure `advanceLegacyPlayDiagonalSequence()` helper that tries both cardinal orderings for the requested diagonal and walks the open sequence until blocked, capped, or goal. Proof: focused input/play-step/layout/edge Vitest packet, `npm run lint`, `npm run build`, local `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and direct browser diagnostics/click proof showing `move_down_right` moved the player from `(37,33)` to `(38,34)` with trail count increasing to `3`.
- `2026-07-06`: stayed at `100%` after correcting diagonal touch controls from path-end jumps to button-like held movement. Diagonal taps now execute one visible cardinal substep from the two possible axis orderings; held controls repeat the same one-step command after an intentional hold delay, clear when released or blocked, and publish touch-sprint diagnostics for proof. Proof: focused play-step/touch/runtime/reset Vitest packet, `npm run lint`, `npm run build`, local `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, `npm run verify`, and controlled fresh-browser mobile proof showing `move_down_right` tap moved from `(37,33)` to `(38,33)` with held state cleared instead of jumping to `(38,34)` or path end.
- `2026-07-06`: stayed at `100%` after making held mobile movement steadier and enabling real two-hand touch input. Touch hold now repeats at a controlled cadence after a shorter intentional delay, Phaser is configured for two active touch pointers, and concurrent held arrows keep press-order priority instead of synthesizing a teleport-like diagonal move; the first-held direction remains primary and the second is retained as fallback. Proof: `npm run lint`, focused Vitest input/touch/runtime/reset packet, `npm run build`, local `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and controlled fresh-browser mobile proof showing tap delta `(0,1)`, held down delta `(0,2)` before path block, and concurrent diagnostics `activeControls=["move_down","move_right"]` with `heldControl="move_down"`.
- `2026-07-06`: stayed at `100%` after closing the local-preview stale-bundle failure that hid recent mobile-control edits in the in-app browser. Vite no longer auto-injects `registerSW.js`; production service-worker registration is now manual and skipped on localhost, while localhost cleanup remains in boot. Proof: in-app browser showed the stale `assets/main-l7Y6NELT.js` bundle before cache clearing, then refreshed to current `assets/main-tbIAkYGi.js` with `hasRegisterSWScript=false`, live runtime diagnostics, all eight movement controls, and headless touch proof on the same build showing tap delta `(0,1)`, concurrent `activeControls=["move_down","move_right"]`, held movement delta `(0,2)`, and release clearing held state.
- `2026-07-07`: stayed at `100%` after fixing held movement repeat for mouse/trackpad-style button holds. The repeat path no longer prunes active held controls by polling Phaser pointer identity, which could clear the hold before the first repeat; held controls now release through pointerup, pointerupoutside, focus-loss, reset, block, or mode/overlay guards. Proof: `npm run lint`, focused input/runtime Vitest packet, `npm run build`, in-app browser refreshed to `assets/main-UhmvgTgJ.js` with live diagnostics, mouse-hold proof showing down hold moved from `(37,33)` to `(37,35)` before corridor block, touch two-pointer proof showing `activeControls=["move_down","move_right"]`, held delta `(0,2)`, and release clearing held state, plus `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`.
- `2026-07-07`: stayed at `100%` after adding a compass onboarding animation for active play. On initial play load and reset-player actions, the compass needle now spins for several eased rotations, slows down, and settles exactly onto the true goal angle while diagnostics expose both true and visual compass angles. Proof: `npm run lint`, focused HUD/reset Vitest packet, `npm run build`, timed browser proof on `assets/main-BqGnHumo.js` showing initial spin active, settled angle diff `0`, reset spin active, and reset-settled angle diff `0`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and in-app browser proof showing `compassSpinActive=true` with visual angle differing from the true goal angle during animation.
- `2026-07-07`: stayed at `100%` after improving active-play trail readability with a Fitness-inspired pulse pass. The played trail keeps the connected corridor geometry but now redraws a green/pale-edge pulse packet from the player backward along the traveled path, with diagnostics exposing pulse color and period for future proof scripts. Proof: `npm run lint`, `npx vitest run tests/scenes/menu-render-frame.test.ts tests/scenes/menu-runtime-diagnostics.test.ts tests/visual/edge-live-check.test.ts --reporter=dot`, `npm run build`, serial `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and in-app browser proof on `assets/main-BdCazDTl.js` showing on-screen controls grew the trail to `5`, marker diagnostics reported `trailPulseColor=3604349`, `trailPulseEdgeColor=15532021`, `trailPulsePeriodMs=1400`, and a board-region frame diff changed while idle.
- `2026-07-07`: stayed at `100%` after making arrow-control feedback apply to the on-screen D-pad as well as physical keyboard input. D-pad rendering now derives active button state from both keyboard movement flags and held touch controls, publishes `touchControls.activeControls`, and dedupes duplicate same-button pointer paths so mouse/touch fallback cannot double-count a held button. Proof: `npm run lint`, focused render/runtime/input Vitest packet, `npm run build`, serial `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and in-app browser proof on `assets/main-cNoBHfXv.js` showing a held on-screen down button reported `activeControls=["move_down"]`, runtime touch sprint reported one `move_down`, release cleared both, and trail count increased to `3`.
- `2026-07-07`: stayed at `100%` after fixing quick on-screen arrow taps that could latch into continuous movement. The document-level fallback path now passes pointer identity into held touch controls and has matching `pointerup`/`pointercancel` release cleanup, so mouse/trackpad-style on-screen button presses stop repeating immediately on release while real holds still repeat. Proof: `npm run lint`, focused render/runtime/input Vitest packet, `npm run build`, and in-app browser proof on `assets/main-DXqQiFhb.js` showing a quick down press moved from `(37,33)` to `(37,34)`, release cleared `activeControls` and `touchSprint`, no additional movement occurred after `850ms`, and an intentional hold still advanced two tiles before stopping at the corridor block.
- `2026-07-07`: stayed at `100%` after adding a pause-menu control-mode switch and flattening feature controls out of nested submenus. Options and Pause now expose Camera, Trail, Dark, and Controls directly under `Game Toggles`; Camera/Trail labels report actual `On`/`Off` state; the Controls toggle switches the mobile play surface between arrow D-pad and a circular stick/drag control with the compass centered in the inner deadzone. Proof: `npm run lint`, focused touch/overlay/render/reset Vitest packet, `npm run build`, repeated `npm run verify`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and browser proof on `assets/main-V64Vh_Yd.js` showing `Controls Stick` after toggle, `touchControls.controlMode="stick"`, compass bounds equal to the stick inner rect, and a center-to-right-ring drag moving the player from `(37,33)` to `(38,33)` with no held state or post-release movement.
- `2026-07-07`: stayed at `100%` after hardening stick drag capture so click-hold-drag does not lose control when the pointer slips outside the visual ring or hits a wall. Stick presses must still start inside the stick, but captured drags now keep directional control beyond the outer radius, pause inside the deadzone without releasing capture, and keep blocked directions active until release or drag-change. Proof: `npm run lint`, focused touch/render Vitest packet, `npm run build`, and browser proof on `assets/main-CoEJo9Os.js` showing outside-right drag stayed `activeControls=["move_right"]`, deadzone cleared active movement without losing capture, outside-left drag resumed `activeControls=["move_left"]` and moved the player back from `(38,33)` to `(37,33)`, and pointer release cleared held state.
- `2026-07-07`: stayed at `100%` after removing the rectangular compass pane from stick-control mode. The stick compass now renders inline inside the deadzone with the circular compass marks and needle only, while non-stick compass surfaces can keep their pane. Proof: focused render Vitest packet, `npm run lint`, `npm run build`, and phone-size screenshot/runtime proof on `assets/main-Dzhnjpde.js` showing `touchControls.controlMode="stick"` and `hud.arrowBounds` equal to the stick inner rect without the white rounded pane.
- `2026-07-07`: stayed at `100%` after cleaning up the Options/Pause `Game Toggles` layout with Fitness-style switch rows. The old compact toggle grid is now four full-width stacked rows, one per setting, with active/inactive row styling, explicit state text, and a pill switch affordance; the mobile overlay panel also reserves enough vertical space for the stacked controls. Proof: focused reset/render Vitest packet, `npm run lint`, `npm run build`, and phone-size screenshot/runtime proof on `assets/main-D1-G9jzd.js` showing the Pause overlay in play mode, stacked switch rows, and the `Controls` row toggling runtime `controlMode` to `stick`.
- `2026-07-07`: stayed at `100%` after slowing the active-play trail pulse and making it user-controllable from `Game Toggles`. The pulse period now runs at `2600ms` instead of `1400ms`, the new `Trail Pulse` switch defaults to `On`, and turning it `Off` disables both pulse redraw scheduling and pulse drawing while keeping the base trail visible. Proof: focused overlay/render/runtime Vitest packet, `npm run lint`, `npm run build`, in-app browser screenshot proof showing `Trail Pulse` switching from `On` to `Off`, and local phone-size runtime proof on `assets/main-4BAk-ohD.js` showing `trailPulsePeriodMs=2600`, `trailPulseEnabled=true` before the click, and `trailPulseEnabled=false` after clicking the row.
- `2026-07-07`: stayed at `100%` after adding an `Animated BG` game toggle for a lower-work stagnant background mode. The toggle defaults to `On`; turning it `Off` repaints the current starfield once, then stops star advancement and per-frame backdrop invalidation while leaving the static background visible. Proof: focused overlay/runtime Vitest packet, `npm run lint`, `npm run build`, in-app browser screenshot proof showing `Animated BG` switching from `On` to `Off`, and local phone-size runtime proof on `assets/main-BMJHgfUc.js` showing `animatedBackdropEnabled=true` / `backdropDirty=true` before the toggle and `animatedBackdropEnabled=false` / `backdropDirty=false` after the toggle.
- `2026-07-07`: stayed at `100%` after making the maze's non-path wall cells target a translucent-glass read instead of opaque blocks. Menu and play wall fills plus the board base were intended to leave the backdrop visible through the dark grid areas while the path corridors stay crisp, but a later investigation found the opaque board-edge slab still blocked that interior glass stack until the `2026-07-08` frame-only edge fix below. Proof: focused render-frame Vitest packet, `npm run lint`, `npm run build`, and in-app browser phone screenshot proof on `assets/main-RXy4ZJTx.js` at `v=wall-glass-proof`.
- `2026-07-08`: stayed at `100%` after fixing the hidden blocker behind the maze glass pass. Investigation found the wall and board fills were translucent, but the board edge was still drawn as one full opaque rectangle under the whole maze, blocking the backdrop from showing through wall cells. The edge now renders as a true 1px frame only, leaving the interior glass stack as backdrop -> translucent board base -> translucent wall cells. Proof: focused render/backdrop Vitest packet, `npm run build`, and in-app browser settled-board screenshot diff at `tmp/wall-transparency-investigation/frame-fix-settled/` showing `Animated BG=Animated`, `lifecyclePhase=settled`, and wall-cell center changes rising from the previously blocked sample.
- `2026-07-07`: stayed at `100%` after adding a thin gothic-cyber sigil treatment to the active maze frame and reflecting the same angular motif into the animated backdrop. The board now draws a crisp outside sigil border after the maze tiles, the backdrop base moved from plain purple to deep blue-violet, and four low-alpha background sigils pulse with the starfield while remaining static when animated background is off. Proof: focused render/runtime Vitest packet, `npm run lint`, `npm run build`, and in-app browser phone screenshot/diagnostics proof on `assets/main-gFkDZpMK.js` at `v=sigil-border-proof` showing `sigils=4`, `farStars=520`, and `animatedBackdropEnabled=true`.
- `2026-07-07`: stayed at `100%` after making `Game Toggles` persist per browser/device through safe localStorage. Camera follow, trail fade, trail pulse, animated background, dark mode, and controls mode now load before menu/play startup and write after each toggle change, while corrupt or blocked storage falls back to defaults without interrupting gameplay. Proof: focused persistence/toggle/render Vitest packet, `npm run lint`, `npm run build`, and in-app browser proof on `assets/main-BhmoPUh6.js` showing seeded `localStorage` restored `controlMode="stick"`, `trailPulseEnabled=false`, and `animatedBackdropEnabled=false` after reload.
- `2026-07-07`: stayed at `100%` after centering the mobile controls in the lower play lane and hardening stick drag range. Phone portrait touch layout now centers the D-pad/stick frame between the maze bottom and screen bottom when board bounds are known, and captured stick drags can stay pulled toward far-screen positions until pointer release. Proof: focused touch/render Vitest packet, `npm run lint`, `npm run verify`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and in-app browser proof on `assets/main-Pok1ZF13.js` at `v=stick-range-center-final-proof` showing board bottom `651`, frame center `804.5` matching the bottom-lane center, far-right drag holding `activeControls=["move_right"]`, and release clearing both visual and runtime touch state.
- `2026-07-07`: stayed at `100%` after making stick-control dragging feel like one continuous wheel instead of snapped button slots. Stick input now resolves a continuous pull vector with distance percent for knob rendering while preserving the nearest 8-way movement command, and captured stick drags normalize to one current held direction instead of retaining stale directions while rotating. Proof: focused touch/render Vitest packet, `npm run lint`, `npm run verify`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and in-app browser proof on `assets/main-BSUrP22_.js` at `v=stick-smooth-wheel-final-proof` showing right/up/left/down drag samples with single active controls and smooth pull vectors near `0.908` distance ratio before release cleared both pull and runtime touch state.
- `2026-07-07`: stayed at `100%` after unifying the active-play HUD, on-screen controls, pause/options controls, and menu buttons around the gothic-cyber chrome. The play timer is now a prominent centered HUD sigil panel, the smaller pause/reset actions flank it left/right, the D-pad/stick control frames no longer draw an extra outer rectangular plate, and the pause/options switch rows plus action buttons share the same dark mint/cyan style. Proof: focused HUD/touch/render/edge Vitest packet, `npm run lint`, `npm run build`, `npm run verify`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, direct 390x844 browser diagnostics showing `timerCentered=true`, `timerProminent=true`, `pauseLeftOfTimer=true`, `resetRightOfTimer=true`, `compassCenteredInDpad=true`, stick-mode diagnostics showing `controlMode="stick"` and compass bounds matching the stick inner rect, and screenshots at `C:\ATLAS\tmp\mazer-hud-cyber-proof\mobile-play-hud.png`, `C:\ATLAS\tmp\mazer-hud-cyber-proof\mobile-stick-control-390x844.png`, `C:\ATLAS\tmp\mazer-hud-cyber-proof\mobile-pause-menu-key.png`, and `C:\ATLAS\tmp\mazer-hud-cyber-proof\desktop-menu-buttons.png`.
- `2026-07-07`: stayed at `100%` after a performance maintenance pass for reported glitching/freezing. The per-frame visual diagnostics DOM write was throttled to the existing diagnostics cadence, animated backdrop redraws now use the ambient runtime cadence instead of invalidating every frame, moving backdrop actors were reduced from `520` to `180`, and fresh sessions now default `Animated BG` to `Off` while still preserving any explicit persisted user choice. Proof: focused render/runtime/backdrop/toggle Vitest packets, `npm run lint`, clean `npm run build`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, runtime-observe receipts at `C:\ATLAS\tmp\captures\mazer-runtime-observe\perf-glitch-baseline.summary.json`, `C:\ATLAS\tmp\captures\mazer-runtime-observe\perf-glitch-after.summary.json`, and `C:\ATLAS\tmp\captures\mazer-runtime-observe\perf-glitch-after-star-budget.summary.json`, plus in-app browser proof on `assets/main-DCNZ64DK.js` showing no warn/error logs, `movingBackground=180`, `recentAverageFrameMs=10.12`, `estimatedFps=98.82`, and `recentSpikeCount=0`.
- `2026-07-07`: stayed at `100%` after tightening active-play on-screen movement response. Held touch movement still advances one tile per tick, but the post-tap repeat delay dropped from `280ms` to `160ms` and the held-repeat cadence dropped from `140ms` to `100ms`; runtime diagnostics now publish both timing values for browser-side feel proof. Proof: focused render/runtime/play-step/touch/keyboard Vitest packet, `npm run lint`, `npm run build`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, and in-app browser proof on `assets/main-CAxQqEoq.js` showing `repeatInitialDelayMs=160`, `repeatIntervalMs=100`, no warn/error logs, full performance mode, and `recentSpikeCount=0`.
- `2026-07-07`: stayed at `100%` after fixing the on-screen single-click double-step regression and tightening the stick/glass readability pass. Canvas pointer events are now owned by Phaser instead of also being consumed by the document fallback, the first held-repeat threshold is back to `240ms` while the repeat cadence stays `100ms`, stick drags clear movement in the deadzone instead of replaying stale left/right commands, cardinal stick directions win when one axis is clearly dominant, and maze walls/board fill plus trail strokes are slimmer and more translucent. Proof: focused touch/render/runtime Vitest packet passed `34` tests, `npm run lint`, `npm run build` producing `assets/main-Drh3TDbU.js`, repeated `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, `git diff --check`, and in-app browser proof on `assets/main-Drh3TDbU.js` showing the same on-screen down click moved from `(37,33)` to `(37,34)` only, stayed there after `650ms`, cleared `activeControls`, kept `repeatInitialDelayMs=240` / `repeatIntervalMs=100`, and reported no warn/error logs.
- `2026-07-07`: stayed at `100%` after a heavy stick-control lag investigation and hot-path cleanup. Investigation found no console errors, no attached terminal server logs, and good idle frame diagnostics, but the stick drag path was still doing unnecessary work: repeated same-direction pointer moves restarted held movement, every drag forced visual/runtime diagnostic DOM writes, compass animation dirtied the whole dynamic board, and trail pulse requested a dynamic board redraw every frame. The fix splits HUD invalidation from dynamic board redraws, throttles trail-pulse redraw scheduling to `50ms`, avoids restarting an unchanged held stick direction, and publishes non-forced diagnostics while the pointer is moving. Proof: `npm run lint`, focused render/runtime/touch Vitest packet passed `34` tests, `npm run build` producing `assets/main-kJKqYYsW.js`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, full `npm run verify` passing `22` files / `158` tests plus build, and in-app browser multi-point stick-drag proof on `assets/main-kJKqYYsW.js` showing the player moved from `(37,33)` to `(38,32)`, release cleared active touch state, no warn/error logs, `recentAverageFrameMs=7.06`, `estimatedFps=141.64`, and `recentSpikeCount=0`.
- `2026-07-07`: stayed at `100%` after making stick control intent more fluid without changing maze movement into impossible non-grid directions. The stick now resolves a `16`-segment intent wheel and publishes both the display movement plus ordered movement candidates; held stick movement uses those candidates so transition zones such as left/up or right/up can fall back to the second cardinal direction when the first direction is blocked or wrong for the branch. Proof: `npm run lint`, focused input/render/runtime Vitest packet passed `34` tests, `npm run build` producing `assets/main-Cv0yNtMb.js`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, full `npm run verify` passing `22` files / `158` tests plus build, `git diff --check`, and in-app browser phone proof on `assets/main-Cv0yNtMb.js` showing stick diagnostics with `intentSegment=15` and candidates `["move_right","move_up"]`, `intentSegment=12` and candidate `["move_up"]`, `intentSegment=9` and candidates `["move_left","move_up"]`, release clearing `pull` and `activeControls`, no warn/error logs, `estimatedFps=144`, and `recentSpikeCount=0`.
- `2026-07-07`: stayed at `100%` after preparing the pressure-mechanics direction and making stick intent more stable under timed-play assumptions. The stick resolver now supports segment hysteresis, so a thumb hovering near a neighboring `16`-segment boundary keeps the previous intent until the drag clearly enters the next zone; this keeps fast room/enemy/obstacle play from feeling like random direction flicker. A pressure-mechanics prep note now records the safe build order: control pressure first, room topology next, hazards before enemies, and no fake non-grid movement. Proof: `npm run lint`, focused input/render/runtime Vitest packet passed `34` tests, `npm run build` producing `assets/main-CX-SI7P7.js`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, full `npm run verify` passing `22` files / `158` tests plus build, `git diff --check`, and in-app browser phone proof on `assets/main-CX-SI7P7.js` showing right baseline `intentSegment=0`, near right/down boundary still `intentSegment=0`, decisive right/down transition `intentSegment=1` with candidates `["move_right","move_down"]`, decisive up `intentSegment=12`, release clearing `pull` and `activeControls`, no warn/error logs, `estimatedFps=140.11`, and `recentSpikeCount=0`.
- `2026-07-07`: stayed at `100%` after restoring fresh maze ownership for normal menu/play launches. Runtime boot now resolves explicit `?seed=` / `?mazeSeed=` first for proof routes, otherwise creates a fresh runtime seed and refreshes unpinned play/menu entries; diagnostics expose whether a maze came from `query` or `runtime-random`, and the maintained mobile smoke route pins `mazeSeed=3749` to avoid proof flake. Proof: focused seed/edge/runtime/generation Vitest packet passed `32` tests, `npm run lint`, `npm run build` producing `assets/main-M1I0LzAF.js`, in-app browser proof showing two unpinned play launches generated different runtime-random seeds `2620058136` and `586363897` while pinned `mazeSeed=3749` stayed `query`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, full `npm run verify` passing `23` files / `161` tests plus build, and `git diff --check`.
- `2026-07-07`: stayed at `100%` after adding a clean held-movement speed slider to the pause/options Game Toggles. The slider persists with the other game controls, runtime diagnostics expose speed percent plus initial/repeat/turn delays, held movement still responds with an immediate first tile, and sustained movement now ramps through the selected repeat interval with a slightly longer delay after direction changes. Proof: focused movement-speed/preferences/runtime/render/reset Vitest packet passed `59` tests, `npm run lint`, `npm run build` producing `assets/main-UvH2FCS9.js`, in-app browser proof showing default `58%` speed as `226ms` initial / `100ms` repeat / `265ms` turn, fast slider as `72ms` repeat, slow slider as `150ms` repeat / `414ms` turn, reload proof preserving slow `0%`, `npm run edge:live -- --skip-build true --headless true --run mobile-touch-smoke`, full `npm run verify` passing `24` files / `163` tests plus build, and `git diff --check`.
- `2026-07-08`: stayed at `100%` after slowing the main-menu maze build into a watchable row-by-row assembly pass. Menu stage-6 draw now reveals one row per cadence tick, the cadence is slowed to `64ms`, menu dynamic trail/player/goal overlays are hidden until their row exists, and menu demo movement is blocked while rows are still building so the maze reads like a clean build-up instead of a full-board flash. Proof: focused generation/render/runtime Vitest packet passed `5` files / `71` tests, `npm run build` producing `assets/main-BIP_0w9x.js`, full `npm run verify` passed `27` files / `175` tests plus build, and in-app browser proof on `assets/main-BIP_0w9x.js` showing `batchSize=1`, `rowsVisible` progressing `0 -> 12 -> 26 -> 49`, `progressPercent` progressing `0 -> 24.5 -> 53.1 -> 100`, `pathCursor` held at `24` until completion then advancing to `28 -> 35`, `recentSpikeCount=0`, plus screenshots at `tmp/menu-build-slow-proof/partial-row-reveal-gated-2.png` and `tmp/menu-build-slow-proof/complete-row-reveal-gated-2.png`.
- `2026-07-08`: stayed at `100%` after changing the main-menu build from row-only reveal to real path-piece assembly. Menu stage-6 now keeps the glass/wall field visible while generated walkable path tiles reveal in an ordered build plan derived from the actual solution spine and nearby carved branches; menu trail/player/goal overlays remain hidden until their tile is revealed, and the menu AI stays frozen until tile build completion. Proof: focused reset/render/runtime Vitest packet passed `3` files / `56` tests, `npm run lint`, `npm run build` producing `assets/main-BIF9k6yJ.js`, and in-app browser proof on `assets/main-BIF9k6yJ.js` showing `tilesVisible` progressing `0 -> 385 -> 836 -> 1005`, `progressPercent` progressing `0 -> 38.3 -> 83.2 -> 100`, `pathCursor` held at `24` until completion then advancing to `33`, `recentSpikeCount=0`, plus screenshots at `tmp/menu-path-build-proof/path-build-early.png`, `tmp/menu-path-build-proof/path-build-mid.png`, `tmp/menu-path-build-proof/path-build-late.png`, and `tmp/menu-path-build-proof/path-build-complete.png`.
- `2026-07-08`: stayed at `100%` after replacing the derived menu path-piece reveal with a trace-backed generation reveal. Legacy generated maze snapshots now carry `generationBuildTrace` data for actual checkpoint targets, path-builder tile order, shortcut bridge placements, reinforcement shortcut placements, source goal, and final goal; the menu build consumes that trace as endpoints first, primary route, branch/split path tiles, shortcut bridges, and final catch-up floors. Proof: focused generation/reset/render/runtime Vitest packet passed `5` files / `71` tests, `npm run lint`, `npm run build` producing `assets/main-DU0B_3sn.js`, and in-app browser proof on `assets/main-DU0B_3sn.js` showing trace counts `checkpointTileCount=56`, `pathTileCount=1034`, `shortcutTileCount=6`, `reinforcementShortcutTileCount=0`, `tilesVisible` progressing `0 -> 420 -> 924 -> 1062`, `progressPercent` progressing `0 -> 39.5 -> 87 -> 100`, `pathCursor` held at `24` until completion then advancing to `35`, `recentSpikeCount=0`, plus screenshots at `tmp/menu-real-trace-build-proof/real-trace-early.png`, `tmp/menu-real-trace-build-proof/real-trace-mid.png`, `tmp/menu-real-trace-build-proof/real-trace-late.png`, and `tmp/menu-real-trace-build-proof/real-trace-complete.png`.

## Marker rule

Ratchet this marker only when a bounded packet changes implemented state or proof-backed confidence in one of the weighted segments.

Do not ratchet for wording changes alone. Do not ratchet the retired legacy visual 1:1 marker from this lane.
