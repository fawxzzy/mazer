# Current Truth

Use this note as the anti-drift override when older prose, screenshots, or implementation layers disagree.

## Active lane

The active lane is now:

`legacy Unreal mechanics -> mobile-clean web game`

This supersedes the prior screenshot-grade visual 1:1 target as the main implementation direction.

2026-08-24 room-subsystem retirement:

- the dormant room-candidate metadata, activation-plan, preview-cue, and preview-edge contracts are retired from source, tests, and runtime diagnostics
- Mazer does not generate, activate, render, or persist rooms, and room eligibility is not a difficulty-band property
- naturally occurring branches, intersections, and open `2x2` floor areas remain ordinary maze topology; this decision does not authorize rewriting the generator to eliminate them
- the dormant static slow-tile and patrol-agent modules, tests, play state, timers, collisions, rendering, and raw runtime diagnostics are retired; fixed world-semantic v1 `patrol` / `pressure` keys remain forced-null compatibility tombstones only
- live play contains no non-player/non-goal gameplay object at any ordinal, bounded difficulty, or maze seed; maze generation, progression, menu AI, player/goal/trail rendering, provider state, and production are unchanged by this source retirement

2026-07-03 operator pivot:

- do not optimize future work for old screenshot-grade visual matching
- preserve and improve legacy gameplay logic, maze generation, AI/demo pathing, reset flow, HUD semantics, and input mechanics
- visual rendering may now change when it improves top-down clarity, mobile readability, board centering, player visibility, trail readability, or maintainability
- legacy screenshots remain useful historical reference, but they no longer define the active completion target

## Source-of-truth order

Read the repo in this order:

1. Restored Unreal legacy truth from `legacy/old-project.zip`
2. Legacy specs under `docs/legacy/`
3. Active mechanics/mobile contract docs:
   - `docs/research/MAZER_MECHANICS_MOBILE_COMPLETION_MARKER.md`
4. Reset-lane contract docs:
   - `docs/research/MAZER_LEGACY_WEB_PORT_CONTRACT.md`
   - `docs/research/MAZER_LEGACY_WEB_PARITY_MATRIX.md`
   - `docs/research/MAZER_LEGACY_ONE_TO_ONE_COMPLETION_MARKER.md`
   - `docs/system-map.md`
5. Legacy screenshots under `legacy/screenshots/`
6. Current web runtime code

If the current web app disagrees with restored gameplay/mechanics truth, restored legacy logic wins for this lane. If the current visual direction disagrees with old screenshot composition, the active mobile-clean direction wins.

### Supabase master cutover truth — 2026-08-27

- Production now uses master project `bxtcuhkotumitoqtrcej`, schema `mazer`; the deployed bundle contains that Supabase URL as its only active Supabase endpoint.
- Current master readback is `13` profiles, `17` player rows, `17` AI rows, and `1,888` conserved receipts.
- The legacy project `geknvnrmktchljnyddwp` is retained only as the exact rollback source. Do not mutate or retire it without a separate destructive decision.
- R017 completed identity mapping, monotonic reconciliation, final-delta capture, Data API/Auth proof, production cutover, observation, and exact legacy restoration. Those gates are durable evidence and must not be replayed for ordinary source corrections.
- The browser client already selects `public` for the exact legacy URL and `mazer` for the exact master URL, failing closed for unknown projects.
- Authenticated own-row progression reads are authoritative for coherent historical player ordinals even when they predate the browser-only provenance sentinel. The client stamps that sentinel before caching the row locally; unproven local saves without it still fail closed to Level 1, impossible remote ordinals still fail closed, and obsolete remote difficulty above today's earned ceiling is clamped without erasing the server-proven level/cycle position.
- The generated master migration sequence is schema parity, runtime RPC/leaderboard contracts, then shared-project signup username authority. The Before User Created hook is activated only after those database contracts and rollback proof pass.
- Existing sessions are project-specific. Any future migration must still prove existing-user re-authentication and must never lower proven player or AI ordinals, discard receipts, expose usernames through anonymous availability probing, or derive a username from email.

## Current implementation truth

### Final app-icon authority — 2026-08-28

- `src/brand/source-art/mazer-app-icon-master.png` is the sole Mazer icon design authority: 1024x1024, 1,173,366 bytes, SHA-256 `ede90e596682795d10f97bed615071ca1f60e08e290eacf1ea143006df914d78`.
- `npm run brand:icons` deterministically fans that master out only to the browser, Apple touch, PWA standard/maskable, Workbox, and Windows-shortcut assets actually consumed by the repository.
- The maskable assets are intentionally padded to keep the complete rainbow frame and central maze inside the guaranteed safe circle. Older ATLAS icon references and `mazer-emblem` files are superseded, non-authoritative evidence.
- This is a source contract. It does not by itself establish that production has received the icon.

### Current Precision Arcade visual integration

The live Phaser renderer now consumes the canonical Fawxzzy Precision Arcade token map. The first shipping visual slice keeps the maze as the hero: a quiet navy canvas, crisp ivory connected corridors, mint/aqua structural rails, one-surface panels, a topology-rendered menu title, one compact color-tiered level glyph per active surface, and one shared cog-button shell for menu Settings and play controls. Header level and Settings controls share the same responsive 44–48px square frame; the numeric metric scales inside that fixed footprint rather than widening the layout. Active play binds its glyph to the player progression track. The main menu instead exposes only the independent menu-AI level, so the animated demo cannot be mistaken for the signed-in player's current difficulty. The Settings cog has a bounded 140ms hover turn and the level glyph has a bounded 420ms progression pulse; operating-system reduced motion suppresses both without removing the controls. A static ambient shard/rune/star layer remains present behind the maze, while the Animated Background preference and operating-system reduced-motion preference control motion. The full-screen rounded perimeter frame is intentionally absent. This is render-only; gameplay topology, progression, inputs, collision, scoring, persistence, tutorial, and menu AI remain unchanged.

The shared Phaser material also owns the live UI font stacks, control/panel/sheet radii, press/panel motion values, and the 44px normal-phone touch floor. Main-menu actions, pause actions, and overlay navigation consume that same contract; only the constrained 172px diagnostic side panel retains a 42px fallback so its two actions and board cannot overlap.

The shared DOM shell now also gives the canvas an explicit accessible name, keyboard-shortcut contract, and visible focus treatment. A focus-only semantic command rail exposes the existing Enter, O, P, and Escape commands without adding a second gameplay input path or crowding the player-facing board. Phaser remains the owner of the maze and input behavior.

- The current web runtime has been reset to a smaller legacy-first Phaser shell.
- The old productized menu shell is no longer the active source of truth.
- The current front-door contract is mobile-clean and play-focused:
  - `Start`
  - `Options`
- The current runtime preserves the one-overlay-at-a-time rule.
- The current overlay family (`LegacyOverlayKind`, `src/legacy-runtime/legacyOverlayRouting.ts`) is the top-level runtime overlay set:
  - options
  - pause
  - auth (also reached as `Login/Account`)
  - confirm-progression-reset
  - leaderboard
  - **Correction (2026-08-30):** this bullet previously read `options / features / game modes / pause`. That's stale — there is no live `'features'` or `'game modes'` top-level overlay kind in current source, and a repo-wide search of `MenuScene.ts` found no nested Features/Game Modes panel under those names either. Feature toggles and game-mode-style settings exist today as ordinary rows inside the single `options`/`pause` overlay content (`createFeatureControlRows`), not as their own overlay or sub-panel. The `auth`, `confirm-progression-reset`, and `leaderboard` overlay kinds were missing from the old bullet entirely.
- Options and Pause currently do **not** render a Move Speed slider. Every `buildOptionsOverlay()` and `buildPauseOverlay()` call site passes `includeMovementSpeed: false`; no call site in `MenuScene.ts` passes `true`. (Whether to reintroduce it is a separate future product/UI decision, not current runtime behavior.)
- The current menu shell is intentionally simpler and closer to the archived Unreal menu composition than the previous recovery shell.

## Current completion marker

Use `docs/research/MAZER_MECHANICS_MOBILE_COMPLETION_MARKER.md` as the active repo-wide completion meter.

Current active marker:

- `100%`

The old legacy visual 1:1 marker is retired at `93%` and is now archival. Do not ratchet it for future packets unless the operator explicitly reopens screenshot-grade legacy parity as the active target.

Why the active marker is now complete for the current scope:

- the project already has strong mechanics coverage for legacy reset flow, generation lifecycle, AI demo routing, active input, collision, HUD semantics, and diagnostics
- the new target gives much less credit for visual screenshot mimicry and more credit for mobile playability/readability
- the current marker scope is covered by cleaner mobile board sizing, player/trail readability, maze-generation quality tuning, and broader mechanics proof
- active-play movement/collision/reset-return semantics are closer, keyboard movement now consumes accepted key events so browser defaults cannot compete with the game loop, browser focus loss now clears held movement flags/pending movement timers/pointer starts before stale input can replay, mobile pointer/touch play input now resolves through the same legacy play-step movement/collision contract as keyboard input, rejects pointer starts outside the active board bounds, binds an in-flight swipe to one pointer identity, ignores competing touches, accepts `pointerupoutside` for inside-board starts, clears stale pointer starts on game-out, and now has maintained-browser `mobile-touch-smoke` proof for three touch movement deltas with board/HUD bounds passing; shared mobile touch controls now route through the Phaser touch pointer path, non-touch pointer fallback ignores touch duplicates, pause/restart/toggle coordinates are separated from D-pad hit slop, runtime summaries expose overlay/resource state, and the maintained smoke proves pause, resume, toggle-thoughts, restart, and post-restart movement state transitions; compact play mode now renders flat visible touch-control affordances from the same shared touch layout resolver, publishes separate touch-control diagnostics, and keeps legacy HUD bounds isolated from the bottom control frame; keyboard play proof now starts from the supported `mode=play` route, focuses the canvas, holds movement keys through the restored 50ms simultaneous-key buffer, reads fresh visual-runtime player state, and covers pause, restart, and trail/thought toggle through the same play command path as touch; generated play maze solution paths now walk from start to goal through the active `advanceLegacyPlayStep()` movement/collision contract, pause reset now preserves existing active-play trail history while returning the player to start instead of wiping the trail to `[start]`, camera-follow now keeps the static maze layer aligned with dynamic overlays/HUD/pointer bounds, and runtime diagnostics now expose the live active-play input buffer state for held directions, resolved vector, pending timer, pointer-start state, and the 50ms source delay; menu-demo reset semantics are closer, demo route candidate admission now follows the restored `AiTilePathCheck` gate, and the HUD timer/goal-arrow contract is now extracted/tested/published through visual diagnostics with source-exact bare timer text and degree readback; active-play HUD visual chrome now avoids the prior heavy bordered browser chip, runtime diagnostics are data-only so no debug/proof panel is drawn over the game, and visual diagnostics now mirror to a data-only DOM attribute for maintained-browser proof, but final menu visual parity still needs proof
- demo AI route/backtrack candidate gating is now aligned for the known dead-end spur seam, the menu walker route plan now derives from a source-shaped neighbor scan / potential-tile / path-stack backtracking planner instead of canonical-solution detour injection, wrong-branch recovery now reconnects to canonical replay through adjacent floor movement instead of a non-adjacent route splice, first-mistake route planning now stops once dead-end/backtrack/reacquire cue evidence has been emitted before replaying cleanly, cue movement now uses one AI step timer to match the extracted C++ `_PlayerAiDelayDuration` scheduling shape, route diagnostics expose `visitedUndoCount`, a deterministic proof fixture now exercises the legacy `_AiBackTrackUndoVisitedFlag` side-effect seam, menu demo bootstrap/advance tests now prove the browser-equivalent trail fade tail used for the old material-revert lane, generated legacy menu maze seed-family proof now verifies adjacent AI moves plus bounded route/traverse diagnostics on actual `menu-generated` mazes, generated-menu scale-band proof now verifies wrong-branch recovery into AI-only reset, same-maze replay, later goal reset, and goal regeneration request without non-adjacent moves or runaway timing, and a bounded `GI_MazerGameInstance.uasset` scan found property names but no trustworthy serialized `_TileColorRevertDelay` default; exact Unreal `_TileColorRevertDelay` timing/material behavior and the numeric Blueprint AI delay default remain future tightening work
- live generated-menu AI now binds to the independent `ai-runner` progression track's bounded difficulty level and rank instead of using one rank-agnostic walker. Its navigation truth is the actual wrap-aware playable shortest path, not the generator's potentially longer construction trail: E keeps one deterministic human-error bucket with shortest-path assistance in four of five buckets, while D through S use the playable shortest runner plan directly. Wrong-branch telemetry now means a move failed to reduce actual goal distance. This changes only the menu AI demonstration; it does not read or write player progression, alter player input or active-play difficulty, activate rooms or pressure objects, or touch provider state.
- maze generation process/stage ownership is mapped, browser shortcut topology now uses family-aware route-affecting bypasses with separated route-reconnection proof instead of random dead-end wall punches, generated domain play-maze rasters apply the restored `CreateShortCuts` opposite-corridor wall-bridge rule, and both live menu mazes plus active reset-lane play mazes now route through the procedural checkpoint path-builder family instead of leaving the main menu on the fixed screenshot snapshot. `createLegacyGeneratedMenuMaze()` uses the same source-shaped `CreateGrid` / `MapPath` / `CreatePath` / `_WallArray` / `CreateShortCuts` pipeline as `createLegacyMaze()`, but with menu-specific shortcut tuning, a shortcut-stage floor of `6` once process `5` is active, and row-sliced menu drawing; `createLegacyMenuMaze()` remains only as a fixed screenshot comparison fixture. `Backtrack()` resumes from the selected next tile like the restored source instead of the already-carved path candidate; generated menu/play mazes also run a browser-safe playable-topology normalization that removes disconnected floor components and rebases trivially weak goals to the farthest reachable floor so the restored shortcut/path-builder work cannot leave unreachable visual noise or a degenerate route; generated default play/menu mazes now add a capped post-normalization shortcut reinforcement only when measured route quality is still `single-route`, score candidate shortcut bridges by measured route-quality improvement across route bands, and rebase post-shortcut-shortened goals to the farthest reachable floor when needed, so weak shortcut outcomes can become meaningful multi-route mazes without reintroducing detached floor components or degenerate routes; generated snapshots publish route-quality stats that separate any bypassable solution edge from meaningful detour-bearing bypass coverage across route bands, so `multi-route` can no longer be claimed from one shallow local loop; current seed/scale proof covers `64` sequential default play seeds, `64` sequential generated-menu seeds, the established regression seeds, shortcut-enabled play/menu scale bands `37`, `50`, and `75`, bounded large-scale `99` smoke, and one bounded extreme scale-`149` smoke with no detached floors, minimum route length, and meaningful multi-route quality. Generated menu/play snapshots now also publish the versioned `legacy-wrap-topology-v1` contract: paired non-corner endpoints, inward connectivity, required-axis satisfaction, renderer-owned decorative cutout candidates, explicit `direct-floor` versus `playable-wrap-aware` shortest-path truth, and a completed-route lower-bound audit; bounded runtime diagnostics carry only counts and audit results, while phone/desktop route-aware proof verifies the exact rendered generated surfaces. Exact Unreal RNG/time seeding and line-for-line process-yield timing remain future parity work outside the current mobile-clean mechanics marker
- HUD parity is now closed for the currently restored browser-equivalent source seam: active-play timer formatting uses the restored bare `M:SS` text plus `% 10` minute wrap, goal-arrow angle math publishes radians/degrees, proof bounds have a repo-owned helper and diagnostics readback, HUD chrome now stays in a minimal Timer/EndArrow widget lane, and runtime diagnostics stay data-only instead of drawing a visible proof panel; if a stronger Unreal widget blueprint/material source is recovered later, that evidence can reopen a narrower visual-style packet
- active-play maze rendering no longer uses the prior square-cell debug-style path fill; play mode now uses connected corridor segments with a flatter edge/core hierarchy, active-play dynamic trail/start/goal overlays use corridor/inset rendering instead of generic square `fillTile()` blocks, and the player now renders as a centered high-contrast halo/core marker so it reads above the route without changing topology, movement, or shortcuts. Dynamic trail width, start/goal marker inset, and player halo/core metrics now resolve through tile-size-aware helpers so ultra-narrow mobile tiles keep readable trail/start/goal/player marks while larger desktop tiles retain visible weight.
- menu and play static board rendering now intentionally drops the heavier pseudo-3D pass: no offset path relief shadow, no extruded menu slab shadow stack, no visible menu grid overlay, no top-left bevel sheen, and no wall-cell bevel texture. The maze still uses connected corridor segments, but the visual lane is cleaner 2D readability rather than a trench-depth look.
- login-first entry waits on the account screen after auth resolution. The forced auth surface is a static product screen: gameplay and ambient board simulation remain halted, there is no guest bypass, and account creation requires one 2-15 character ASCII username as both the public username and auth display identity. The dependency-ordered signup contract assigns canonical `app_namespace=mazer` metadata, server hook/trigger SQL, and categorical error mapping to Auth Wave 3B; Menu Wave 3A owns retiring the signed-out availability request and post-session profile save. `app_namespace` is user-supplied routing metadata, never authorization: the shared Before User Created hook passes absent, Fitness, Website, and other non-Mazer signups through unchanged, then validates username metadata only for explicit Mazer events. The final combined contract never derives username from email, creates the profile/username in the same `auth.users` transaction, retains the lowercased partial unique index as final race authority, and routes later account renames through the authenticated `mazer_set_username` definer. That RPC binds `auth.uid()` to the expected user, changes only the caller's username, preserves direct profile-write revocation, leaves the case-insensitive unique index as final collision authority, and lets the username-origin trigger switch an actual rename to `claimed`.
- mobile maze crispness is handled at both the geometry and backing-store layers: canvas CSS no longer forces whole-surface pixelated/crisp-edge sampling, board wall/path/cue rectangles snap to integer canvas-pixel boundaries, and the Canvas renderer now keeps logical CSS-pixel layout while applying a capped DPR-aware backing store. Visual diagnostics expose target ratio, deficit, and `undersampledForDevicePixelRatio`. The shared viewport controller also publishes its authoritative parent dimensions directly to Phaser `RESIZE` mode, preventing a late stale maximize/restore measurement from leaving pointer coordinates in the previous viewport while the shell is visibly restored.
- the folded-corner sigil border now treats the small triangular corner gaps as translucent jewel facets instead of black voids. The corner material is a bounded dynamic-layer shimmer using mint/cyan iridescent fills and diagonal glints; it preserves the flat top-down board and does not repaint the static maze field every frame.
- active-play now implements the frozen Navigation Core v1 visual authority (`docs/assets/reference/navigation-core-v1/`, Wave 4D-A) for floor, trail, player, and goal. **Implemented and real-browser-verified as of head `19b71fce`:** (1) the corridor floor is one connected material -- the old 1px-overlap seam hack and the floor-tile texture's own duplicated border are both gone, and the boot-time interior-crop that derives the floor accent texture (`src/scenes/BootScene.ts`) is tuned to a 400x400 crop (down from an initial 730x730 cut) after a live-renderer `getImageData` scanline check found the larger crop still read as a per-tile "breathe" (194-246 brightness swing) versus the procedural fill alone (175-176, effectively flat) -- 400x400 measures 206-249, and shrinking further (tested to 100x100) doesn't reduce it more, so this is treated as the practical floor for this asset, not a still-open defect; (2) the real play-mode player marker's two drop-shadow glow layers are a genuine `shadowBlur` compositor (`src/render/navigationCorePlayerGlowCanvas.ts`), replacing two flat `fillRoundedRect` "shells" that read as nested solid bodies instead of light; (3) the goal star's ambient halo is a genuine `createRadialGradient` compositor (`src/render/navigationCoreGoalHaloCanvas.ts`), replacing two flat `fillCircle` discs that read as sharply bounded "target" rings -- the hollow star and ring were already correct and are unchanged; (4) the play trail is one continuous, corner-rounded, arc-length-colored path composited on a real 2D canvas (`src/render/navigationCoreTrailCanvas.ts`), backing-resolution-aware for DPR/board-zoom, with a corrected `shadowBlur * resolution` scaling (Canvas 2D `shadowBlur` is a raw device-pixel radius the context transform does NOT scale -- verified empirically by holding a shape's device footprint constant across transform scales 1-4 and observing an identical device-pixel halo width each time; the player-glow and goal-halo compositors reuse this same corrected pattern, and the goal halo's gradient geometry needs no such correction since gradient stops are ordinary logical-space coordinates, not a device-pixel effect radius). The menu's own ambient demo trail/start/goal markers are unchanged and out of scope for this reference; the corridor floor material IS shared with the menu (confirmed unregressed: a full settled-menu capture at this head still shows the intact title/START construction sequence and a clean connected corridor). **Real-command-driven evidence:** `scripts/analysis/verify-trail-canvas-lifecycle.mjs` drives the actual built game (real `window.__MAZER_QA__.movePlayPlayer` calls, not direct field assignment) through Play, Play->Menu, a real reset, a real next-maze generation, Pause/resume, reduced motion, and a viewport resize, asserting all three canvas layers (trail/player-glow/goal-halo) never leave a stale ghost image and never go missing -- 25/25 checks pass (this check's own runtime path is unchanged since head `19b71fce`). `scripts/analysis/capture-wave4d-a-evidence.mjs` records two distinct video clips that must not be conflated: `mazer-wave4d-a-trail-shine-demo.webm` installs the player/trail directly and stops the game loop to hold the frame still for the shine sweep -- a controlled shine demonstration, not movement evidence -- while `mazer-wave4d-a-real-player-movement.webm` (added at head `16ffdf46`, after a reviewer found the former was being described as movement evidence it never was) drives the real `movePlayPlayer` command for several ordinary moves plus one turn with the game loop never stopped, each glide actually visible across real recorded time, continuing into a subsequent shine traversal; synthetic `loop.step()` calls between real move commands force visible repaints for the video recorder, but every move itself is the real accepted command. The frame-paired `mazer-wave4d-a-real-player-motion-frames.png` contact sheet is separate, deterministic evidence of the same real-movement claim (intermediate sub-tile positions, trail growth, no whole-cell arrival pop, the turn itself) -- the two capture methods corroborate each other but do not substitute for one another. `mazer-wave4d-a-title-construction.png` (added at head `1791c3ff`) similarly replaces a bare assertion with real evidence: a single settled Main Menu screenshot cannot prove the board/title is actually built tile-by-tile, so this drives the real reveal a fresh page load triggers (`armLegacyMenuStaticDrawStage`'s 'building' phase), reading the scene's own tile-count fields (not a reimplementation) to capture five genuinely distinct frames from 0% through 100%-settled. `scripts/analysis/verify-trail-corner-raster.mjs` is a bounded real-pixel regression (not a mock-context assertion) for the trail's corner-rendering fix: all four corner orientations, frozen animation phase, compared against a committed baseline PNG under two checks -- a per-channel tolerance (24/255, up to 2% of pixels) AND, as of head `16ffdf46`, a largest-connected-differing-region cap (40px, calibrated against 20-36px observed across repeated real re-renders). The region cap closes a reviewer-demonstrated false-pass: the fraction check alone let a 60x60 opaque-black corruption over the trail bend through (1.61% of the image, under the 2% budget) because the fraction was diluted by unaffected background; `tests/analysis/rasterCompare.test.ts` reproduces that exact corruption against the real committed baseline and asserts the real comparator rejects it under its real shipped tolerance, not just at tolerance zero. Both checks are wired into CI (`.github/workflows/verify.yml`, which installs Chromium for this one bounded step). Real frame-time evidence (`scripts/analysis/measure-trail-performance.mjs`) shows full-frame `requestAnimationFrame` p95 delta under 0.1ms versus an empty-trail baseline across desktop/mobile-390x844-DPR3/compact/fractional tile scales and Trail Fade/Shine on/off; the two demanding configs (desktop-normal, mobile-390x844-dpr3) sample for at least 10 seconds AND at least one full shine cycle (whichever is longer, computed from the real production shine-speed/quiet-gap-ratio constants) as of head `16ffdf46` -- desktop-normal sampled 25.5s (1,531 frame intervals, zero over the 33.33ms budget), mobile-dpr3 sampled 19.9s (1,194 intervals, `maxWithTrail`=33.30ms, reported as exactly that near-miss rather than as proof of perfect delivery); the remaining four non-demanding configs still sample 800ms (~48 intervals), which is sufficient for their narrower comparative purpose. Do not read any of this as trail-only cost; `trailCpuCost` in that report is the trail's own geometry-build/draw microbenchmark, while `fullFrame` reflects the real combined scene (floor + trail + player-glow + goal-halo together). This performance/raster/video tooling work (heads `9caf9797`-`16ffdf46`) fixes how the evidence is measured and captured; it does not change any runtime rendering code -- the compiled game bundle at head `16ffdf46` is behaviorally identical to head `30e0e219`, so floor/player/goal/trail visual evidence captured against either head remains valid. **Fixture-only evidence** (direct `scene.trail`/`scene.player`/`scene.maze.goal` assignment, not real movement commands) backs the specific equal-scale reference comparisons for floor (three tile scales: ~65px, ~26px, ~34px fractional) and the isolated player-only/goal-only marker crops. **Remaining limitation:** the Options/Guide legend's own miniature static goal icon (a separate, non-gameplay caller) intentionally keeps the prior flat-disc halo approximation -- out of scope for this reference, a known follow-on, not a defect in the reviewed marker. **Merged:** PR #345 squash-merged to `main` at commit `daf92fd4cf404c9471b55030416d366af201399f` (a review recorded on the exact prior head, `e817f182842d`, found no remaining blocking findings within this closeout's scope). Navigation Core v1 is the accepted, merged implementation baseline; further changes here need a demonstrated regression or new owner direction, not another correction pass.
- **Teleport System v1** (`docs/assets/reference/teleport-system-v1/`, Wave 4D-B): implementation ownership per that package's own README. **Primary-anchor policy, real pose/clearance from live layout (including real HUD-control exclusions), correct per-identity slot rendering, and a bounded QA preview session all verified live. As of PR #348 (branch `claude/mazer-wave4d-b-transfer-renderer`, open, unmerged), the real single-primary gameplay conduit is also wired into actual goal-to-delivery Active Play -- see the dedicated paragraph below for its current state, verification, and disclosed limitation.** `src/render/teleportPrimaryAnchor.ts` (PR #346, squash-merged at `bf4e4bec92392efa9ad1e6cef9216befc634385b`) is the pure selection policy: nearest eligible anchor by inward beam-port distance -- not sprite/canvas center -- with eligibility supplied by the caller, fixed anchor-ID tie-break, retained across outbound/stored/delivering regardless of a closer candidate or a changed target, remapped only when the held anchor becomes ineligible or disappears. `src/render/teleportAnchorPose.ts` resolves the REAL candidate data that policy needs from real layout: anchor position reproduces `MenuScene.ts`'s own existing perimeter-anchor formula (cross-checked in tests against the real exported `resolveLegacyMenuPathTitleOrbitPose`), rotation/port use the frozen contract's own precisely MEASURED shell-tip angle (-59.43 degrees, independently re-derived from the derivative manifest) rather than `MenuScene`'s admittedly-approximate -45-degree constant used for its unrelated ambient title-orbit sigils, and clearance is a rotation-aware conservative bounding box (a fixed unrotated box was a real defect a review caught -- it under-counted a rotated shell's true footprint by up to ~41% at a 45-degree facing angle; the fix bounds the actual rotated source-canvas corners at every angle, proven in tests against the transformed corners themselves, not just the function's own self-consistency) checked against real exclusion rectangles AND real viewport containment.

  A reviewed correction round fixed three further defects the same review caught by inspecting the actual code and a supplied screenshot, not just running the existing tests (all now covered by permanent tests, pure and real-browser): (1) HUD clearance previously used only the four device safe-area strips -- `resolveLegacyTeleportAnchorPreviewExclusions` now also unions in this scene's own live `uiButtons` bounds (Account, Settings, Leaderboard, Start, and any open overlay's buttons -- whichever are actually active this frame, never a copied/representative coordinate) and, in menu mode, the real title region (`resolveLegacyMenuPathTitleLayout`); play mode's touch movement control has no discrete rectangular hit-zone in this codebase to extract (a whole-surface drag gesture, not a fixed-bounds button) and is a disclosed, real, not-yet-included limitation, not a claimed completion. (2) The preview override previously always repositioned a fixed spare pool slot (`titleOrbitDiamondImages[7]`) regardless of which anchor the selector actually chose -- confirmed live as a real bug (selecting anchor 0 left slot 7 impersonating it while slot 0 stayed under unrelated ambient control) -- fixed to operate on `titleOrbitDiamondImages[result.selectedId]` directly, verified live that the other seven identities remain under normal ambient control with no duplicate representative. (3) The computed pose (real layout/viewport space) was being assigned directly as the Image's LOCAL position/rotation with no explicit conversion into `boardZoomContainer`'s own space -- this codebase's "Board Zoom" setting was confirmed live to not currently move that container (it changes tile size instead), so the bug was latent, not yet visibly triggered, but relying on that rather than converting explicitly was exactly the fragility the review flagged. Fixed via Phaser's own `Container.pointToContainer` for position (verified live under a real non-identity offset+uniform-scale transform, comparing the Image's actual WORLD transform, not its local x/y, against the resolver's real output) and an explicit rotation conversion (correct under identity rotation plus any UNIFORM parent scale; non-uniform/anisotropic parent scale is a documented non-goal, since a rotated child's world silhouette becomes sheared into an ellipse under such a transform and "the correct world rotation" stops having one answer -- `boardZoomContainer`'s own name/purpose is a uniform zoom).

  The preview is now a bounded session, not a stateless probe: `window.__MAZER_QA__.previewTeleportPrimaryAnchor(targetX, targetY, newSession?)` retains the previously-selected anchor across calls (the real selector's own retention rule -- a closer candidate appearing does not reselect) unless `newSession: true` requests a genuinely fresh selection; verified live that a session survives a target change and that `newSession: true` starts over. `endTeleportPrimaryAnchorPreview()` clears the session; the existing unconditional ambient draw then reasserts control of every slot on its own the next frame, with no explicit restore step needed. This does not touch or pause the existing player-transfer beam rendering (`src/legacy-runtime/legacyPlayerTransfer.ts` and `drawLegacyPlayerTransferEnergy` remain exactly as before) and does not implement the conduit/spawn/extraction choreography itself. `src/scenes/MenuScene.ts` remains handed off from Wave 4D-A to Wave 4D-B (recorded in the decision registry, `REQUIRED_INTEGRATOR_WAVE_BY_PATH` updated to match) for exactly this QA surface.

  A second reviewed correction round fixed three further defects the same reviewer caught by reading the corrected code's own math and gating logic, not by re-running the existing (insufficiently strengthened) tests: (1) **the port/footprint were still wrong under a non-identity parent transform even though the center was now correct** -- `applyLegacyTeleportAnchorPreviewOverride` converted position and rotation into `boardZoomContainer`'s local space but still set the Image's scale to a fixed `worldScale` (`TELEPORT_ANCHOR_PREVIEW_SHELL_CANVAS_SIDE_PX / MAZER_VFX_DIAMOND_SOURCE_SIZE`) without dividing by the container's own scale, so under a non-identity parent scale the rendered shell's real WORLD size scaled WITH the container instead of staying constant -- confirmed live (`world.scaleX` drifting away from the intended constant under a 1.6x parent) and by the reviewer's own worked calculation (a 32-unit shell becoming 51.2 world units, an ~8.8-unit port error); fixed via `const localScale = worldScale / this.boardZoomContainer.scaleX;`, verified live that `world.scaleX` now holds the correct constant regardless of parent scale and that `world.transformPoint()` (transforming the shell's own measured tip through its real world matrix) matches the resolver's `portX/portY` at identity, a 1.6x, and a below-1x parent scale. (2) **preview cleanup depended on unrelated ambient animation, not an explicit invalidation** -- begin/update/end only mutated `teleportAnchorPreviewSession` state and relied on the per-frame ambient draw to pick up the change, but that draw path is dirty-gated and explicitly disables itself under reduced motion (`hasLegacyMenuTitleAnimationPendingFrame` returns `false` when `prefersLegacyReducedMotion()` is true, and the dirty flag is only ever force-set when reduced motion is OFF) -- a settled, reduced-motion scene therefore had no guaranteed next frame to apply or restore the preview at all; fixed via a new `applyLegacyTeleportAnchorPreviewPresentationUpdate()` that calls `drawLegacyMenuPathTitle(this.time.now)` directly and synchronously from begin/update and from end, verified live with `prefersLegacyReducedMotion` monkey-patched true and zero manual frame steps that both application and restoration happen immediately. (3) **the mobile-control geometry the exclusions never consumed already existed in this codebase and was real, not absent** -- `resolveLegacyPlayTouchControlLayout()` (the fixed pause-button hit region) and `resolveLegacyPlayFloatingStickGeometry(origin)` (an active floating stick's real outer/inner rects) were live, correct functions the preview simply never called; fixed by extending `resolveLegacyTeleportAnchorPreviewExclusions()` to add the pause button's bounds whenever touch controls should render, and the active stick's outer bounds only while `playFloatingStickOrigin !== null` (a transient exclusion, not a permanent one) -- verified live that a target on the real pause button is excluded, that mounting a stick near an anchor excludes exactly that anchor while it is active and stops excluding it once released, and that the much larger whole-canvas gesture-capture surface itself is never treated as an exclusion.

  **The gameplay conduit (PR #348, still open/unmerged as of this writing).** `src/render/teleportTransferPresentation.ts` is a stateless presentation projection layered directly on the existing single authoritative lifecycle (`src/legacy-runtime/legacyPlayerTransfer.ts`, unchanged, not owned by this wave) rather than a second gameplay state machine: it derives conduit open/closed fraction, packet position/visibility, and source/target flare intensity for both extraction (goal toward the held primary's measured port) and delivery (primary toward the real next-start tile) directions from the held primary's pose plus the lifecycle's own raw elapsed-ms inputs, including a genuine reduced-motion crossfade (not a hard on/off step) at both phase boundaries. `src/render/teleportTransferConduitCanvas.ts` is the Canvas-2D compositor: a continuous stroke built from fixed-size absolute-distance chunks (not equal-division chunking, which redistributed every chunk boundary as the conduit grew, producing visible material churn -- confirmed and fixed), reusing `navigationCoreTrailCanvas.ts`'s own chunked glow/core stroker and sampling `navigationCoreTrail.ts`'s real `sampleTrailEnergyColor`/`NAVIGATION_CORE_TRAIL_ENERGY_STOPS` (the same energy palette the play trail itself uses, not the unrelated `legacyIridescentMaterial.ts` "midnight" palette that module's own doc explicitly warns against for this contract) at each chunk's real distance along the conduit, so color reads as one continuous material rather than independently-colored parts. `MenuScene.ts`'s `applyLegacyGameplayTransferPresentation` wires this into real Active Play: it reuses `resolveLegacyTeleportAnchorPreviewResult` (the same real selector/pose resolver the QA preview above already exercises) for the real gameplay session, keeps one sticky held primary across the whole outbound/stored/delivering cycle, positions the SAME `titleOrbitDiamondImages[id]` slot the QA preview uses (never the old fixed eight-origin `playerTransferBeamStripImages`, which stay unused for gameplay transfers and only remain for menu/title choreography), and renders the conduit as a `boardZoomContainer` child inserted before `titleOrbitDiamondImages` in the scene's own child-add order so it never draws on top of the shell it should be masked behind (a real Phaser display-list ordering bug, confirmed and fixed: a container is one atomic entry in its parent's own list and cannot be interleaved with a top-level sibling via depth alone). Four real defects a review found by reading the corrected code's own arithmetic, not by re-running the existing tests, were fixed in earlier rounds on this same branch: an extraction-direction endpoint-continuity bug (the illuminated segment was derived backwards for extraction, causing the beam to shrink to near-zero then jump to full length at the phase boundary -- fixed via explicit `conduitStartPoint`/`conduitEndPoint` fields), the wrong-palette bug above, the z-order/masking bug above, and a sticky-identity bug (the held primary id was being cleared to `null` on any transient 'unavailable' selector result instead of only on a genuine reset). A further round fixed a target-flare containment bug (the flare radius could exceed half the real cell size on a small board, visibly bleeding past the destination tile) and a material-clock staleness bug (the animation clock used to be advanced only from inside the conduit's own closed-state-gated draw call, so a settled 'stored' interval left it stale and the next reopen applied the entire elapsed gap as one visible color jump -- fixed by advancing it unconditionally, every frame, from the caller).
  
  This round added the permanent, committed, CI-wired real-browser test this wave's own coverage was missing (`scripts/analysis/verify-gameplay-teleport-transfer.mjs`, wired into `.github/workflows/verify.yml` alongside the existing QA-preview lifecycle check) and, in writing it, found and fixed two further real defects neither prior round's evidence exercised: (1) **pausing mid-transfer silently reselected the held primary to a different anchor** -- `applyLegacyGameplayTransferPresentation` re-resolved anchor selection every frame regardless of overlay state, and opening the pause overlay populates `this.uiButtons` with that overlay's own (large) buttons, which the real exclusion computation folds in -- if one of those buttons covers the held anchor's footprint, the selector legitimately picks a different one while the player is looking at a paused, frozen screen; confirmed live (`beforePause`/`duringPause` primary ids differing) and fixed by skipping selection entirely while `this.overlay !== 'none'` and redrawing from a newly-added cached `gameplayTransferLastPrimaryPose` instead, mirroring the same freeze rule the material clock already uses. (2) **that fix alone still reselected on the exact frame resume completed** -- `this.overlay` flips back to `'none'` synchronously the instant a `RESUME_RUN` command is dispatched, one full frame before `this.uiButtons` is rebuilt free of the pause menu's own rects (the scene's render pipeline always rebuilds `uiButtons` strictly after the dynamic-board draw that calls this method, within the same frame), so gating on `overlay === 'none'` alone still read the stale, pause-era buttons on that one transitional frame; confirmed live via direct `uiButtons`/`overlay` inspection immediately after dispatch versus after one stepped frame, and fixed by also requiring the PREVIOUS frame's observed overlay (`gameplayTransferLastObservedOverlay`, a new field) to have been `'none'`, giving the rebuild exactly one extra frame to catch up. The new test's two headline cases are a controlled near-goal fixture (fast boundary coverage: held-primary persistence, real conduit visibility/masking, phase progression through stored and delivering, real re-armed input after completion) and a FULL real-BFS-solver-driven run from the maze's actual start to its actual goal through nothing but real accepted `movePlayPlayer` commands (no direct player/goal/phase/primary/elapsed-time assignment anywhere in that case) -- both reach real completion, which is itself only externally observable as a transition straight from `'delivering'` to `'idle'` (`settleLegacyPlayerTransferEnergy` resets the armed lifecycle synchronously, within the same `update()` tick, the instant it observes the resolver's own `'complete'` phase, so that phase is never actually visible to an external poll at any granularity -- a real, confirmed structural fact about this lifecycle, not a test artifact). Supplementary cases cover reduced motion, pause/resume (the two fixes above), a compact/touch-emulated layout, and a forced non-identity `boardZoomContainer` transform (this container is, as of this writing, always identity-scale through every real UI path in this codebase -- no board-zoom command exists in `uiCommands.ts` -- so this last case is a controlled fixture on a rendering-config field, not gameplay state). 41/41 checks pass. **Remaining limitation (as of that round):** the conduit/shell mask each other by real draw order and container placement, not an internal clip -- confirmed correct at the transform scales this test suite exercises (identity and a forced 1.6x), but a per-pixel opacity assertion of the masked region itself is not part of this suite.

  **A fourth round (ChatGPT-assisted review 5144999445, changes requested) found a real, blocking gap the 41/41-passing suite above had not covered: the OLD eight-origin volley was still rendering in real Active Play.** `playerTransferBeamStripImages` staying hidden (the check every earlier round relied on) does not prove the old volley never ran -- `drawLegacyPlayerSpawnBurst` is a completely SEPARATE old effect, drawing one procedural beam per orbit sigil directly into `playerSpawnBurstGraphics` with no mode branch of its own, armed by `armLegacyPlayerArrivalForFinalBuildStep` on every settled maze build (not only when a teleport transfer was armed) -- so it fired during every real transfer's own delivery AND on the very first maze of a run, with no preceding goal completion at all. Confirmed independently by reading `drawDynamicBoard`'s own call site (no mode check) before fixing. Fixed with two changes: (1) Play mode never calls `drawLegacyPlayerSpawnBurst` again; a real armed transfer's own delivering phase is already fully rendered by `applyLegacyGameplayTransferPresentation`, so nothing else is needed there. (2) A new `applyLegacyGameplayInitialArrivalPresentation` covers every OTHER Play-mode arrival (chiefly the very first maze of a run) by reusing the exact same single-primary conduit/shell renderer, driven by `playerSpawnBurst`'s own elapsed/travel/flash clock (the identical `LEGACY_PLAYER_SPAWN_BEAM_TRAVEL_MS`/`FLASH_MS` constants the old burst itself used) instead of a real transfer's clock -- so Play mode never shows two different arrival visual languages, and hiding the old burst does not silently remove the arrival presentation entirely (a real risk the review explicitly flagged). The selection/positioning/draw logic itself was extracted into one shared `renderGameplayTransferPresentation` helper used by both callers, avoiding a second copy of logic that had already drifted once before (the z-order bug above). The permanent suite gained a real regression for exactly this class of defect: every case now spies on the real `playerSpawnBurstGraphics.lineBetween` method and asserts zero calls during Play mode, and a new case 7 covers the pure initial-entry scenario (no goal ever reached) end to end. **Negative control, live-verified:** temporarily restoring the old unconditional call reintroduced 416 real `lineBetween` calls and made 5 of the new checks fail (including case 7's own arrival-presentation checks, confirming the fix's second half -- the new arrival visual -- is also load-bearing, not just the suppression); reverting restored 48/48. Full regression pass at head (this round's own commit): `tsc --noEmit` clean; 107/107 required Teleport tests; QA-preview lifecycle (30/30), trail-corner raster (4/4) unaffected; decision-registry + Navigation Core required suite (199/199) unaffected; canonical `npm run verify` -- 99 files / 1103 tests (1 skipped), zero failures; `npm run visual:ui-surfaces` shows only the same two pre-existing documented unrelated failures. **Remaining, explicitly not addressed this round (disclosed, not silently accepted):** the review's requests for strengthened held-identity/geometry/reduced-motion/pause assertion precision, an uninterrupted real-time video recording separate from the deterministic stepped runner, and a dedicated performance measurement pass for the transfer specifically, are all still open.

  **A fifth round (ChatGPT-assisted checkpoint 5146800659, continuing 5144999445) completed all three deferred items above, and found two further real things along the way.** (1) `resolveShortestPathToGoal()` is a new QA method exposing the REAL production wrap-aware solver (`resolveLegacyPlayableShortestPath`, the same one this scene's own AI/telemetry code already calls) so every real-browser script in this wave now drives an actual maze route through the real solver instead of each maintaining its own separate, non-wrap-aware BFS -- the review's own explicit request. (2) `getGameplayTransferPresentationDiagnostics()` is a new read-only QA method exposing the last real `resolveTeleportTransferPresentation` output (source/target/conduit points, all real WORLD-space) so tests can assert on the actual displayed geometry, not just visibility. (3) The permanent suite's assertions were substantially strengthened using both: case 1 now compares the shell's own real measured world tip (the exact technique `verify-teleport-anchor-preview-lifecycle.mjs` already established) against the presentation's reported source point, verifies the held primary id stays non-null and constant through the whole active/stored/delivering window (clearing ONLY at the real completion boundary, not "null-or-held" as the previous round's looser check allowed), and verifies the real player marker's alpha genuinely fades out during outbound and recovers at completion (replicating `drawDynamicBoard`'s own real `playerAlpha` formula from real scene state, after a first attempt using `isLegacyMenuPointVisibleInStaticDraw` was caught live measuring the wrong signal); case 3 (reduced motion) now verifies real intermediate crossfade values, a real absent traveling packet throughout, and real completion; case 4 gained a sibling case 4b covering a pause during the closed/stored interval specifically (not only mid-conduit), with a resume-clock tolerance now derived from the real 16ms step instead of a generic number; a new committed `scripts/analysis/capture-gameplay-transfer-evidence.mjs` (real BFS-solved full-cycle, initial-arrival, and reduced-motion clips, the real game loop never stopped, matching `capture-wave4d-a-evidence.mjs`'s own established stepped-video technique) and `scripts/analysis/measure-gameplay-transfer-performance.mjs` (real end-to-end full-frame interval sampling, bucketed by whether a transfer is genuinely armed, across a real solved cycle, desktop and mobile-DPR3) close out the video and performance asks.

  **A third real defect, found strengthening case 7 (initial arrival) and fixed:** `gameplayTransferConduitCanvasImage` could get stuck permanently visible after a real Play-mode initial arrival with no other system also forcing a redraw around the same instant. `drawDynamicBoard`'s own hide branch for this image only runs from inside itself, which is dirty-flag-gated -- once `playerSpawnBurst.active`'s own falling edge stopped forcing `boardDynamicDirty`, nothing requested another redraw, so the hide branch never ran. Fixed the same way this file's own settings-cog defect was (a `wasActive`-style one-more-redraw-on-the-falling-edge tracker, `playerSpawnBurstWasActiveForRedraw`); a real transfer's own completion never showed this because other systems happen to force a redraw around that same instant, masking it.

  **A confirmed real, PRE-EXISTING characteristic that a fifth round disclosed rather than fixed (over-reading that round's own "preserve existing gameplay/progression timing" as covering pause correctness too), and that a seventh round (below) actually fixed:** pausing froze the material animation clock and the held primary id (both had their own explicit gate) but did NOT freeze the underlying `time.now`-relative lifecycle timer -- `this.scene.pause()` is never called anywhere in this codebase; "pause" is a UI-state flag individual subsystems check, matching the same pre-existing pattern already established for the play trail's own animation clock. A first attempt at case 4/4b's own pause assertions asserted full presentation stasis and caught this live (phase genuinely advancing outbound -> stored during a "paused" window); that round weakened the assertion to disclose the gap rather than close it. **This is now fixed** -- see the seventh-round paragraph below; the disclosure above is kept as the historical record of what was and was not true at the time, not as current behavior.

  Full regression pass at the fifth round's own head: `tsc --noEmit` clean; 107/107 required Teleport tests; QA-preview lifecycle (30/30) and trail-corner raster (4/4) unaffected; decision-registry + Navigation Core required suite (199/199) unaffected; the gameplay-transfer suite (70/70, up from 48); canonical `npm run verify` and `npm run visual:ui-surfaces` (only the two pre-existing documented unrelated failures) both clean.

  **A seventh round (ChatGPT-assisted review 5147427468, "preserve the renderer; correct pause and evidence") fixed the pause gap above for real, and corrected three evidence-quality defects the sixth round's own delivery had.** (1) **Pause now genuinely freezes the conduit presentation, not only the lifecycle phase/material clock.** `applyLegacyGameplayTransferPresentation`/`applyLegacyGameplayInitialArrivalPresentation` computed `outboundElapsedMs`/`deliveryElapsedMs` as their OWN separate `time - startedAtMs` read, uncompensated by the pause-offset accumulator (`gameplayTransferPausedDurationMs`/`PauseStartedAtMs`) that `resolveLegacyPlayerTransferState`/`resolveLegacyPlayerSpawnBurstState` already subtracted -- so `phase`/the material clock correctly froze while `openFraction`/`conduitVisible`/`sourceFlareIntensity` (the presentation module's own closure/crossfade windows, which read these same elapsed inputs directly) kept advancing underneath, confirmed live via a real pause showing `openFraction` and `sourceFlareIntensity` changing while `phase` stayed identical. Fixed by subtracting the same paused-offset at both call sites too. A second, smaller defect surfaced once the first was fixed: the pause boundary itself let exactly one real frame's worth of elapsed-ms slip through unfrozen, because `gameplayTransferPauseStartedAtMs` was anchored at the CURRENT (already-paused) frame's own time instead of the last real pre-pause time -- confirmed live as a small (~1 frame) but real, non-zero delta between the last-unpaused and first-paused sample. Fixed via a new `gameplayTransferLastUnpausedTimeMs` field, anchoring the pause start there instead. No second transfer state machine, no whole-scene pause, no change to ordinary unpaused durations/ordering -- both fixes are the same narrow, already-authorized pause-offset-subtraction pattern the fifth round already used elsewhere, applied to the two sites it missed. (2) **Real-time recording now has exactly one clock owner.** `capture-gameplay-transfer-evidence.mjs` previously called `game.loop.step()` from its own synthetic accumulator WHILE the real game loop kept running -- a genuine mixed-clock bug; fixed by removing every `loop.step()` call from that file and driving pacing purely through `page.waitForTimeout()` against the real, never-stopped RAF loop, matching `measure-gameplay-transfer-performance.mjs`'s own established technique. The full-cycle clip now counts actual accepted moves and asserts a real armed transfer rather than assuming the planned route length completed. (3) **The reduced-motion clip now captures a real transfer.** The prior version fired the goal-reaching move without checking acceptance, then treated ANY visible conduit -- including the unrelated initial-arrival conduit -- as success; the shipped clip's own diagnostics showed `phase: 'idle', active: false`, proof no real transfer ever happened. Fixed to wait for genuine initial-arrival settlement, require the goal move to be REAL accepted, and require a REAL armed transfer (`playerTransferEnergyArmed`) before capturing anything. Fixing this surfaced a second, real defect: waiting only for `resolveLegacyPlayerSpawnBurstState(...).active` to clear is not sufficient -- confirmed live (real, unthrottled Playwright context) that the separate maze-tile-reveal build lifecycle (`isLegacyPlayLifecycleInputLocked()`) can still be running for roughly 1.7 real seconds after the spawn burst itself reads inactive, silently rejecting the injected fixture move with reason `'lifecycle-locked'`; fixed by waiting for both conditions together. The capture script's own `playerVisible`/`poseAvailable` diagnostics were also brought in line with the permanent test's already-correct formulas (a propagation gap from an earlier round). (4) **Presentation diagnostics and performance measurement re-scoped.** `getGameplayTransferPresentationDiagnostics()` (added the prior round) now backs real `phase`/`openFraction`/`conduitVisible`/`sourceFlareIntensity` assertions in cases 4/4b/4c instead of tile-visibility. `measure-gameplay-transfer-performance.mjs` now buckets every real frame into five workload categories (initial-arrival, extraction, stored, delivery, settled-idle) instead of a flat armed/idle split that had silently folded the initial-arrival burst into "idle," waits for genuine settled readiness (no transfer armed AND no spawn-burst active) before sampling the baseline instead of a blind 1500ms wait, and reports `repoDirty`/`provenanceWarning` fields so a stale-commit report is self-evident rather than silent -- this closes a real provenance gap the reviewer caught: an earlier report named commit `205aec1b` (not the round's actual final head) because the measurement had been run before that round's own commit landed, even though `dist/` was correctly rebuilt from the fixed working tree. (5) `verify-gameplay-teleport-transfer.mjs` restored genuinely strict "frozen identically" assertions for cases 4/4b (phase/openFraction/packetVisible/conduitVisible/sourceFlareIntensity, not merely valid ranges) and added case 4c (pause during initial arrival specifically, exercising `resolveLegacyPlayerSpawnBurstState`'s own pause-offset path), fixing that new case's own precondition-polling budget (a 60-iteration tight loop was not generous enough for the real initial reveal/arrival timing) to match case 7's already-established batched-polling pattern.

  Full regression pass at this round's own head (`ba0a269e`): `tsc --noEmit` clean; the gameplay-transfer suite is genuinely 79/79 (up from 70/74 restored-but-failing at the start of this round); QA-preview lifecycle (30/30) and trail-corner raster (4/4) unaffected; the required Teleport + decision-registry + Navigation Core suite (214/214) unaffected; canonical `npm run verify` clean; `npm run visual:ui-surfaces -- --maze-seed 3749` shows only the same two pre-existing documented unrelated failures (`options-bottom-account-action`, `mobile-overlay-scroll-reachability`). Performance measured fresh after this round's own commit, with `repoCommit` in the report matching this exact head and `repoDirty: false`.

  **Real-browser evidence:** `scripts/analysis/verify-teleport-anchor-preview-lifecycle.mjs` drives the actual built game (real QA preview calls, not a mock) and asserts, among other checks: the pool stays at exactly 8 slots throughout; the SELECTED identity's own slot (not a fixed spare) shows the real computed pose in real WORLD space (via `Image.getWorldTransformMatrix`, not local x/y); the other seven identities are not pinned to that same pose; a real non-identity `boardZoomContainer` transform (offset + uniform scale) still produces the correct displayed world position and rotation; a target placed on a real, live HUD control (Account/Settings/Leaderboard/Start, read from this scene's own `uiButtons`) is genuinely excluded, not merely a synthetic rectangle; a preview session retains its held anchor across a target change and `newSession: true` starts fresh; ending the preview restores ambient control; the rendered port (measured by transforming the shell's own real source tip through its actual world transform matrix) matches the resolver's `portX/portY`, and the transformed source-canvas corners fit the reported footprint, at identity, a 1.6x, and a below-1x parent scale, with a negative control proving the original uncompensated-scale bug genuinely fails this same comparison; a settled, reduced-motion scene with zero manual frame steps still applies and restores the preview immediately on begin/update/end; the real fixed pause-button exclusion and an active floating stick's real transient footprint both work as intended (excluding only while genuinely active, never the whole gesture surface); the preview also resolves correctly in settled Active Play, not only Main Menu; and Navigation Core's own trail/player-glow/goal-halo canvases are unaffected -- 30/30 checks pass, wired into CI. `scripts/analysis/capture-teleport-anchor-pose-evidence.mjs` produces `mazer-wave4d-b-teleport-anchor-pose-proof.png`, a real 3-panel contact sheet (all policy-selected via `newSession: true`, no forced IDs) showing the real iridescent shell at three different real targets/anchors, each genuinely clearing the real HUD controls near it (verified live: a target near the Account icon now selects a different anchor entirely, rather than the icon-crowding placement an earlier round's screenshot showed).

  **Not yet done:** non-uniform (anisotropic) parent scale is a documented non-goal, not a defect -- rotation composition under an anisotropic transform has no single correct answer (a rotated shape shears into an ellipse), and `boardZoomContainer`'s own stated purpose is a uniform zoom (confirmed live to currently always be identity in this codebase; the "Board Zoom" setting changes tile size, not this container); the full spawn/extraction state machines and shared conduit/packet choreography; and replacing the existing player-transfer beam rendering with this system. `src/scenes/BootScene.ts` and the `navigationCore*.ts` modules remain actively owned by Wave 4D-A, untouched by this slice.

  **An eighth round (direct product-owner instruction in the working session, 2026-09-09 -- not a GitHub review) addressed four things the owner reported seeing in the actual running game, in two clearly separated commits.**

  *Correctness commit (Teleport continuity + a movement-lock regression guard):*
  (1) **The engaged perimeter diamond read as an unrelated object during a transfer.** Root cause, found live: a real gameplay transfer (and the Play-mode initial arrival) always coincides with the maze deconstruct/rebuild, which drives the ambient title-orbit spin -- so the seven non-engaged perimeter diamonds swirled chaotically around the whole viewport edge while the one engaged diamond sat pinned at its anchor with the conduit attached, reading as "a diamond that isn't one of the ones on the edge". Fixed in `resolveLegacyMenuPathTitleOrbitFrame`: in Play mode, while a transfer is armed or the arrival burst is presenting, the orbit spin is suppressed and every sigil holds its resting corner/edge-midpoint station (via the module's existing settle-phase ease). Menu mode -- the demo AI's own goal-reset choreography -- is completely unaffected (no gameplay transfer is ever armed there). Verified live: through a full real transfer the non-engaged diamonds now hold station with < 3px drift and the spin reads as inactive the whole time. (2) **The conduit blinked out partway through a transfer.** During the maze rebuild inside a transfer's own 'stored' phase, the anchor selector transiently returned no legal pose for the held anchor (a temporarily-larger exclusion overlapping its footprint for a few frames, not a real viewport change), degrading the presentation to inactive mid-transfer. Fixed with a sticky last-good POSE (not just the sticky id already there): when selection returns null but a transfer is genuinely active and a cached pose exists, the cached pose is reused through the transient window -- the exact same "don't forget mid-transfer" rule already applied to the held id. (3) **Movement-before-the-maze-is-built: investigated, does not reproduce for ordinary movement.** Ordinary player input is genuinely locked (`isLegacyPlayLifecycleInputLocked`) through the entire visible maze build on both first entry and the next-maze transition -- confirmed live by hammering a real move command every frame across the whole reveal and observing zero accepted while any tile was still unrevealed. The only motion during the build is the arrival "materialization" presentation (the conduit delivering the player to its start tile), which the owner accepted as part of arrival. Added a permanent regression case (case 9) that asserts this lock on both build paths so it cannot silently regress. New permanent cases 8 (calm ring) and 9 (build lock) bring `verify-gameplay-teleport-transfer.mjs` to 87 checks (all passing).

  *Owner-authorized visual-amendment commit (calmer trail material + readable shine):* the owner explicitly authorized reopening the trail's visual treatment because "the coloring is abrasive". The frozen reference file (`docs/assets/reference/navigation-core-v1/`) and the shared Wave 4D-A module `src/render/navigationCoreTrail.ts` (`NAVIGATION_CORE_TRAIL_ENERGY_STOPS`, `sampleTrailEnergyColor`) are BOTH left untouched -- the amendment is scoped entirely to Wave 4D-B's own rendering in `MenuScene.ts`, via a new `LEGACY_NAV_CORE_TRAIL_CALM_ENERGY_STOPS` palette and a local `sampleCalmTrailEnergyColor`, so only this scene's three consumers of that material (the continuous play trail, the gameplay-transfer conduit -- the owner reported that beam as abrasive too -- and the on-board goal-star ring) sample the calmer set. The original reference palette runs every stop at full saturation with three consecutive fully-saturated warm stops (red/orange/yellow, ~37% of the cycle), which reads on the real in-game trail as a neon full-spectrum rainbow filling the corridor; the calmer set keeps the recognizable cool "energy" identity and the exact 8-stop cyclic structure but pulls every stop to medium luminance / moderate saturation, collapses the three-stop warm band to one dusty-mauve accent bracketed by violet on both sides, and keeps adjacent stops close in hue so the blend stays smooth. Also: the trail glow-pass alpha dropped 0.22 -> 0.15 (a soft halo supporting the crisp core, not a wash flooding the corridor), and the traveling-shine highlight mix nudged up (0.85 -> 0.92 white, +0.5 -> +0.58 alpha) -- its own math was already correct (verified live: the clock advances, the envelope fades, the highlight travels the whole path) but an 0.85 white mix on a fully-saturated rainbow base barely registered; against the calm base it now reads clearly as a soft light travelling along the line. The four `tests/fixtures/trail-corner-raster/*.png` baselines were regenerated (`--update-baseline`) for the new palette and visually confirmed. **The new palette is a candidate for the owner's review, not automatically accepted because it was implemented.**

  **A ninth round (ChatGPT-assisted exact-head review 5173052153, anchored to head `1092acc8`) kept the calm-trail candidate and closed three correctness gaps it found in the eighth round's own delivery.** (1) **The pause fix did not go far enough: the coupled maze reveal/deconstruct lifecycle kept advancing while paused** -- the submitted pause recording showed corridor tiles disappearing between two frames with the pause menu still open. The round-7/8 pause work froze the transfer's OWN outbound/delivery clocks (`resolveGameplayTransferPausedOffsetMs`), but `advanceLegacyMenuStaticDrawStage`, the bleed-dock/handoff/preroll resolvers, and the pending generation/reset request consumers all run off separate absolute-time deadlines and phase anchors that were never compensated -- and a next-maze generation request could even be consumed behind the frozen screenshot. Fixed with `holdLegacyPlayMazeLifecycleDuringPause`: for the duration of a Play-mode pause it shifts every one of those deadlines/anchors (`menuStaticDrawNextRowAtMs`/`NextTileAtMs`, `menuStaticDeconstructStartedAtMs`, `menuStaticDeconstructZeroHoldStartedAtMs`, `menuStaticBuildPrerollStartedAtMs`, `menuStaticBuildPhaseStartedAtMs`, and both pending requests' `dueAtMs`) forward by exactly the real frame that elapsed while paused -- so every `time >= deadline` / `time - anchor` comparison yields the last-unpaused result and on resume each is still exactly as far in the future as when the pause began. No `this.scene.pause()`, no `time` mutation, no whole-scene freeze; Resume UI keeps working; unpaused durations/ordering unchanged; menu mode never reaches `overlay === 'pause'` so the demo AI's own choreography is untouched. The transfer's own anchors are deliberately NOT shifted here (already compensated at read time -- shifting both would double-count). (2) **The eighth round's sticky-pose fallback reused a cached pose without re-checking clearance.** Fixed: `resolveLegacyTeleportAnchorPreviewResult` now also returns the exact `{viewport, exclusions}` snapshot the selection was resolved against, and the fallback only reuses `gameplayTransferLastPrimaryPose` when `resolveTeleportAnchorEligibility(cachedPose, snapshot.exclusions, snapshot.viewport).eligible` -- validated against the SAME frame's layout inputs, not a second recomputed snapshot. A genuine clearance failure (a real resize, or an exclusion that actually now covers the cached footprint) falls through to the presentation module's existing explicit degradation (`poseAvailable: false`, no fabricated shell/conduit, held id + all domain/lifecycle state retained), never an offscreen or HUD-overlapping shell drawn to avoid a blink. (3) **Case 9 was strengthened from a shape that could pass without proving readiness enforcement.** It now probes with a real legal move direction (a walkable neighbour of the player, so a rejection can ONLY be `lifecycle-locked`, never `blocked`), requires a genuine incomplete-reveal window to be observed, requires every rejection in that window to be reason `lifecycle-locked` specifically, tracks the DOMAIN player position (so arrival materialization is distinguished from ordinary traversal), requires the build to genuinely reach settled + input-unlocked within the bound, proves a legal move then succeeds AND moves the player, and asserts the accepted goal move before claiming the next-maze path was exercised -- on both build paths. New negative controls: case 4d (pause-hold patched off -> the maze reveal genuinely keeps advancing while paused, proving cases 4/4b's new board-frozen assertions are load-bearing), case 8b (exclusions patched to cover the whole viewport -> the cached pose is correctly rejected and the presentation degrades rather than rendering the stale pose, with held id + domain state retained), and case 9's own (lifecycle lock patched off -> a legal move IS accepted while the reveal is incomplete). Cases 4/4b/4c gained "board reveal/deconstruct frozen while paused" and "does not race ahead on the first resumed frame" assertions. `verify-gameplay-teleport-transfer.mjs` is now 118 checks, all passing.

  **Merged:** PR #348 squash-merged to `main` at commit `b0b99a9ca53acf36586881ef3fce3feef2994c4e` (title "feat(teleport): integrate single-primary gameplay transfer"), following the owner's own direct in-session authorization, exact-head merge guard, and a green post-merge CI run. Navigation Core v1 and Teleport System v1 (single-primary gameplay transfer) are now both accepted, merged implementation baselines; further changes to `src/render/teleport*.ts` need a demonstrated regression or new owner direction, not another correction pass. Wave 4D-B stays `active` for those four remaining Teleport-owned paths; `src/scenes/MenuScene.ts` itself has handed off to a new Wave 4E ("World Identity", see immediately below) now that its Teleport-specific QA-preview/gameplay-transfer work is done.

- **World Identity: background star twinkle stability fix** (Wave 4E, `src/legacy-runtime/legacyMenuBackdrop.ts` + `src/scenes/MenuScene.ts`'s star-draw path, branch `claude/mazer-world-identity-star-twinkle`, PR open/unmerged). **Root cause, found by live Playwright trace, not by inspection alone:** the reported "stars switch colors like a strobe" was not a tuning issue with an intentional color-rotation feature -- `MenuScene.ts`'s star-draw loop recomputed `const starSeed = ((star.x * 9973) + (star.y * 6151)) % 1;` fresh every single frame from the star's own continuously-drifting warp position. The large multiplier constants combined with `% 1` amplify the star's tiny continuous per-frame position drift into an effectively uncorrelated value every frame -- confirmed live: 30 samples 50ms apart jumped the star's `starSeed` across the full `[0, 1]` range with no pattern. That one unstable value silently fed three separate things every frame: the star's own twinkle period, its round-star color-bucket choice (blue/warm/white), and -- for the brightest tier of stars -- a `HSVToRGB` sparkle hue. **Fix:** a genuine persisted `seed: number` field was added to `LegacyMenuBackdropStar`, assigned once at creation (`createLegacyMenuBackdropStars`) and reassigned ONLY at real rebirth (`resetLegacyMenuBackdropStarNearWarpOrigin`, the recycle path when a star exits the warp field and respawns near center) -- never recomputed mid-life, never reseeded on draw. `MenuScene.ts` now reads `star.seed` directly instead of the unstable hash. The brightest-tier sparkle hue was additionally changed from a continuously time-rotating hue (`(animationTime / period) + starSeed`) to a fully stable per-star hue (`starSeed` alone, no time term), matching the explicit product direction to prefer a stable per-star color over rapid cycling through unrelated hues. **Left untouched:** star silhouette/rendering primitive, placement/warp motion physics (`advanceLegacyMenuBackdropStars`), the `toggleAnimatedBackdrop`/`prefersLegacyReducedMotion` gates (both confirmed byte-identical to before), and the title-wordmark and goal-star-ring sparkle call sites, which share the same `drawLegacyFourPointSparkle` primitive but keep their own separate, untouched time/cycle-based hue logic (a background star should not inherit an announcement's attention-grabbing peak merely because they share a draw function). **Verified live (headless Playwright, real unthrottled RAF, not the interactive browser-pane tool which has a known RAF-throttle artifact):** `star.seed` is now stable across 30 sampled frames per star and changes only at genuine recycle; a 60-frame/60ms-interval trace of one star's twinkle alpha, size multiplier, and sparkle hue shows a smooth, continuous sinusoidal-eased brightness/size curve with zero discontinuities and a perfectly constant hue for the entire trace (versus chaotic, frame-to-frame-random values before the fix). Before/after real-time video evidence was captured from the actual built game at identical viewport/scene/settings (`?mazeSeed=3749&authFixture=authenticated`, one real, never-stopped simulation clock, 8 real seconds each) by temporarily reverting only the two source files to pre-fix `main`, rebuilding, and recording, then restoring the fix and re-recording -- deterministic manually-stepped diagnostic traces are kept as separate, explicitly labeled evidence and are not a substitute for this real-time footage. **Tests:** `tests/reset/legacy-menu-backdrop.test.ts` gained a case proving an alive star's seed is byte-identical after 50 advances and that only a recycling star gets a fresh seed matching the supplied random source exactly (6/6 passing). **Ownership:** `src/scenes/MenuScene.ts` and the existing `src/legacy-runtime/legacyMenuBackdrop.ts` module (its data model gains this fix's `seed` field; the module itself predates this slice) are registered to a new Wave 4E ("World Identity") in the decision registry, depending on 4D-B; `REQUIRED_INTEGRATOR_WAVE_BY_PATH` and `tests/architecture/decision-registry-contract.test.ts` were updated to match (43/43 passing, including the registry's own "this working tree's real changed files stay within one registered wave" self-check). Navigation Core and Teleport source files are untouched by this diff -- note the retained Teleport modules themselves live under `src/render/` (`teleportPrimaryAnchor.ts`, `teleportAnchorPose.ts`, `teleportTransferPresentation.ts`, `teleportTransferConduitCanvas.ts`), not `src/legacy-runtime/`. **Full regression at this head:** `tsc --noEmit` clean; the two focused suites above pass (49/49 combined); canonical `npm run test:verify` -- 99 files / 1105 tests (1 skipped) + a separate 12/12 suite, zero failures; `npm run visual:ui-surfaces -- --maze-seed 3749` shows only the same two pre-existing documented unrelated failures (`options-bottom-account-action`, `mobile-overlay-scroll-reachability`). **Remaining limitation:** this slice covers only the background starfield's own twinkle/sparkle-hue stability; it does not touch star placement/count/silhouette, the title/announcer sparkle timing those share a primitive with, or any other World Identity surface -- those remain open for a future 4E slice if the owner reopens them.

  **A review round (ChatGPT-assisted comment 5184268748, anchored to head `165e8e41`) found the runtime fix itself sound but a real verification gap: this wave's own `tests/reset/legacy-menu-backdrop.test.ts` ran locally (globbed by `npm run test:verify`'s TEST_SPINE) but was never named in `.github/workflows/verify.yml`'s own explicit vitest invocations -- the same silent-CI-gap class Navigation Core's and Teleport's own required-test lists already exist to close for their directories, just not yet extended to this new wave. A green CI run and a local "6/6 passed" were therefore two different claims. Fixed by adding this wave's own `scripts/verify/world-identity-required-tests.mjs` (`WORLD_IDENTITY_REQUIRED_TESTS`) plus `scripts/verify/run-world-identity-required-tests.mjs`, mirroring the existing Navigation Core / Teleport pattern exactly, wired into both CI (`.github/workflows/verify.yml`, a new "Verify Wave 4E World Identity required coverage" step) and the local canonical runner (`scripts/verify/run-test-verify.mjs`). The review also flagged that the data-model test alone would still pass if `drawBackdrop()` reverted to the old per-frame hash while leaving the `seed` field intact -- closed with a new source-pin contract, `tests/architecture/world-identity-star-render-contract.test.ts` (same established pattern as `directional-intent-contract.test.ts` and its neighbors), asserting the draw loop's exact `const starSeed = star.seed;` assignment and the bright-star sparkle hue's exact stable `HSVToRGB(starSeed, 0.8, 1)` form, and rejecting the old per-frame-hash assignment and the old time-rotated hue expression respectively -- scoped narrowly enough not to false-positive on the title/goal-ring sparkle call sites' own legitimate, unrelated time-driven hues elsewhere in the same file. **Negative controls, live-verified:** temporarily restoring both rejected expressions in `MenuScene.ts` made the new contract test fail as expected, then reverting restored a clean pass; separately, temporarily breaking an assertion inside `tests/reset/legacy-menu-backdrop.test.ts` made `run-world-identity-required-tests.mjs` itself exit with status 1, then restoring it returned to exit 0 -- confirming the required CI invocation actually fails when its named suite fails, not just when the file is missing. Also corrected two documentation inaccuracies this same review caught: `legacyMenuBackdrop.ts` is described above as the existing module it is, not new, and the retained Teleport modules are now correctly named under `src/render/`, not `src/legacy-runtime/`. **Full regression after this round:** `tsc --noEmit` clean; canonical `npm run test:verify` -- 100 files / 1106 tests (1 skipped) + the separate 12/12 suite, zero failures (the +1 file/+1 test is the new render contract; `legacy-menu-backdrop.test.ts` is not double-counted, since the local runner's own `_NOT_COVERED_BY_LOCAL_SPINE` filter excludes `tests/reset/*` paths TEST_SPINE's glob already runs); `npm run visual:ui-surfaces -- --maze-seed 3749` still shows only the same two pre-existing documented unrelated failures. No runtime source file changed in this round -- `src/scenes/MenuScene.ts` and `src/legacy-runtime/legacyMenuBackdrop.ts` are byte-identical to the previously reviewed head `165e8e41`; only test, CI-workflow, verify-runner, and documentation files changed. Per the reviewing owner's own explicit instruction, PR #349 remains open/unmerged for review; no production deployment.
- final screenshot-grade menu material/composition is still open

## What is already restored

- legacy archive extraction command: `npm run legacy:extract`
- restored Unreal truth workspace in ATLAS temp storage
- legacy menu-shaped web front door
- fixed legacy-shaped menu snapshot separated from the play-maze generator
- fixed legacy-shaped menu snapshot now projects its named 25-space blueprint into a 49-cell browser grid, which better matches the old source default-scale density while preserving contiguous demo path truth
- filled the fixed menu snapshot with staircase and right-spine silhouette branches for closer screenshot parity
- upper-left menu snapshot branches now carry a denser frame/pocket/lattice read, which moves the upper-left corner a little closer to the restored legacy screenshots without claiming final screenshot-grade closure
- upper-right menu snapshot branches now carry a small title-adjacent lattice run, which reduces one coarse upper-right board mass without claiming final screenshot-grade closure
- lower-left menu snapshot shelves now reduce the oversized empty lower-left block in the fixed front-door board, moving the board silhouette a little closer to the restored legacy screenshots without claiming final screenshot-grade closure
- title lockup is now tuned to sit deeper over the board instead of reading like a small shell label
- title lockup opacity now uses a more translucent green-glass profile, moving the wordmark closer to the restored screenshots without claiming exact wordmark material or overlap
- legacy-style menu demo board/trail presentation
- legacy menu demo now emits live recovery cues and cue-specific pacing instead of collapsing wrong-turn motion back to plain explore timing
- fixed front-door menu snapshot fixture also runs the mistake-enabled legacy recovery lane instead of a solver-only attract path, so screenshot-comparison fixtures can surface dead-end, backtrack, reacquire, and AI-only reset behavior
- fixed front-door menu snapshot fixture bootstrap settles into a visible `explore` pose before the first source-shaped dead-end/backtrack seam instead of stopping in `goal-hold` or `reset-hold`; live menu runtime now uses the procedural `menu-generated` lane for the actual first impression
- menu-demo humanized routing now builds its route from the restored AI loop shape: scan unvisited neighboring path/floor tiles, admit only `AiTilePathCheck` candidates, queue potential tiles idempotently, choose the nearest candidate to the end, backtrack through the path stack when direct movement fails, surface the direct-fail beat as `dead-end`, mark the first recovery seam as the AI-only reset point, reconnect to canonical replay through adjacent floor movement instead of a non-adjacent route splice, stop first-mistake exploration once the visible dead-end/backtrack/reacquire cue loop is represented before clean replay, and advance movement/backtrack/reacquire beats on a single AI step cadence like the extracted C++ timer loop
- menu-demo goal reset now queues an immediate process-8 reset request after reset-hold has elapsed instead of waiting one extra explore-step delay before the process-0 generation handoff, and AI-only reset replay is covered without regenerating the menu maze
- menu-demo wrong-turn selection now applies a legacy `AiTilePathCheck`-style candidate gate, so the attract runner no longer commits into a one-tile spur that exposes no unvisited onward path from the candidate tile
- legacy generation/reset now routes through explicit queued request contracts instead of collapsing every branch into immediate rebuild calls
- legacy generation consumption now carries explicit stage-7 finalize responsibilities for play/menu spawn, title, and timer start
- legacy process-8 reset branches now route through explicit reset requests for active-play return vs menu-demo regeneration
- menu-demo process-8 reset consumption now enqueues the next process-0 generation request instead of regenerating inline, which preserves a real reset-branch handoff in the scene update loop without claiming the full staged generator is ported
- legacy stage `0/3/4/5/6` execution cadence is now explicit for menu-sliced versus play-continuous generation
- legacy generation metadata now carries explicit checkpoint/shortcut budget contracts into runtime diagnostics for menu versus play lanes, including the menu-only shortcut-stage floor that prevents small enabled menu mazes from under-braiding into single-route topology
- queued generation requests now publish their own build, stage, and budget contract alongside the live maze contract in scene diagnostics
- queued generation requests and consumed runtime mazes now carry an explicit stage-cursor projection, so diagnostics can distinguish queued process-0 entry from consumed stage-7 finalization without claiming the full staged Unreal generator is ported
- generation requests now carry the explicit delay-gated process-0 entry contract, and reset requests now carry the explicit initialized process-8 entry contract
- legacy level-building scheduler truth is now explicit in the runtime contract: process `0` requires the armed start-time + delay-start flag, the exact legacy delay duration remains honestly unrecovered, and initialized process `8` reset entry is marked as the branch that bypasses that delay gate
- legacy stage progression truth is now explicit in the runtime contract: stages now publish their own completion signals, next-stage transitions, and the stage-5 skip-to-6 rule when shortcuts are disabled
- stage `4` now advances directly to stage `6` when scale disables shortcut process `5`, so small-maze runtime plans no longer publish an impossible `4 -> 5` transition
- browser shortcut braiding now scores wall openings by useful loop distance, applies family-specific shortcut profiles, disables weak fallback shortcuts for sparse mazes, adds a bounded route-aware braided bypass pass after endpoint selection that requires separated route reconnection before opening the wall, applies a raster-level legacy shortcut bridge pass, and active `createLegacyMaze()` play snapshots now build topology through a source-shaped checkpoint path pass, convert path-neighbor walls through `CreatePath`-style wall-array collection, then apply the legacy opposite-corridor shortcut bridge rule through random wall-array removal plus capped weak-route reinforcement, so generated mazes can expose more than one start-goal route without starting from the previous DFS perfect-maze topology; generated play snapshots now also report stricter route-quality stats for bypassable solution edges, meaningful detour-bearing bypass edges, and route bands, guarding against future shortcut openings that look valid locally but only create shallow loops
- active `createLegacyMaze()` play snapshots now prune disconnected floor components after shortcut creation and rebase weak source-selected goals to the farthest reachable floor only when the restored source-shaped path builder would otherwise leave a trivially short route; this is a quality/stability guard for the browser port, not a new claim of exact Unreal RNG/timing parity
- menu static-board drawing now follows the stage-6 row-slice contract during generation application, so the front-door board reveal advances by legacy draw-stage rows instead of appearing only as one completed static pass
- menu stage-6 row-sliced drawing is now cadence-gated, so the front-door board reveal remains visible over time instead of advancing as fast as the browser frame loop can run
- active-play movement now follows the restored Unreal simultaneous-key input buffer: first movement keydown arms a 50ms resolve delay, keyup flushes an accepted pending tap before clearing its held flag, held direction flags can combine into one composite step, opposing directions cancel, and repeat movement uses one shared movement-speed interval across all held directions instead of browser-specific repeat frequency. The buffer and repeat gate clear on pause/menu/reset and browser focus-loss boundaries; runtime diagnostics publish held flags, resolved vector, pending timer, repeat interval, accepted/merged counts, pointer-start state, and source delay. The `player-input-keyboard-gate-fixed` live QA route completed `174/174` planned one-step keyboard moves after the pre-fix run failed on move zero. See `docs/ops/MAZER-PLAYER-INPUT-MOVEMENT-CORRECTNESS-PACKET-2026-07-11.md`.
- active-play directional intent now resolves through the versioned, wrap-aware `LegacyDirectionalIntentResolver` before the world-turn movement phase: taps remain one accepted step and held keyboard/touch/stick intent continues at the configured cadence. Discrete inputs retain a single latest-wins queued turn. Stick input uses its normalized 360-degree vector directly, ranks legal cardinal exits by continuous dot-product projection on every tile, and turns as soon as another branch is more aligned with the thumb; there is no angular sector or hysteresis bucket. When the strongest projection meets a wall, a weaker positive projection may take a legal corner, while Smart Steering may move exactly one perpendicular tile only when the next tile immediately restores the strongest lane. Captured arrow/stick input remains owned beyond the visible frame, preserves direction through the center deadzone, and keeps its repeat scheduled while blocked so the same pointer can recover. Release, focus loss, pause, reset, generation, lifecycle transitions, and Smart Steering changes clear or synchronize intent. Runtime diagnostics publish bounded input mode, analog vector, active/queued directions, requested candidates, assisted lane-shift count/one-tile limit, and the last decision, never a future route.
- active-play gameplay mutation now enters one scene-owned `WorldTurnHost`: non-play mode is stopped, overlays and lifecycle input locks are paused, and active play is running. The host owns one `WorldTurnSystem`, registers phase handlers once, exposes registered phases in canonical order, and keeps timed-mode ticks disabled unless a future owning mode opts in explicitly. Paused/stopped commands and disabled timed ticks cannot run handlers or consume a turn; cosmetic render clocks remain outside world-turn mutation truth.
- held keyboard, touch-arrow, and stick timing now shares `legacy-movement-pace-v1`: the persisted Move Speed slider remains the base preference, new players retain the prior timing exactly, and established player progression can apply only a bounded level/pace adjustment after at least one completed cycle. Explicit `0%` and `100%` selections remain hard overrides, all input paths consume the same effective profile, and diagnostics expose only the version plus bounded base/effective/context fields.
- active-play pointer/touch input now maps mobile swipes and short taps into the same one-step vector contract used by keyboard movement before passing through the legacy axis-gated collision resolver; pointer starts outside the active board bounds are ignored so HUD/shell taps no longer trigger movement, in-flight swipes are bound to one pointer identity so second touches cannot overwrite the active start, `pointerupoutside` still completes inside-board swipes, game-out clears stale pointer starts, and `mobile-touch-smoke` now proves three maintained-browser touch movement deltas on the active play route
- active-play phone controls now use a full-width bottom deck instead of the prior cramped right-side stack: movement remains a D-pad, pause is the primary right-side action, restart/trail are smaller secondary actions, and the maintained geometry proof keeps the deck below the board on `390x844`, `360x740`, `320x568`, and `172x407`
- active-play camera-follow now applies the same board offset to the static maze, dynamic overlays, HUD geometry, pointer-bound checks, and visual diagnostics board bounds; movement marks the static board dirty when camera-follow is active so player/trail layers and proof rectangles do not drift away from the maze
- active-play layout now uses a play-specific board-framing surface instead of reusing the front-door menu composition: the maintained `172x407` side-browser play board remains width-fit at `169x169`, but is intentionally top-biased (`top=57`, `bottom=226.001`) so compact touch controls can start at `241` with a clear board/control gap; menu mode keeps its stacked `Exit` / `Start` / `Options` layout (`250/300/350`), and visual diagnostics now publish the resolved layout/button surface for proof
- active-play collision now follows the restored Unreal axis-gated movement shape more closely: simultaneous movement checks the horizontal and vertical side gates independently, slides along the open axis when one held axis is blocked, and blocks a true diagonal corner move when the final diagonal tile is a wall
- active-play goal reset now uses the explicit process-8 `LegacyResetRequest` as the single return-to-menu authority, matching the restored Unreal `_ResetGame` -> process `8` branch more closely and removing the redundant scene-local `playResetReturnAtMs` shadow timer
- active-play pause reset now preserves existing trail history while moving the player back to the start tile, matching the restored source's `_ResetPlayerPosition` branch more closely than the prior browser trail wipe
- active-play HUD timer and goal-arrow geometry now route through `src/legacy-runtime/legacyPlayHud.ts`, and visual diagnostics publish source-exact bare `timerText`, `arrowAngleRadians`, and `arrowAngleDegrees` for desktop/mobile play-route proof
- active-play board rendering now shares the connected-corridor path segment treatment instead of drawing each walkable tile as a bright inset square, active-play dynamic overlays now use connected trail strokes plus inset start/goal markers instead of full square blocks, and the player marker is drawn from tile center with a high-contrast halo/core treatment. Overlay stroke/inset/player metrics now clamp responsively for tiny mobile tiles, reducing debug-grid, off-center, and oversized-marker visual drift in generated play mazes without changing maze topology
- active-play player readability now has a play-only locator glyph layered over the existing halo/core marker: tiny mobile tiles get a small ring/tick target so the player can be found over dense generated corridors and trail overlays, while menu-demo marker styling remains unchanged
- active-play endpoint readability now has distinct play-only start/goal glyphs: the start renders as a small target ring/core and the goal renders as a red diamond/core marker, while menu endpoints remain on the existing demo treatment
- front-door `Exit` has been intentionally removed from the active web runtime, including the old browser-safe quit equivalence module; the menu now exposes only `Start` and `Options`, normal phone portrait menu widths keep the two buttons horizontal while only tiny side-panel menu widths below `300px` use a two-button vertical stack, and the dead `message` overlay family remains removed from the active reset lane
- desktop menu layout now gives the front-door board more dominant space in wide viewports, which moves the current web shell closer to the legacy board-first screenshot composition without reopening other menu modules
- desktop title lockup now sits higher and reads less heavily over the board in wide viewports, which moves the wordmark treatment closer to the restored legacy screenshots without reopening button or backdrop ownership
- desktop front-door support chrome now uses narrower button boxes, more inward side-button placement, lighter button treatment, and a slightly stronger button-label/outline read in wide viewports, which moves the lower menu composition closer to the restored legacy screenshots without reopening board or backdrop ownership
- front-door button chrome now uses a darker menu-only pane fill and darker hover fill instead of translucent white block fills, keeping `Start` and `Options` visually aligned with the restored dark support-pane treatment without changing overlay or pause button behavior
- desktop front-door button plates now use a larger legacy-facing width/height envelope while keeping portrait button sizing bounded, which moves the lower menu support plates closer to the restored screenshot proportions without changing button behavior
- front-door button layout now uses a centered two-button `Start` / `Options` pair in desktop and normal phone portrait layouts, with a two-button stack reserved for tiny side-panel panes, removing the old unused quit button and its empty layout pressure
- legacy menu backdrop field now routes through an explicit backdrop owner contract in `src/legacy-runtime/legacyMenuBackdrop.ts`; the current mobile-clean direction uses a centered radial-warp pixel starfield, small edge-weighted broken-rail glyph shards, reduced floating sigil marks, and tiny drift runes instead of the prior vertical falling-star/floating circular light/orb treatment, keeping the gothic cyber motif sharper without claiming final screenshot-grade closure
- desktop menu board material now reads darker and less evenly tiled in wide viewports, which moves the grayscale trench mass closer to the restored screenshots without claiming final screenshot-grade closure
- menu-mode trench rendering now carries a wider light-core read with quieter wall-grid noise, which moves the board tile read a little closer to the restored screenshots without claiming final screenshot-grade closure
- menu-mode trench core rendering now bridges across connected walkable neighbors, so the board reads less checkerboarded while still not claiming dense screenshot-grade geometry parity
- menu-mode trench rendering now uses a stronger closed-edge inset and narrower light-core channel, reducing the chunky filled-cell read without claiming final screenshot-grade corridor geometry parity
- menu-mode dynamic trail/start/goal/player overlays now use corridor-framed or inset rendering instead of full square cells, reducing the most obvious modern block-path read without claiming final screenshot-grade board parity
- menu-mode dynamic trail/start/goal/player overlays now also use thinner corridor/highlight ratios, reducing the chunky cyan route and marker footprint in both desktop and mobile browser captures without claiming exact legacy trail/sprite parity
- menu-mode dynamic trail/start/goal overlays now use a heavier corridor-style route/marker footprint than the hairline-thin pass, and the live player marker now uses a centered halo/core glyph instead of a low-contrast square; this improves readability without returning to full-square block cells or claiming exact player sprite parity
- menu-mode static trench rendering now uses segment-based connected strokes and a gray-slab/dark-route role hierarchy, reducing the previous broad filled-cell material read without claiming final screenshot-grade board parity
- menu-mode static route cores now use a wider dark channel and softer static edge pass, reducing the tiny-grid/checker read in dense route areas without claiming final screenshot-grade board parity
- menu-mode static board material now uses the restored screenshot-facing role direction more cleanly: light-gray connected corridor cores over darker wall/slab fields, with flat dark edge strokes preserved for path definition rather than fake trench depth; this improves the board/material read but still does not close screenshot-grade parity
- menu-mode title pieces and dynamic trail overlays now reuse the same connected path material primitive as the maze path layer: edge/core segments come from `resolveLegacyMenuPathRenderSegments` / `resolveLegacyMenuPathRenderFrames`, the title supplies its own title-grid mask, and the trail supplies a trail-grid mask so title/trail tiles match maze path proportions instead of using independent square/stroke ratios
- front-door title pieces now carry a bounded animated jewel-sigil treatment on top of the path-piece wordmark: top/bottom rails, small crown glyphs, green/cyan prism sweep, warm/mint gem-facet tile cuts, and eight viewport-edge orbit diamonds refresh only the title layer on the bounded title cadence. Their settled poses point inward from the four corners and four edge midpoints. Player-transfer and spawn beams resolve their origins from the same current orbit phase, direction, corner rotation, and settled pose as the visible diamonds, so the energy remains attached while the sigils move. Outbound transfer begins with the final deconstruction tile batch; inbound beams and marker reveal begin with the final construction tile batch. The surrounding continuous effects use time-derived sine, cosine, smoothstep, smootherstep, or tween easing; the maze tile reveal itself remains intentionally discrete while the overlapping beams remove the prior blocky handoff.
- bleed-off docks use one direction-owned corridor frame for their base edge, nested core, facet, and traveling glow. Lateral terminal-tile connectivity cannot widen the off-board strip; the existing longitudinal continuation, notch split, corner guards, wrap topology, and true-screen-edge target remain unchanged.
- mobile visual crispness now comes from integer-snapped maze wall/path/cue rectangles instead of forcing whole-canvas pixelated CSS scaling; visual diagnostics publish CSS canvas size, backing pixel size, DPR, render-resolution ratio, capped target ratio, render deficit, and the `undersampledForDevicePixelRatio` flag for proof.
- menu-mode static corridor rendering previously tested a dark offset relief shadow, but the current maintained renderer removes that pseudo-depth pass and residual wall-grid bevel noise so the procedural menu board reads as a cleaner 2D maze without claiming final screenshot-grade material parity
- menu-mode board material now uses darker wall/slab mass and stronger path-edge contrast, moving the board closer to the restored screenshots' hard charcoal/light-gray maze read without claiming exact silhouette, title overlap, or trail/sprite parity
- menu-mode static board material now uses a denser slab/tile-read pass for the maintained side-browser scale: wall mass is slightly lighter, path edge/core contrast is stronger, residual wall texture is more visible, and very small menu tiles no longer collapse into hairline-only strokes
- live menu maze identity is now explicit on the runtime maze object (`source: 'menu-generated'` versus `source: 'play-generated'`), while fixed screenshot fixtures remain tagged as `source: 'menu-snapshot'`; demo policy no longer guesses from grid size
- `runtimeDiagnostics=1` now flows through the live `MenuScene` update loop and publishes repo-owned runtime diagnostics instead of stopping at helper-only parsing/tests
- runtime diagnostics now publish a proof-only DOM attribute so localhost inspection does not depend on the hidden `window.__MAZER_*` globals alone, without drawing a visible debug/proof panel into the game
- runtime diagnostics expose active menu-demo cue, mistake-enabled lane state, path cursor, and generation stage cursor through the DOM attribute/window proof surfaces so front-door demo and stage-7 finalization proof no longer depends on visible debug text
- the removed visible runtime diagnostics panel was never legacy UI; keeping diagnostics data-only preserves proof while preventing proof tooling from polluting the maintained `4173` browser
- runtime and visual diagnostics now expose live menu-demo AI wrong-branch/backtrack/recovery counters plus stage-6 row-reveal progress, so menu AI review and generation-fluidity review no longer depend on placeholder counters or hidden row state
- menu stage-6 reveal is intentionally watchable and trace-backed: generated mazes carry a `generationBuildTrace` with actual path-builder tiles, checkpoint targets, and shortcut bridge placements; the menu shows endpoints early, reveals the primary route, then branch/split path tiles, then shortcut bridges, clamps trail/player/goal overlays to revealed tiles, and holds menu demo movement until trace-build completion plus a short settle. The front-door title is now drawn as maze-path pieces and uses the same visible-tile progress as the path layer, so it rebuilds and deconstructs with the maze instead of staying as independent text. Generated menu bootstrap uses zero preroll, so the player stays on the generated start tile with a one-tile trail while the path is building and begins moving only after the build has settled. Generated menu AI can still show mistake/backtrack/recovery movement, but it no longer uses the legacy AI-only replay reset on the live generated menu loop; the visible loop must reach the goal before teardown. Live generated menu teardown now starts on goal arrival instead of lingering in a solved-state hold. The menu deconstructs the revealed walkable path layer while keeping the backdrop, wall field, border, title, and buttons stable; it preserves the final goal-position player/trail state, hides the player first instead of accepting the reset-frame start teleport, fades the existing trail, then removes path tiles. The next menu seed is queued during teardown, diagnostics publish a `500ms` zero-visible handoff frame, the border emits a brief cyan/mint star-burst handoff, and each new trace-backed build now starts with a matching `500ms` border-burst preroll before the first path tile appears.
- runtime diagnostics now also expose `menuDemo.route` with route length, segment count, canonical path length, traverse duration, cue counts, trail-mode counts, and AI reset cursor; the representative split-flow menu AI route is guarded below 4x the canonical solution and under 60 seconds so shortcut-heavy recovery cannot silently regress into a runaway attract loop
- runtime diagnostics now expose compact maze provenance and quality readback under `generation.maze`, including `source`, `buildKind`, size, seed, solution path length, shortcut stats, checkpoint/path-builder stats, playable-topology normalization stats, and route-quality classification; maintained `4173` browser proof confirms the menu surface reports `menu-generated` and the play route reports `play-generated` without relying on hidden window globals
- runtime diagnostics now expose compact local cycle telemetry under `cycleTelemetry`, including bounded recent completion receipts and a conservative `learning` summary signal; this is local-only observation for future tuning and does not automatically alter maze generation, controls, or visuals yet
- `npm run cycle:report` now converts raw local cycle history, runtime diagnostics, or localStorage-style exports into `mazer.cycle-learning.report.v1` Atlas-safe reports; full raw player paths stay out of the report, and the bridge is for durable tuning context rather than remote analytics or automatic adaptation
- normal player-facing `Options` now hide numeric maze/camera/color tuning by default; those advanced fields are recoverable only through an explicit `advancedOptions=1` or `devOptions=1` query flag, while `runtimeDiagnostics=1` remains data-only and does not reveal developer controls
- legacy options/pause rebuilds now defer to overlay close instead of rebuilding immediately on field commit
- in-game pause commands now route through an explicit legacy pause lifecycle contract for `Back`, `Reset`, and `Main Menu`
- features and game-modes toggles now route through an explicit legacy overlay-toggle contract, with inverted `On/Off` copy kept only where the legacy widget actually owned it
- menu-time overlay fields now route through an explicit legacy field-commit contract, separating deferred reload-on-back fields from the immediate camera-flag path
- nested overlay routing from `Options` / `Pause` back through `Features` and `Game Modes` now routes through an explicit legacy overlay-routing contract — **retired (2026-08-30):** `tests/reset/legacy-reset.test.ts:1098-1099` explicitly asserts `this.openNestedOverlay('features'...)`/`'gameModes'...)` are **absent** from `MenuScene.ts` today, confirming this nested-overlay routing was itself later superseded by the single-overlay, section-based layout (`applyOverlayToggleFieldChange`, `addSectionHeading('Controls'...)`/`('Display'...)`) the corrected "current overlay family" bullet above describes. This line is retained as history, not current routing.
- legacy-style active play start path
- active-play HUD now uses a tighter timer/goal-arrow overlay, repo-owned timer/arrow geometry, a minimal source-shaped Timer/EndArrow visual treatment, and data-only diagnostics proof that bounds the full overlay footprint without drawing debug text in the maintained play browser
- legacy-style options/features/game-mode/pause overlay structure
- repo-owned reset-lane tests in `tests/reset/`

## What is not yet 1:1

- live menu topology and active reset-lane play topology now use a source-shaped checkpoint path builder instead of the previous DFS/fixed-snapshot split, and `Backtrack()` now returns the selected next tile like the restored source; generated menu/play mazes now also reject disconnected playable-looking floor components and avoid trivially weak goals, with bounded large-scale `99` and extreme scale-`149` smoke in the normal proof spine, but exact Unreal RNG/time seeding and process-yield timing are still not claimed
- shortcut topology is stronger than the earlier browser-native random dead-end braider and now proves multiple route-bypass bands plus legacy opposite-corridor bridges in both the domain raster path and active reset-lane play snapshot path; the active reset-lane shortcut pass now consumes a duplicate-preserving `_WallArray` collected during the checkpoint path-builder's `CreatePath` equivalent, reports requested/attempted/created shortcut stats plus optional weak-route reinforcement stats, applies a small menu-only budget floor once shortcuts are enabled, and now publishes route-quality stats that require meaningful detour-bearing bypass coverage across more than one route band before calling a generated snapshot `multi-route`, but exact legacy randomness remains a partial port
- generation/reset branches, stage cadence, budget formulas, process-entry gates, the menu-demo process-8-to-process-0 handoff, shortcut-disabled stage `4 -> 6` progression, and menu stage-6 row-sliced drawing are now explicit runtime contracts
- stage `7` responsibilities, process `8` reset branches, process-0 delay entry, the menu reset handoff, shortcut-disabled stage progression, and stage `0/3/4/5/6` execution cadence are now explicit lifecycle truth
- stage-cursor diagnostics now expose queued process-0 entry and consumed stage-7 finalization, and visual/runtime diagnostics now expose the cadence-gated menu draw-stage row cursor, remaining rows, completion state, and percent progress
- the browser builder still resolves play topology as a one-shot browser-safe stage before stage-6 row reveal, so exact per-tick process-yield timing remains future gameplay/topology work rather than an unstated lifecycle claim; the visible row reveal is paced, and active play topology is now closer to the Unreal checkpoint path-builder and Backtrack return semantics than the prior DFS owner
- demo AI route/backtracking is now source-shaped instead of canonical-route-detour based, with live recovery cues, AI-only reset replay, goal reset handoff timing, `AiTilePathCheck` candidate admission, source-shaped potential-tile/path-stack backtracking, bounded first-mistake replay planning, single-timer AI movement cadence shape, live wrong-branch/backtrack/recovery diagnostics, positive visited-undo proof, menu trail-fade-tail proof, generated-menu seed-family adjacency/bounded-route proof across `16` default menu seeds plus the larger-scale case, generated-menu scale-band proof for recovery -> AI-only reset -> same-maze replay -> goal reset -> regeneration request, a reduced-motion trail-shine cadence of `8000ms` per leg / `16000ms` round trip, and a bounded `GI_MazerGameInstance.uasset` scan explicitly covered; exact Unreal material color-revert timing and the numeric Blueprint AI delay default remain open
- generated-menu navigation skill now scales from the independent AI progression track: E is boundedly imperfect, D through S follow the actual playable shortest route, rank pressure is monotonic, and diagnostics compare graph-distance progress rather than treating the generator construction trail as shortest-path truth
- active-play movement, collision, keyboard input, mobile pointer/touch input, and reset-return ownership are closer after the simultaneous-key buffer, shared latest-wins directional-intent resolver, bounded unambiguous-corner assistance, keyboard default-consumption guard, keyboard pause/restart/toggle command coverage, browser focus-loss cleanup, board-bounded pointer-vector adapter, one-active-pointer identity guard, pointer-up-outside completion path, game-out stale-pointer cleanup, generated-play solution traversal through `advanceLegacyPlayStep()`, maintained-browser `play-mode-interactive` and `mobile-touch-smoke` movement/control proof, camera-follow layer/proof alignment, axis-gated collision, single reset-request, pause-reset trail-preservation ports, and data-only input-buffer diagnostics for maintained-browser feel review
- in-game HUD is restored for the currently available source-backed browser seam: source-exact timer formatting, goal-arrow radians/degrees, proof bounds, minimal widget chrome, and diagnostics-safe docking are explicit repo-owned contracts
- active-play board material is closer after replacing square debug-cell path fills with connected corridors, moving active dynamic overlays into a corridor/inset lane, centering the player marker with a high-contrast halo/core glyph, removing the offset player shadow, and removing the static path relief shadow so the generated maze reads as a cleaner 2D surface; final old-game play-board styling is not yet screenshot-grade
- browser quit equivalence is no longer part of the active front-door contract; the old engine quit behavior remains archival legacy context only
- visual/material parity still needs another pass against screenshots and restored assets
- menu static/dynamic material, title opacity, front-door button pane fills/proportions/baseline, cyan route weight, and live player readability are closer after the corridor-frame, thinner-overlay, path-relief, title-glass, button dark-pane, material-contrast, dynamic-trail weight, button-plate proportion, button-baseline, dense-slab material, clean-2D de-depth, and centered player-marker passes, but the restored screenshots still show different exact player sprite treatment, exact maze silhouette, exact button placement, final title overlap, and tighter screenshot-grade composition than the current web runtime
- browser automation localhost still does not reliably expose the published `window.__MAZER_*` globals directly, but runtime diagnostics and visual diagnostics now have DOM-backed fallback read surfaces on the single `4173` preview server without visible debug panels, including the active maze source/build family, resolved layout/button surface, and board/HUD visual bounds needed to prove procedural menu versus play generation, active-play camera-follow alignment, and side-browser play/menu layout separation

## Current proof rule

For the reset lane, the local proof spine is:

1. `npm run legacy:extract`
2. `npm run verify`

Current `verify` means:

- `npm run test:verify`
- `npm run build`

Current `test:verify` means:

- `tests/reset`
- `tests/ai/demo-walker.test.ts`
- `tests/ai/demo-walker-known-frontier.test.ts`
- `tests/ai/demo-walker-rank-ladder.test.ts`
- `tests/ai/demo-walker-recovery-diagnostics.test.ts`
- `tests/scenes/menu-render-frame.test.ts`
- `tests/analysis/maze-cycle-telemetry-report.test.mjs`
- `tests/analysis/ai-run-corpus-audit.test.mjs`
- every normal file runs with `--maxWorkers 1 --pool=threads --poolOptions.threads.singleThread`, avoiding the default fork-worker RPC timeout on long deterministic corpus checks
- `legacy-unreal-source-fixture.test.ts` runs in the fork pool with `--maxWorkers 1`, because it deliberately verifies `process.chdir` isolation

That proof spine currently guards marker arithmetic/current-truth sync, reset and generation lifecycle, topology scale audits, menu AI recovery/replay, active play movement/HUD/reset contracts, render-frame layout/readability contracts, and the export-only AI corpus audit contract. `npm run lint` remains a separate TypeScript gate for code-edit packets and should still be run before commit when implementation files change.

Current fast iteration rule:

- `npm run verify:fast -- --list` previews the changed-file-to-proof-slice selection before spending time on checks.
- `npm run verify:fast` runs TypeScript when implementation files changed and then runs only the selected Vitest files.
- `npm run verify:fast:tests` runs the same selected Vitest slice with TypeScript skipped for same-cluster reruns after a TypeScript-backed pass already succeeded.
- `npm run verify:fast:all` runs the full reset-lane test spine without the production build.
- `npm run verify:fast -- --build` is the opt-in bridge when a bounded packet needs production bundle proof without switching back to the full reset-lane wrapper.
- `npm run verify` remains the closure/release/marker-ratchet proof and still means `test:verify` plus `build`.

The older visual matrix / Edge live proof lane is now archival for comparison, not current closure truth for the reset lane.

## Viewport Layout Contract

- Browser viewport geometry is now owned by `src/boot/viewportGeometry.ts`. It normalizes layout and usable visual viewport measurements, safe-area insets, effective content bounds, device pixel ratio, and phone-landscape classification before publishing one snapshot to CSS, Phaser, `MenuScene`, and visual diagnostics. Derived geometry is intentionally not persisted: Mazer has no supported free-positioned UI intent, so raw dimensions and coordinates must be recomputed after resize, maximize/restore, browser chrome changes, or rotation.
- Authenticated account progression and game-toggle settings hydrate from Mazer Supabase before Phaser creates the first maze, and `MenuScene` consumes that exact preloaded account state before its first generation instead of leaving the local Level-1 placeholder active. Profile/settings writes and explicit reset replacement retain monotonic revision checks. Completed runs no longer use the direct progression-row PATCH path: each accepted player or menu-AI completion is first written to an account-scoped local outbox with one stable run UUID, then submitted to the server completion RPC until the same UUID is accepted exactly once. Failed or interrupted attempts remain queued and diagnostics expose `pending`, `synced`, pending-count, recovered-count, and exact technical failure truth without showing routine cloud-storage messages to players. Fine-pointer portrait browser panes from `431px` through `600px` reuse the phone maze/control composition, while the existing `390x844` and all coarse-pointer/touch device paths remain byte-for-byte on their prior layout branch.
- Player and menu-AI progression start at Level 1 and each advance their own visible completion ordinal by exactly one for every accepted completion. Those ordinals have no application-level 99 ceiling, never derive from run score or target complexity, and monotonic local/cloud reconciliation cannot lower them. JavaScript, local storage, JSON, RPC payloads, diagnostics, and UI use canonical unsigned decimal strings for these ordinals; exact arithmetic/comparison uses `bigint` internally, so values beyond `Number.MAX_SAFE_INTEGER` are never rounded, clamped, or serialized as lossy numbers. The server RPC locks the owning row, validates the current ordinal/revision, inserts the bounded receipt, and advances ordinal plus separately bounded difficulty in one transaction. A reload may reconstruct a missing outbox only when local telemetry contains the exact uninterrupted receipt sequence from the cloud cursor to the local cursor; missing or contradictory evidence keeps local progress visible and holds recovery instead of guessing. Target complexity, rank color, hazards, maze scale, and topology pressure remain a separate `8..400` difficulty track; the 2026-08-23 pacing repair applies that pressure at half speed and never feeds a large visible ordinal back into geometry. Historical play receipts that lack the modern run UUID/ruleset/recipe tuple remain conserved completion evidence for non-destructive migration classification; their nullable metadata must never be interpreted as proof that no completion happened. Receipt count is not converted into a guessed level: exact legacy scalar/JSON preimages remain authoritative for restoration, while accounts with zero play receipts may be rebased to the source Level-1 baseline. Deterministic proof advances through the real completion path for Levels 1–10, yielding target complexity `8,12,...,44`, tutorial then starter bands, monotonic board dimensions with no jump larger than two cells, and in-bounds desktop/mobile layouts. Player completion, local persistence, and cloud-sync outcomes remain silent in the game surface; active play's compact level glyph is the durable player-facing signal, while the menu uses the independent AI ordinal.
- Leaderboard pagination renders exactly ten users per page. Menu and play board layers remain at neutral visual scale `1x`; there is no maze-load or level-derived implicit zoom. Only the persisted user-controlled Board Zoom value (`camScale`, default `0`) can alter generated board scale, and visual diagnostics publish the user value, container scale, and `implicitZoomApplied=false` for route-aware production proof.
- Phone landscape no longer rotates `#app`. The runtime requests portrait where supported; otherwise a single accessible rotate-device shell blocks and sleeps gameplay until portrait returns. Desktop landscape stays normal. The first `390x844 -> 844x390 -> 390x844` contract proof restored the original portrait canvas/board geometry with `transform: none`, native DPR-2 backing, and no overlap/offscreen diagnostics. See `docs/ops/MAZER-VIEWPORT-LAYOUT-CONTRACT-PACKET-2026-07-11.md`.
- The maintained `390x844` authenticated Options/Play/Pause capture now checks a single normalized viewport owner across those surfaces. The menu shows only the compact menu-AI level glyph at the top-left, paired with the icon-only Settings cog; active play shows the player level glyph at the top-left, timer at center, and icon-only Pause control at the top-right. Neither surface exposes a score/rank panel or completion/sync message stack. Options and Pause share a scroll-body/pinned-footer composition, reserve the guide from the scroll rail, and suppress empty clipped guide fragments. **Correction (2026-08-30):** this line previously also said they "separate the Move Speed label and slider lanes" — stale; every current `buildOptionsOverlay()`/`buildPauseOverlay()` call site passes `includeMovementSpeed: false`, so neither surface renders that slider today (see the corrected bullet above). The page, install metadata, and Phaser shell use one near-black safe-area color so browser chrome or PWA insets cannot expose purple bands.
- Menu and play layout now reserve ordered content lanes before choosing maze dimensions. Menu uses title, maze, rank, and action lanes; play uses a slim level/timer/Pause HUD, maze, and portrait controls lanes. The maze is capped by the remaining lane rectangle, and the pure solver restores identical bounds after narrow-to-wide-to-narrow transitions. The app shell also anchors to the normalized visual-viewport plus safe-area origin instead of only shrinking its width and height. The platform proof preset covers iPhone Dynamic Island, Android cutout, macOS browser, and Windows browser dimensions. See `docs/ops/MAZER-CROSS-PLATFORM-UI-FOLLOWUP-PACKET-2026-07-11.md`.

Localhost operation rule:

- keep one maintained preview server on `http://127.0.0.1:4173/`
- prefer the in-app browser as the live human proof surface for the current branch
- reload that single tab after code changes instead of scattering proof across multiple localhost ports unless a packet explicitly needs another surface
- when judging legacy desktop screenshot parity from the side browser, use a temporary wider viewport on that same tab, then reset it instead of opening extra browser instances

## Current execution rule

- Do not run broad visual polish passes by default.
- Do not ratchet the active mechanics/mobile marker without a bounded segment proof packet.
- Do not restore the old `97%` or retired `93%` legacy visual 1:1 marker unless screenshot-grade parity is explicitly reopened.
- Lock work one module at a time:
  - maze generation / topology quality
  - demo AI route / pacing
  - active play movement / collision / reset
  - mobile input / layout / board readability
  - player / trail / HUD readability
- For the active module, name one owner chain, one proof surface, and one mechanics or mobile-readability miss before editing.

## Release rule

- Do not claim production parity with the old game unless the target is explicitly reopened and proved.
- Do not claim active mechanics/mobile completion until the active marker gaps are closed with proof.
- Do not reopen the old recovery/product shell as if it were the authoritative direction.

## Still open

- exact demo AI route/backtrack internals beyond the restored front-door recovery lane, cue/pacing surface, scale-band AI-only reset/replay/goal-regeneration proof, `AiTilePathCheck` candidate gate, and connected reacquire path
- exact active-play feel beyond the keyboard/pointer/focus-loss movement adapters, live keyboard/touch interaction proof, one-active-pointer mobile guard, pause-reset trail preservation, camera-follow layer alignment, active dynamic corridor overlays, extracted source-exact timer/goal-arrow contract, tighter compact overlay, minimal HUD widget chrome, data-only diagnostics proof, and full proof bounds
- exact generation/reset-flow parity beyond the queued request contract
- final mobile-clean board sizing plus wider player/trail readability proof beyond the now-separated compact touch-control lane and ultra-narrow play-route pass
